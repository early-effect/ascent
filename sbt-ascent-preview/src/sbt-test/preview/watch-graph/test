# fileInputs must match Hello.scala and index.html. Nested runTask is the rebuild loop.
> checkWatchGraph
> proveNestedRunTask
> ascentPreviewOnce
> recordOnceStamp
# Watch is a bg job; the next lines MUST run (old loop would hang here).
> ascentPreview
> compile
> Test / compile
> test
# Forced compile: wipe products + zinc, then compile. Not a cached no-op.
> forceCompile
> test
# File change, then yield the command queue so the queued rebuild can run.
> rewriteHello
> eval Thread.sleep(5000L)
> checkWatchFired
> test
> forceCompile
> checkWatchFired
> ascentPreviewStop
