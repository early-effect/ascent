// AUTO-GENERATED by ascent-domgen. Do not edit by hand — see domgen/README.md to regenerate.

package ascent.css

import StylesFoundation.*

/** Generated CSS property catalog. Mixed into `object Styles` via `Styles extends StylesGenerated`, so users see them
  * at the same call site as the hand-written foundation traits (Length / Percent / ColorLike / Auto / None / Normal /
  * Numeric) and the generated value-type traits (see [[StylesValueTraits]]).
  */
trait StylesGenerated extends StylesValueTraits:
  /** `z-index` — `auto | <integer> | inherit`. Initial: `auto`. Inherited: no. */
  object zIndex extends DS("z-index") with Auto with Numeric

  /** `page-break-before` — `auto | always | avoid | left | right | inherit`. Initial: `auto`. Inherited: no. */
  object pageBreakBefore extends DS("page-break-before") with Auto:
    def always: Declaration = apply("always")
    def avoid: Declaration  = apply("avoid")
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")

  /** `page-break-after` — `auto | always | avoid | left | right | inherit`. Initial: `auto`. Inherited: no. */
  object pageBreakAfter extends DS("page-break-after") with Auto:
    def always: Declaration = apply("always")
    def avoid: Declaration  = apply("avoid")
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")

  /** `page-break-inside` — `avoid | auto | inherit`. Initial: `auto`. Inherited: no. */
  object pageBreakInside extends DS("page-break-inside") with Auto:
    def avoid: Declaration  = apply("avoid")
    def always: Declaration = apply("always")
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")

  /** `cx` — `<length-percentage>`. Initial: `0`. Inherited: no. */
  object cx extends DS("cx") with Length with Percent

  /** `cy` — `<length-percentage>`. Initial: `0`. Inherited: no. */
  object cy extends DS("cy") with Length with Percent

  /** `r` — `<length-percentage>`. Initial: `0`. Inherited: no. */
  object r extends DS("r") with Length with Percent

  /** `rx` — `<length-percentage> | auto`. Initial: `auto`. Inherited: no. */
  object rx extends DS("rx") with Length with Percent with Auto

  /** `ry` — `<length-percentage> | auto`. Initial: `auto`. Inherited: no. */
  object ry extends DS("ry") with Length with Percent with Auto

  /** `x` — `<length-percentage>`. Initial: `0`. Inherited: no. */
  object x extends DS("x") with Length with Percent

  /** `y` — `<length-percentage>`. Initial: `0`. Inherited: no. */
  object y extends DS("y") with Length with Percent

  /** `vector-effect` — `none | non-scaling-stroke | non-scaling-size | non-rotation | fixed-position`. Initial: `none`.
    * Inherited: no.
    */
  object vectorEffect extends DS("vector-effect") with None:
    def nonScalingStroke: Declaration = apply("non-scaling-stroke")
    def nonScalingSize: Declaration   = apply("non-scaling-size")
    def nonRotation: Declaration      = apply("non-rotation")
    def fixedPosition: Declaration    = apply("fixed-position")

  /** `d` — `none | <string>`. Initial: `none`. Inherited: no. */
  object d extends DS("d") with None

  /** `path-length` — `none | <number [0,∞]>`. Initial: `none`. Inherited: no. */
  object pathLength extends DS("path-length") with None with Numeric

  /** `text-anchor` — `start | middle | end`. Initial: `start`. Inherited: yes. */
  object textAnchor extends DS("text-anchor"):
    def start: Declaration  = apply("start")
    def middle: Declaration = apply("middle")
    def end: Declaration    = apply("end")

  /** `fill` — `<paint>`. Initial: `black`. Inherited: yes. */
  object fill extends DS("fill") with None with Imageish

  /** `stroke` — `<paint>`. Initial: `none`. Inherited: yes. */
  object stroke extends DS("stroke") with None with Imageish

  /** `marker-start` — `none | <marker-ref>`. Initial: `none`. Inherited: yes. */
  object markerStart extends DS("marker-start") with None

  /** `marker-mid` — `none | <marker-ref>`. Initial: `none`. Inherited: yes. */
  object markerMid extends DS("marker-mid") with None

  /** `marker-end` — `none | <marker-ref>`. Initial: `none`. Inherited: yes. */
  object markerEnd extends DS("marker-end") with None

  /** `marker` — `none | <marker-ref>`. Initial: `not defined for shorthand properties`. Inherited: yes. */
  object marker extends DS("marker") with None

  /** `paint-order` — `normal | [ fill || stroke || markers ]`. Initial: `normal`. Inherited: yes. */
  object paintOrder extends DS("paint-order") with Normal:
    def fill: Declaration    = apply("fill")
    def stroke: Declaration  = apply("stroke")
    def markers: Declaration = apply("markers")

  /** `color-interpolation` — `auto | sRGB | linearRGB`. Initial: `sRGB`. Inherited: yes. */
  object colorInterpolation extends DS("color-interpolation") with Auto:
    def sRGB: Declaration      = apply("sRGB")
    def linearRGB: Declaration = apply("linearRGB")

  /** `shape-rendering` — `auto | optimizeSpeed | crispEdges | geometricPrecision`. Initial: `auto`. Inherited: yes. */
  object shapeRendering extends DS("shape-rendering") with Auto:
    def optimizeSpeed: Declaration      = apply("optimizeSpeed")
    def crispEdges: Declaration         = apply("crispEdges")
    def geometricPrecision: Declaration = apply("geometricPrecision")

  /** `text-rendering` — `auto | optimizeSpeed | optimizeLegibility | geometricPrecision`. Initial: `auto`. Inherited:
    * yes.
    */
  object textRendering extends DS("text-rendering") with Auto:
    def optimizeSpeed: Declaration      = apply("optimizeSpeed")
    def optimizeLegibility: Declaration = apply("optimizeLegibility")
    def geometricPrecision: Declaration = apply("geometricPrecision")

  /** `pointer-events` —
    * `auto | bounding-box | visiblePainted | visibleFill | visibleStroke | visible | painted | fill | stroke | all | none`.
    * Initial: `auto`. Inherited: yes.
    */
  object pointerEvents extends DS("pointer-events") with Auto with None:
    def boundingBox: Declaration    = apply("bounding-box")
    def visiblePainted: Declaration = apply("visiblePainted")
    def visibleFill: Declaration    = apply("visibleFill")
    def visibleStroke: Declaration  = apply("visibleStroke")
    def visible: Declaration        = apply("visible")
    def painted: Declaration        = apply("painted")
    def fill: Declaration           = apply("fill")
    def stroke: Declaration         = apply("stroke")
    def all: Declaration            = apply("all")
  end pointerEvents

  /** `stop-color` — `<'color'>`. */
  object stopColor extends DS("stop-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end stopColor

  /** `stop-opacity` — `<'opacity'>`. */
  object stopOpacity extends DS("stop-opacity") with Numeric

  /** `trigger-scope` — `none | all | <dashed-ident>#`. Initial: `none`. Inherited: no. */
  object triggerScope extends DS("trigger-scope") with None:
    def all: Declaration = apply("all")

  /** `timeline-trigger-name` — `none | <dashed-ident>#`. Initial: `none`. Inherited: no. */
  object timelineTriggerName extends DS("timeline-trigger-name") with None

  /** `timeline-trigger-source` — `<single-animation-timeline>#`. Initial: `auto`. Inherited: no. */
  object timelineTriggerSource extends DS("timeline-trigger-source") with Auto with None

  /** `timeline-trigger-activation-range` —
    * `[ <'timeline-trigger-activation-range-start'> <'timeline-trigger-activation-range-end'>? ]#`. Initial:
    * `see individual properties`. Inherited: see individual properties.
    */
  object timelineTriggerActivationRange
      extends DS("timeline-trigger-activation-range")
      with Length
      with Percent
      with Normal

  /** `timeline-trigger-activation-range-start` —
    * `[ normal | <length-percentage> | <timeline-range-name> <length-percentage>? ]#`. Initial: `normal`. Inherited:
    * no.
    */
  object timelineTriggerActivationRangeStart
      extends DS("timeline-trigger-activation-range-start")
      with Length
      with Percent
      with Normal

  /** `timeline-trigger-activation-range-end` —
    * `[ normal | <length-percentage> | <timeline-range-name> <length-percentage>? ]#`. Initial: `normal`. Inherited:
    * no.
    */
  object timelineTriggerActivationRangeEnd
      extends DS("timeline-trigger-activation-range-end")
      with Length
      with Percent
      with Normal

  /** `timeline-trigger-active-range` —
    * `[ <'timeline-trigger-active-range-start'> <'timeline-trigger-active-range-end'>? ]#`. Initial:
    * `see individual properties`. Inherited: see individual properties.
    */
  object timelineTriggerActiveRange
      extends DS("timeline-trigger-active-range")
      with Length
      with Percent
      with Auto
      with Normal

  /** `timeline-trigger-active-range-start` —
    * `[ auto | normal | <length-percentage> | <timeline-range-name> <length-percentage>? ]#`. Initial: `auto`.
    * Inherited: no.
    */
  object timelineTriggerActiveRangeStart
      extends DS("timeline-trigger-active-range-start")
      with Length
      with Percent
      with Auto
      with Normal

  /** `timeline-trigger-active-range-end` —
    * `[ auto | normal | <length-percentage> | <timeline-range-name> <length-percentage>? ]#`. Initial: `auto`.
    * Inherited: no.
    */
  object timelineTriggerActiveRangeEnd
      extends DS("timeline-trigger-active-range-end")
      with Length
      with Percent
      with Auto
      with Normal

  /** `timeline-trigger` —
    * `none | [ <'timeline-trigger-name'> <'timeline-trigger-source'> <'timeline-trigger-activation-range'> [ / <'timeline-trigger-active-range'> ]? ]#`.
    * Initial: `see individual properties`. Inherited: see individual propert
    */
  object timelineTrigger extends DS("timeline-trigger") with Auto with None

  /** `event-trigger-name` — `none | <dashed-ident>#`. Initial: `none`. Inherited: no. */
  object eventTriggerName extends DS("event-trigger-name") with None

  /** `event-trigger-source` — `[ none | <event-trigger-event>+ [ / <event-trigger-event>+ ]? ]#`. Initial: `none`.
    * Inherited: no.
    */
  object eventTriggerSource extends DS("event-trigger-source") with None

  /** `event-trigger` — `none | [ <'event-trigger-name'> <'event-trigger-source'> ]#`. Initial: `none`. Inherited: no.
    */
  object eventTrigger extends DS("event-trigger") with None

  /** `animation-trigger` — `[ none | [ <dashed-ident> <animation-action>+ ]+ ]#`. Initial: `none`. Inherited: no. */
  object animationTrigger extends DS("animation-trigger") with None with AnimationAction

  /** `-webkit-text-fill-color` — `<color>`. Initial: `currentcolor`. Inherited: yes. */
  object WebkitTextFillColor extends DS("-webkit-text-fill-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end WebkitTextFillColor

  /** `-webkit-text-stroke-color` — `<color>`. Initial: `currentcolor`. Inherited: yes. */
  object WebkitTextStrokeColor extends DS("-webkit-text-stroke-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end WebkitTextStrokeColor

  /** `-webkit-text-stroke-width` — `<line-width>`. Initial: `0`. Inherited: yes. */
  object WebkitTextStrokeWidth extends DS("-webkit-text-stroke-width") with Length with LineWidth

  /** `-webkit-text-stroke` — `<line-width> || <color>`. Initial: `See individual properties`. Inherited: yes. */
  object WebkitTextStroke extends DS("-webkit-text-stroke") with Length with None with ColorLike with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end WebkitTextStroke

  /** `touch-action` —
    * `auto | none | [ [ pan-x | pan-left | pan-right ] || [ pan-y | pan-up | pan-down ] || pinch-zoom ] | manipulation`.
    * Initial: `auto`. Inherited: no.
    */
  object touchAction extends DS("touch-action") with Auto with None:
    def panX: Declaration         = apply("pan-x")
    def panLeft: Declaration      = apply("pan-left")
    def panRight: Declaration     = apply("pan-right")
    def panY: Declaration         = apply("pan-y")
    def panUp: Declaration        = apply("pan-up")
    def panDown: Declaration      = apply("pan-down")
    def pinchZoom: Declaration    = apply("pinch-zoom")
    def manipulation: Declaration = apply("manipulation")

  /** `-webkit-align-items` — `normal | stretch | <baseline-position> | <overflow-position>? <self-position>`. */
  object WebkitAlignItems
      extends DS("-webkit-align-items")
      with Normal
      with BaselinePosition
      with OverflowPosition
      with SelfPosition:
    def stretch: Declaration = apply("stretch")

  /** `-webkit-align-content` —
    * `normal | <baseline-position> | <content-distribution> | <overflow-position>? <content-position>`.
    */
  object WebkitAlignContent
      extends DS("-webkit-align-content")
      with Normal
      with BaselinePosition
      with ContentDistribution
      with ContentPosition
      with OverflowPosition

  /** `-webkit-align-self` — `auto | <overflow-position>? [ normal | <self-position> ]| stretch | <baseline-position>`.
    */
  object WebkitAlignSelf
      extends DS("-webkit-align-self")
      with Auto
      with Normal
      with BaselinePosition
      with OverflowPosition
      with SelfPosition:
    def stretch: Declaration = apply("stretch")

  /** `-webkit-animation-name` — `[ none | <keyframes-name> ]#`. */
  object WebkitAnimationName extends DS("-webkit-animation-name") with None

  /** `-webkit-animation-duration` — `<time [0s,∞]>#`. */
  object WebkitAnimationDuration extends DS("-webkit-animation-duration") with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `-webkit-animation-timing-function` — `<easing-function>#`. */
  object WebkitAnimationTimingFunction extends DS("-webkit-animation-timing-function") with TimingFunctionish

  /** `-webkit-animation-iteration-count` — `<single-animation-iteration-count>#`. */
  object WebkitAnimationIterationCount
      extends DS("-webkit-animation-iteration-count")
      with Numeric
      with SingleAnimationIterationCount

  /** `-webkit-animation-direction` — `<single-animation-direction>#`. */
  object WebkitAnimationDirection extends DS("-webkit-animation-direction") with Normal with SingleAnimationDirection

  /** `-webkit-animation-play-state` — `<single-animation-play-state>#`. */
  object WebkitAnimationPlayState extends DS("-webkit-animation-play-state") with SingleAnimationPlayState

  /** `-webkit-animation-delay` — `<time>#`. */
  object WebkitAnimationDelay extends DS("-webkit-animation-delay") with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `-webkit-animation-fill-mode` — `<single-animation-fill-mode>#`. */
  object WebkitAnimationFillMode extends DS("-webkit-animation-fill-mode") with None with SingleAnimationFillMode

  /** `-webkit-animation` — `<single-animation>#`. */
  object WebkitAnimation extends DS("-webkit-animation") with None with Timeish with TimingFunctionish

  /** `-webkit-backface-visibility` — `visible | hidden`. */
  object WebkitBackfaceVisibility extends DS("-webkit-backface-visibility"):
    def visible: Declaration = apply("visible")
    def hidden: Declaration  = apply("hidden")

  /** `-webkit-background-clip` — `<visual-box>#`. */
  object WebkitBackgroundClip extends DS("-webkit-background-clip") with VisualBox

  /** `-webkit-background-origin` — `<visual-box>#`. */
  object WebkitBackgroundOrigin extends DS("-webkit-background-origin") with VisualBox

  /** `-webkit-background-size` — `<bg-size>#`. */
  object WebkitBackgroundSize extends DS("-webkit-background-size") with Length with Percent with Auto with BgSize

  /** `-webkit-border-bottom-left-radius` — `<border-radius>`. */
  object WebkitBorderBottomLeftRadius extends DS("-webkit-border-bottom-left-radius")

  /** `-webkit-border-bottom-right-radius` — `<border-radius>`. */
  object WebkitBorderBottomRightRadius extends DS("-webkit-border-bottom-right-radius")

  /** `-webkit-border-top-left-radius` — `<border-radius>`. */
  object WebkitBorderTopLeftRadius extends DS("-webkit-border-top-left-radius")

  /** `-webkit-border-top-right-radius` — `<border-radius>`. */
  object WebkitBorderTopRightRadius extends DS("-webkit-border-top-right-radius")

  /** `-webkit-border-radius` — `<length-percentage [0,∞]>{1,4} [ / <length-percentage [0,∞]>{1,4} ]?`. */
  object WebkitBorderRadius extends DS("-webkit-border-radius") with Length with Percent with LengthBox

  /** `-webkit-box-shadow` — `<spread-shadow>#`. */
  object WebkitBoxShadow extends DS("-webkit-box-shadow") with Length with None with Shadowish

  /** `-webkit-box-sizing` — `content-box | border-box`. */
  object WebkitBoxSizing extends DS("-webkit-box-sizing"):
    def contentBox: Declaration = apply("content-box")
    def borderBox: Declaration  = apply("border-box")

  /** `-webkit-flex` — `none | [ <'flex-grow'> <'flex-shrink'>? || <'flex-basis'> ]`. */
  object WebkitFlex extends DS("-webkit-flex") with Auto with None with Numeric:
    def content: Declaration = apply("content")

  /** `-webkit-flex-basis` — `content | <'width'>`. */
  object WebkitFlexBasis extends DS("-webkit-flex-basis") with Length with Percent with Auto:
    def content: Declaration    = apply("content")
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `-webkit-flex-direction` — `row | row-reverse | column | column-reverse`. */
  object WebkitFlexDirection extends DS("-webkit-flex-direction"):
    def row: Declaration           = apply("row")
    def rowReverse: Declaration    = apply("row-reverse")
    def column: Declaration        = apply("column")
    def columnReverse: Declaration = apply("column-reverse")

  /** `-webkit-flex-flow` — `<'flex-direction'> || <'flex-wrap'>`. */
  object WebkitFlexFlow extends DS("-webkit-flex-flow"):
    def row: Declaration           = apply("row")
    def rowReverse: Declaration    = apply("row-reverse")
    def column: Declaration        = apply("column")
    def columnReverse: Declaration = apply("column-reverse")
    def nowrap: Declaration        = apply("nowrap")
    def wrap: Declaration          = apply("wrap")
    def wrapReverse: Declaration   = apply("wrap-reverse")
    def balance: Declaration       = apply("balance")

  /** `-webkit-flex-grow` — `<number [0,∞]>`. */
  object WebkitFlexGrow extends DS("-webkit-flex-grow") with Numeric

  /** `-webkit-flex-shrink` — `<number [0,∞]>`. */
  object WebkitFlexShrink extends DS("-webkit-flex-shrink") with Numeric

  /** `-webkit-flex-wrap` — `nowrap | [ wrap | wrap-reverse ] || balance`. */
  object WebkitFlexWrap extends DS("-webkit-flex-wrap"):
    def nowrap: Declaration      = apply("nowrap")
    def wrap: Declaration        = apply("wrap")
    def wrapReverse: Declaration = apply("wrap-reverse")
    def balance: Declaration     = apply("balance")

  /** `-webkit-filter` — `none | <filter-value-list>`. */
  object WebkitFilter extends DS("-webkit-filter") with None with Filterish

  /** `-webkit-justify-content` —
    * `normal | <content-distribution> | <overflow-position>? [ <content-position> | left | right ]`.
    */
  object WebkitJustifyContent
      extends DS("-webkit-justify-content")
      with Normal
      with ContentDistribution
      with ContentPosition
      with OverflowPosition:
    def left: Declaration  = apply("left")
    def right: Declaration = apply("right")

  /** `-webkit-mask` — `<mask-layer>#`. */
  object WebkitMask extends DS("-webkit-mask") with MaskLayer with Positionish:
    override def borderBox: Declaration  = apply("border-box")
    override def contentBox: Declaration = apply("content-box")
    override def paddingBox: Declaration = apply("padding-box")

  /** `-webkit-mask-box-image` —
    * `<'mask-border-source'> || <'mask-border-slice'> [ / <'mask-border-width'>? [ / <'mask-border-outset'> ]? ]? || <'mask-border-repeat'> || <'mask-border-mode'>`.
    */
  object WebkitMaskBoxImage extends DS("-webkit-mask-box-image"):
    def fill: Declaration      = apply("fill")
    def luminance: Declaration = apply("luminance")
    def alpha: Declaration     = apply("alpha")

  /** `-webkit-mask-box-image-outset` — `<'border-image-outset'>`. */
  object WebkitMaskBoxImageOutset extends DS("-webkit-mask-box-image-outset") with Length with Numeric:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end WebkitMaskBoxImageOutset

  /** `-webkit-mask-box-image-repeat` — `<'border-image-repeat'>`. */
  object WebkitMaskBoxImageRepeat extends DS("-webkit-mask-box-image-repeat"):
    def stretch: Declaration = apply("stretch")
    def repeat: Declaration  = apply("repeat")
    def round: Declaration   = apply("round")
    def space: Declaration   = apply("space")

  /** `-webkit-mask-box-image-slice` — `<'border-image-slice'>`. */
  object WebkitMaskBoxImageSlice extends DS("-webkit-mask-box-image-slice") with Percent with Numeric:
    def fill: Declaration = apply("fill")

  /** `-webkit-mask-box-image-source` — `<'border-image-source'>`. */
  object WebkitMaskBoxImageSource extends DS("-webkit-mask-box-image-source") with None with Imageish

  /** `-webkit-mask-box-image-width` — `<'border-image-width'>`. */
  object WebkitMaskBoxImageWidth
      extends DS("-webkit-mask-box-image-width")
      with Length
      with Percent
      with Auto
      with Numeric

  /** `-webkit-mask-clip` — `[ <coord-box> | no-clip ]#`. */
  object WebkitMaskClip extends DS("-webkit-mask-clip") with CoordBox:
    def noClip: Declaration = apply("no-clip")

  /** `-webkit-mask-composite` — `<compositing-operator>#`. */
  object WebkitMaskComposite extends DS("-webkit-mask-composite") with CompositingOperator

  /** `-webkit-mask-image` — `<mask-reference>#`. */
  object WebkitMaskImage extends DS("-webkit-mask-image") with None with Imageish

  /** `-webkit-mask-origin` — `<coord-box>#`. */
  object WebkitMaskOrigin extends DS("-webkit-mask-origin") with CoordBox

  /** `-webkit-mask-position` — `<position>#`. */
  object WebkitMaskPosition extends DS("-webkit-mask-position") with Length with Percent with Positionish:
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")

  /** `-webkit-mask-repeat` — `<repeat-style>#`. */
  object WebkitMaskRepeat extends DS("-webkit-mask-repeat") with RepeatStyle

  /** `-webkit-mask-size` — `<bg-size>#`. */
  object WebkitMaskSize extends DS("-webkit-mask-size") with Length with Percent with Auto with BgSize

  /** `-webkit-order` — `<integer>`. */
  object WebkitOrder extends DS("-webkit-order") with Numeric

  /** `-webkit-perspective` — `none | <length [0,∞]>`. */
  object WebkitPerspective extends DS("-webkit-perspective") with Length with None:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end WebkitPerspective

  /** `-webkit-perspective-origin` — `<position>`. */
  object WebkitPerspectiveOrigin extends DS("-webkit-perspective-origin") with Length with Percent with Positionish:
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")

  /** `-webkit-transform-origin` — `[ left | center | right | top | bottom | <length-percentage> ] | [ left | center |
    * right | <length-percentage> ] [ top | center | bottom | <length-percentage> ] <length>? | [ [ center | left |
    * right ] && [ cent
    */
  object WebkitTransformOrigin extends DS("-webkit-transform-origin") with Length with Percent:
    def left: Declaration   = apply("left")
    def center: Declaration = apply("center")
    def right: Declaration  = apply("right")
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")
    def rex: Declaration    = apply("rex")
    def cap: Declaration    = apply("cap")
    def rcap: Declaration   = apply("rcap")
    def rch: Declaration    = apply("rch")
    def ic: Declaration     = apply("ic")
    def ric: Declaration    = apply("ric")
    def lh: Declaration     = apply("lh")
    def rlh: Declaration    = apply("rlh")
    def svw: Declaration    = apply("svw")
    def lvw: Declaration    = apply("lvw")
    def dvw: Declaration    = apply("dvw")
    def svh: Declaration    = apply("svh")
    def lvh: Declaration    = apply("lvh")
    def dvh: Declaration    = apply("dvh")
    def vi: Declaration     = apply("vi")
    def svi: Declaration    = apply("svi")
    def lvi: Declaration    = apply("lvi")
    def dvi: Declaration    = apply("dvi")
    def vb: Declaration     = apply("vb")
    def svb: Declaration    = apply("svb")
    def lvb: Declaration    = apply("lvb")
    def dvb: Declaration    = apply("dvb")
    def svmin: Declaration  = apply("svmin")
    def lvmin: Declaration  = apply("lvmin")
    def dvmin: Declaration  = apply("dvmin")
    def svmax: Declaration  = apply("svmax")
    def lvmax: Declaration  = apply("lvmax")
    def dvmax: Declaration  = apply("dvmax")
    def Q: Declaration      = apply("Q")
  end WebkitTransformOrigin

  /** `-webkit-transform-style` — `flat | preserve-3d`. */
  object WebkitTransformStyle extends DS("-webkit-transform-style"):
    def flat: Declaration       = apply("flat")
    def preserve3d: Declaration = apply("preserve-3d")

  /** `-webkit-transform` — `none | <transform-list>`. */
  object WebkitTransform extends DS("-webkit-transform") with None with Transformish

  /** `-webkit-transition-delay` — `<time>#`. */
  object WebkitTransitionDelay extends DS("-webkit-transition-delay") with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `-webkit-transition-duration` — `<time [0s,∞]>#`. */
  object WebkitTransitionDuration extends DS("-webkit-transition-duration") with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `-webkit-transition-property` — `none | <single-transition-property>#`. */
  object WebkitTransitionProperty extends DS("-webkit-transition-property") with None with SingleTransitionProperty

  /** `-webkit-transition-timing-function` — `<easing-function>#`. */
  object WebkitTransitionTimingFunction extends DS("-webkit-transition-timing-function") with TimingFunctionish

  /** `-webkit-transition` — `<single-transition>#`. */
  object WebkitTransition
      extends DS("-webkit-transition")
      with None
      with Timeish
      with TimingFunctionish
      with Transitionish

  /** `-webkit-text-size-adjust` — `auto | none | <percentage [0,∞]>`. */
  object WebkitTextSizeAdjust extends DS("-webkit-text-size-adjust") with Percent with Auto with None

  object WebkitBoxAlign extends DS("-webkit-box-align")

  object WebkitBoxFlex extends DS("-webkit-box-flex")

  object WebkitBoxOrdinalGroup extends DS("-webkit-box-ordinal-group")

  object WebkitBoxOrient extends DS("-webkit-box-orient")

  object WebkitBoxPack extends DS("-webkit-box-pack")

  /** `mix-blend-mode` — `<blend-mode> | plus-lighter`. Initial: `normal`. Inherited: no. */
  object mixBlendMode extends DS("mix-blend-mode") with Normal with BlendMode:
    def plusLighter: Declaration = apply("plus-lighter")

  /** `isolation` — `<isolation-mode>`. Initial: `auto`. Inherited: no. */
  object isolation extends DS("isolation") with Auto with IsolationMode

  /** `background-blend-mode` — `<'mix-blend-mode'>#`. Initial: `normal`. Inherited: no. */
  object backgroundBlendMode extends DS("background-blend-mode") with Normal with BlendMode:
    def plusLighter: Declaration = apply("plus-lighter")

  /** `align-content` —
    * `normal | <baseline-position> | <content-distribution> | <overflow-position>? <content-position>`. Initial:
    * `normal`. Inherited: no.
    */
  object alignContent
      extends DS("align-content")
      with Normal
      with BaselinePosition
      with ContentDistribution
      with ContentPosition
      with OverflowPosition

  /** `justify-content` —
    * `normal | <content-distribution> | <overflow-position>? [ <content-position> | left | right ]`. Initial: `normal`.
    * Inherited: no.
    */
  object justifyContent
      extends DS("justify-content")
      with Normal
      with ContentDistribution
      with ContentPosition
      with OverflowPosition:
    def left: Declaration  = apply("left")
    def right: Declaration = apply("right")

  /** `place-content` — `<'align-content'> <'justify-content'>?`. Initial: `normal`. Inherited: no. */
  object placeContent
      extends DS("place-content")
      with Normal
      with BaselinePosition
      with ContentDistribution
      with ContentPosition
      with OverflowPosition:
    def left: Declaration  = apply("left")
    def right: Declaration = apply("right")

  /** `justify-self` —
    * `auto | <overflow-position>? [ normal | <self-position> | left | right ] | stretch | <baseline-position>`.
    * Initial: `auto`. Inherited: no.
    */
  object justifySelf
      extends DS("justify-self")
      with Auto
      with Normal
      with BaselinePosition
      with OverflowPosition
      with SelfPosition:
    def left: Declaration    = apply("left")
    def right: Declaration   = apply("right")
    def stretch: Declaration = apply("stretch")
  end justifySelf

  /** `align-self` — `auto | <overflow-position>? [ normal | <self-position> ]| stretch | <baseline-position>`. Initial:
    * `auto`. Inherited: no.
    */
  object alignSelf
      extends DS("align-self")
      with Auto
      with Normal
      with BaselinePosition
      with OverflowPosition
      with SelfPosition:
    def stretch: Declaration = apply("stretch")

  /** `place-self` — `<'align-self'> <'justify-self'>?`. Initial: `auto`. Inherited: no. */
  object placeSelf
      extends DS("place-self")
      with Auto
      with Normal
      with BaselinePosition
      with OverflowPosition
      with SelfPosition:
    def stretch: Declaration = apply("stretch")
    def left: Declaration    = apply("left")
    def right: Declaration   = apply("right")
  end placeSelf

  /** `justify-items` —
    * `normal | stretch | <baseline-position> | <overflow-position>? [ <self-position> | left | right ] | legacy | legacy && [ left | right | center ]`.
    * Initial: `legacy`. Inherited: no.
    */
  object justifyItems
      extends DS("justify-items")
      with Normal
      with BaselinePosition
      with OverflowPosition
      with SelfPosition:
    def stretch: Declaration = apply("stretch")
    def left: Declaration    = apply("left")
    def right: Declaration   = apply("right")
    def legacy: Declaration  = apply("legacy")
  end justifyItems

  /** `align-items` — `normal | stretch | <baseline-position> | <overflow-position>? <self-position>`. Initial:
    * `normal`. Inherited: no.
    */
  object alignItems extends DS("align-items") with Normal with BaselinePosition with OverflowPosition with SelfPosition:
    def stretch: Declaration = apply("stretch")

  /** `place-items` — `<'align-items'> <'justify-items'>?`. Initial: `see individual properties`. Inherited: no. */
  object placeItems extends DS("place-items") with Normal with BaselinePosition with OverflowPosition with SelfPosition:
    def stretch: Declaration = apply("stretch")
    def left: Declaration    = apply("left")
    def right: Declaration   = apply("right")
    def legacy: Declaration  = apply("legacy")

  /** `anchor-name` — `none | <anchor-name>#`. Initial: `none`. Inherited: no. */
  object anchorName extends DS("anchor-name") with None

  /** `anchor-scope` — `none | all | <anchor-name>#`. Initial: `none`. Inherited: no. */
  object anchorScope extends DS("anchor-scope") with None:
    def all: Declaration = apply("all")

  /** `position-anchor` — `normal | none | auto | <anchor-name> | match-parent`. Initial: `normal`. Inherited: no. */
  object positionAnchor extends DS("position-anchor") with Auto with None with Normal:
    def matchParent: Declaration = apply("match-parent")

  /** `position-area` — `none | <position-area>`. Initial: `none`. Inherited: no. */
  object positionArea extends DS("position-area") with None with PositionArea

  /** `top` — `auto | <length-percentage>`. Initial: `auto`. Inherited: no. */
  object top extends DS("top") with Length with Percent with Auto

  /** `left` — `auto | <length-percentage>`. Initial: `auto`. Inherited: no. */
  object left extends DS("left") with Length with Percent with Auto

  /** `right` — `auto | <length-percentage>`. Initial: `auto`. Inherited: no. */
  object right extends DS("right") with Length with Percent with Auto

  /** `bottom` — `auto | <length-percentage>`. Initial: `auto`. Inherited: no. */
  object bottom extends DS("bottom") with Length with Percent with Auto

  /** `width` —
    * `auto | <length-percentage [0,∞]> | min-content | max-content | fit-content(<length-percentage [0,∞]>) | <calc-size()>`.
    * Initial: `auto`. Inherited: no.
    */
  object width extends DS("width") with Length with Percent with Auto:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `height` —
    * `auto | <length-percentage [0,∞]> | min-content | max-content | fit-content(<length-percentage [0,∞]>) | <calc-size()>`.
    * Initial: `auto`. Inherited: no.
    */
  object height extends DS("height") with Length with Percent with Auto:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `min-width` —
    * `auto | <length-percentage [0,∞]> | min-content | max-content | fit-content(<length-percentage [0,∞]>) | <calc-size()>`.
    * Initial: `auto`. Inherited: no.
    */
  object minWidth extends DS("min-width") with Length with Percent with Auto:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `min-height` —
    * `auto | <length-percentage [0,∞]> | min-content | max-content | fit-content(<length-percentage [0,∞]>) | <calc-size()>`.
    * Initial: `auto`. Inherited: no.
    */
  object minHeight extends DS("min-height") with Length with Percent with Auto:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `max-width` —
    * `none | <length-percentage [0,∞]> | min-content | max-content | fit-content(<length-percentage [0,∞]>) | <calc-size()>`.
    * Initial: `none`. Inherited: no.
    */
  object maxWidth extends DS("max-width") with Length with Percent with None:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `max-height` —
    * `none | <length-percentage [0,∞]> | min-content | max-content | fit-content(<length-percentage [0,∞]>) | <calc-size()>`.
    * Initial: `none`. Inherited: no.
    */
  object maxHeight extends DS("max-height") with Length with Percent with None:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `margin-top` — `<length-percentage> | auto`. Initial: `0`. Inherited: no. */
  object marginTop extends DS("margin-top") with Length with Percent with Auto

  /** `margin-left` — `<length-percentage> | auto`. Initial: `0`. Inherited: no. */
  object marginLeft extends DS("margin-left") with Length with Percent with Auto

  /** `margin-right` — `<length-percentage> | auto`. Initial: `0`. Inherited: no. */
  object marginRight extends DS("margin-right") with Length with Percent with Auto

  /** `margin-bottom` — `<length-percentage> | auto`. Initial: `0`. Inherited: no. */
  object marginBottom extends DS("margin-bottom") with Length with Percent with Auto

  /** `position-try-fallbacks` — `none | [ [<dashed-ident> || <try-tactic>] | <position-area> ]#`. Initial: `none`.
    * Inherited: no.
    */
  object positionTryFallbacks extends DS("position-try-fallbacks") with None with PositionArea:
    def flipBlock: Declaration  = apply("flip-block")
    def flipInline: Declaration = apply("flip-inline")
    def flipX: Declaration      = apply("flip-x")
    def flipY: Declaration      = apply("flip-y")
    def flipStart: Declaration  = apply("flip-start")

  /** `position-try-order` — `normal | <try-size>`. Initial: `normal`. Inherited: no. */
  object positionTryOrder extends DS("position-try-order") with Normal with TrySize

  /** `position-try` — `<'position-try-order'>? <'position-try-fallbacks'>`. Initial: `see individual properties`.
    * Inherited: see individual properties.
    */
  object positionTry extends DS("position-try") with None with Normal with PositionArea with TrySize:
    def flipBlock: Declaration  = apply("flip-block")
    def flipInline: Declaration = apply("flip-inline")
    def flipX: Declaration      = apply("flip-x")
    def flipY: Declaration      = apply("flip-y")
    def flipStart: Declaration  = apply("flip-start")

  /** `position-visibility` — `always | [ anchor-valid || anchor-visible || no-overflow ]`. Initial: `anchor-visible`.
    * Inherited: no.
    */
  object positionVisibility extends DS("position-visibility"):
    def always: Declaration        = apply("always")
    def anchorValid: Declaration   = apply("anchor-valid")
    def anchorVisible: Declaration = apply("anchor-visible")
    def noOverflow: Declaration    = apply("no-overflow")

  /** `animation-duration` — `<time [0s,∞]>#`. Initial: `0s`. Inherited: no. */
  object animationDuration extends DS("animation-duration") with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `animation-composition` — `<single-animation-composition>#`. Initial: `replace`. Inherited: no. */
  object animationComposition extends DS("animation-composition") with SingleAnimationComposition

  /** `animation-timeline` — `<single-animation-timeline>#`. Initial: `auto`. Inherited: no. */
  object animationTimeline extends DS("animation-timeline") with Auto with None

  /** `animation-name` — `[ none | <keyframes-name> ]#`. Initial: `none`. Inherited: no. */
  object animationName extends DS("animation-name") with None

  /** `animation-timing-function` — `<easing-function>#`. Initial: `ease`. Inherited: no. */
  object animationTimingFunction extends DS("animation-timing-function") with TimingFunctionish

  /** `animation-iteration-count` — `<single-animation-iteration-count>#`. Initial: `1`. Inherited: no. */
  object animationIterationCount extends DS("animation-iteration-count") with Numeric with SingleAnimationIterationCount

  /** `animation-direction` — `<single-animation-direction>#`. Initial: `normal`. Inherited: no. */
  object animationDirection extends DS("animation-direction") with Normal with SingleAnimationDirection

  /** `animation-play-state` — `<single-animation-play-state>#`. Initial: `running`. Inherited: no. */
  object animationPlayState extends DS("animation-play-state") with SingleAnimationPlayState

  /** `animation-delay` — `<time>#`. Initial: `0s`. Inherited: no. */
  object animationDelay extends DS("animation-delay") with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `animation-fill-mode` — `<single-animation-fill-mode>#`. Initial: `none`. Inherited: no. */
  object animationFillMode extends DS("animation-fill-mode") with None with SingleAnimationFillMode

  /** `animation` — `<single-animation>#`. Initial: `see individual properties`. Inherited: no. */
  object animation extends DS("animation") with None with Timeish with TimingFunctionish

  /** `background-color` — `<color>`. Initial: `transparent`. Inherited: no. */
  object backgroundColor extends DS("background-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end backgroundColor

  /** `background-image` — `<bg-image>#`. Initial: `none`. Inherited: no. */
  object backgroundImage extends DS("background-image") with None with Imageish

  /** `background-repeat-x` — `<repetition>#`. Initial: `repeat`. Inherited: no. */
  object backgroundRepeatX extends DS("background-repeat-x") with Repetition

  /** `background-repeat-y` — `<repetition>#`. Initial: `repeat`. Inherited: no. */
  object backgroundRepeatY extends DS("background-repeat-y") with Repetition

  /** `background-repeat-block` — `<repetition>#`. Initial: `repeat`. Inherited: no. */
  object backgroundRepeatBlock extends DS("background-repeat-block") with Repetition

  /** `background-repeat-inline` — `<repetition>#`. Initial: `repeat`. Inherited: no. */
  object backgroundRepeatInline extends DS("background-repeat-inline") with Repetition

  /** `background-repeat` — `<repeat-style>#`. Initial: `repeat`. Inherited: no. */
  object backgroundRepeat extends DS("background-repeat") with RepeatStyle

  /** `background-attachment` — `<attachment>#`. Initial: `scroll`. Inherited: no. */
  object backgroundAttachment extends DS("background-attachment") with Attachment

  /** `background-position-x` — `[ center | [ [ left | right | x-start | x-end ]? <length-percentage>? ]! ]#`. Initial:
    * `0%`. Inherited: no.
    */
  object backgroundPositionX extends DS("background-position-x") with Length with Percent:
    def center: Declaration = apply("center")
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")
    def xStart: Declaration = apply("x-start")
    def xEnd: Declaration   = apply("x-end")

  /** `background-position-y` — `[ center | [ [ top | bottom | y-start | y-end ]? <length-percentage>? ]! ]#`. Initial:
    * `0%`. Inherited: no.
    */
  object backgroundPositionY extends DS("background-position-y") with Length with Percent:
    def center: Declaration = apply("center")
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")
    def yStart: Declaration = apply("y-start")
    def yEnd: Declaration   = apply("y-end")

  /** `background-position-inline` — `[ center | [ [ start | end ]? <length-percentage>? ]! ]#`. Initial: `0%`.
    * Inherited: no.
    */
  object backgroundPositionInline extends DS("background-position-inline") with Length with Percent:
    def center: Declaration = apply("center")
    def start: Declaration  = apply("start")
    def end: Declaration    = apply("end")

  /** `background-position-block` — `[ center | [ [ start | end ]? <length-percentage>? ]! ]#`. Initial: `0%`.
    * Inherited: no.
    */
  object backgroundPositionBlock extends DS("background-position-block") with Length with Percent:
    def center: Declaration = apply("center")
    def start: Declaration  = apply("start")
    def end: Declaration    = apply("end")

  /** `background-position` — `<bg-position>#`. Initial: `0% 0%`. Inherited: no. */
  object backgroundPosition extends DS("background-position") with Length with Percent with Positionish:
    def top: Declaration    = apply("top")
    def right: Declaration  = apply("right")
    def bottom: Declaration = apply("bottom")
    def left: Declaration   = apply("left")

  /** `background-clip` — `<visual-box>#`. Initial: `border-box`. Inherited: no. */
  object backgroundClip extends DS("background-clip") with VisualBox

  /** `background-origin` — `<visual-box>#`. Initial: `padding-box`. Inherited: no. */
  object backgroundOrigin extends DS("background-origin") with VisualBox

  /** `background-size` — `<bg-size>#`. Initial: `auto`. Inherited: no. */
  object backgroundSize extends DS("background-size") with Length with Percent with Auto with BgSize

  /** `background-tbd` — `<bg-layer>#`. Initial: `see individual properties`. Inherited: no. */
  object backgroundTbd extends DS("background-tbd") with Imageish with Positionish

  /** `background` — `<bg-layer>#? , <final-bg-layer>`. Initial: `see individual properties`. Inherited: no. */
  // could not parse: Expected root:1:1 / ("{" | [ \t\r\n] | postfix | "&&" | "||" | "|" | end-of-input):1:12, found "? , <final"
  object background extends DS("background")

  /** `border-top-color` — `<color> | <image-1D>`. Initial: `currentcolor`. Inherited: no. */
  object borderTopColor extends DS("border-top-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderTopColor

  /** `border-right-color` — `<color> | <image-1D>`. Initial: `currentcolor`. Inherited: no. */
  object borderRightColor extends DS("border-right-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderRightColor

  /** `border-bottom-color` — `<color> | <image-1D>`. Initial: `currentcolor`. Inherited: no. */
  object borderBottomColor extends DS("border-bottom-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderBottomColor

  /** `border-left-color` — `<color> | <image-1D>`. Initial: `currentcolor`. Inherited: no. */
  object borderLeftColor extends DS("border-left-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderLeftColor

  /** `border-block-start-color` — `<color> | <image-1D>`. Initial: `currentcolor`. Inherited: no. */
  object borderBlockStartColor extends DS("border-block-start-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderBlockStartColor

  /** `border-block-end-color` — `<color> | <image-1D>`. Initial: `currentcolor`. Inherited: no. */
  object borderBlockEndColor extends DS("border-block-end-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderBlockEndColor

  /** `border-inline-start-color` — `<color> | <image-1D>`. Initial: `currentcolor`. Inherited: no. */
  object borderInlineStartColor extends DS("border-inline-start-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderInlineStartColor

  /** `border-inline-end-color` — `<color> | <image-1D>`. Initial: `currentcolor`. Inherited: no. */
  object borderInlineEndColor extends DS("border-inline-end-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderInlineEndColor

  /** `border-color` — `[ <color> | <image-1D> ]{1,4}`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object borderColor extends DS("border-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderColor

  /** `border-block-color` — `<'border-top-color'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object borderBlockColor extends DS("border-block-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderBlockColor

  /** `border-inline-color` — `<'border-top-color'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object borderInlineColor extends DS("border-inline-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderInlineColor

  /** `border-top-style` — `<line-style>`. Initial: `none`. Inherited: no. */
  object borderTopStyle extends DS("border-top-style") with None with LineStyle

  /** `border-right-style` — `<line-style>`. Initial: `none`. Inherited: no. */
  object borderRightStyle extends DS("border-right-style") with None with LineStyle

  /** `border-bottom-style` — `<line-style>`. Initial: `none`. Inherited: no. */
  object borderBottomStyle extends DS("border-bottom-style") with None with LineStyle

  /** `border-left-style` — `<line-style>`. Initial: `none`. Inherited: no. */
  object borderLeftStyle extends DS("border-left-style") with None with LineStyle

  /** `border-block-start-style` — `<line-style>`. Initial: `none`. Inherited: no. */
  object borderBlockStartStyle extends DS("border-block-start-style") with None with LineStyle

  /** `border-block-end-style` — `<line-style>`. Initial: `none`. Inherited: no. */
  object borderBlockEndStyle extends DS("border-block-end-style") with None with LineStyle

  /** `border-inline-start-style` — `<line-style>`. Initial: `none`. Inherited: no. */
  object borderInlineStartStyle extends DS("border-inline-start-style") with None with LineStyle

  /** `border-inline-end-style` — `<line-style>`. Initial: `none`. Inherited: no. */
  object borderInlineEndStyle extends DS("border-inline-end-style") with None with LineStyle

  /** `border-block-style` — `<'border-top-style'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object borderBlockStyle extends DS("border-block-style") with None with LineStyle

  /** `border-inline-style` — `<'border-top-style'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object borderInlineStyle extends DS("border-inline-style") with None with LineStyle

  /** `border-style` — `<'border-top-style'>{1,4}`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object borderStyle extends DS("border-style") with None with LineStyle

  /** `border-top-width` — `<line-width>`. Initial: `medium`. Inherited: no. */
  object borderTopWidth extends DS("border-top-width") with Length with LineWidth

  /** `border-right-width` — `<line-width>`. Initial: `medium`. Inherited: no. */
  object borderRightWidth extends DS("border-right-width") with Length with LineWidth

  /** `border-bottom-width` — `<line-width>`. Initial: `medium`. Inherited: no. */
  object borderBottomWidth extends DS("border-bottom-width") with Length with LineWidth

  /** `border-left-width` — `<line-width>`. Initial: `medium`. Inherited: no. */
  object borderLeftWidth extends DS("border-left-width") with Length with LineWidth

  /** `border-block-start-width` — `<line-width>`. Initial: `medium`. Inherited: no. */
  object borderBlockStartWidth extends DS("border-block-start-width") with Length with LineWidth

  /** `border-block-end-width` — `<line-width>`. Initial: `medium`. Inherited: no. */
  object borderBlockEndWidth extends DS("border-block-end-width") with Length with LineWidth

  /** `border-inline-start-width` — `<line-width>`. Initial: `medium`. Inherited: no. */
  object borderInlineStartWidth extends DS("border-inline-start-width") with Length with LineWidth

  /** `border-inline-end-width` — `<line-width>`. Initial: `medium`. Inherited: no. */
  object borderInlineEndWidth extends DS("border-inline-end-width") with Length with LineWidth

  /** `border-block-width` — `<'border-top-width'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object borderBlockWidth extends DS("border-block-width") with Length with LengthBox with LineWidth

  /** `border-inline-width` — `<'border-top-width'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object borderInlineWidth extends DS("border-inline-width") with Length with LengthBox with LineWidth

  /** `border-width` — `<'border-top-width'>{1,4}`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object borderWidth extends DS("border-width") with Length with LengthBox with LineWidth

  /** `border-top` — `<line-width> || <line-style> || <color>`. Initial: `See individual properties`. Inherited: no. */
  object borderTop
      extends DS("border-top")
      with Length
      with None
      with ColorLike
      with Borderish
      with LineStyle
      with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderTop

  /** `border-right` — `<line-width> || <line-style> || <color>`. Initial: `See individual properties`. Inherited: no.
    */
  object borderRight
      extends DS("border-right")
      with Length
      with None
      with ColorLike
      with Borderish
      with LineStyle
      with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderRight

  /** `border-bottom` — `<line-width> || <line-style> || <color>`. Initial: `See individual properties`. Inherited: no.
    */
  object borderBottom
      extends DS("border-bottom")
      with Length
      with None
      with ColorLike
      with Borderish
      with LineStyle
      with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderBottom

  /** `border-left` — `<line-width> || <line-style> || <color>`. Initial: `See individual properties`. Inherited: no. */
  object borderLeft
      extends DS("border-left")
      with Length
      with None
      with ColorLike
      with Borderish
      with LineStyle
      with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderLeft

  /** `border-block-start` — `<line-width> || <line-style> || <color>`. Initial: `See individual properties`. Inherited:
    * no.
    */
  object borderBlockStart
      extends DS("border-block-start")
      with Length
      with None
      with ColorLike
      with Borderish
      with LineStyle
      with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderBlockStart

  /** `border-block-end` — `<line-width> || <line-style> || <color>`. Initial: `See individual properties`. Inherited:
    * no.
    */
  object borderBlockEnd
      extends DS("border-block-end")
      with Length
      with None
      with ColorLike
      with Borderish
      with LineStyle
      with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderBlockEnd

  /** `border-inline-start` — `<line-width> || <line-style> || <color>`. Initial: `See individual properties`.
    * Inherited: no.
    */
  object borderInlineStart
      extends DS("border-inline-start")
      with Length
      with None
      with ColorLike
      with Borderish
      with LineStyle
      with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderInlineStart

  /** `border-inline-end` — `<line-width> || <line-style> || <color>`. Initial: `See individual properties`. Inherited:
    * no.
    */
  object borderInlineEnd
      extends DS("border-inline-end")
      with Length
      with None
      with ColorLike
      with Borderish
      with LineStyle
      with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderInlineEnd

  /** `border-block` — `<'border-block-start'>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object borderBlock
      extends DS("border-block")
      with Length
      with None
      with ColorLike
      with Borderish
      with LineStyle
      with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderBlock

  /** `border-inline` — `<'border-block-start'>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object borderInline
      extends DS("border-inline")
      with Length
      with None
      with ColorLike
      with Borderish
      with LineStyle
      with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end borderInline

  /** `border` — `<line-width> || <line-style> || <color>`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object border extends DS("border") with Length with None with ColorLike with Borderish with LineStyle with LineWidth:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end border

  /** `border-top-left-radius` — `<border-radius>`. Initial: `0`. Inherited: no. */
  object borderTopLeftRadius extends DS("border-top-left-radius")

  /** `border-top-right-radius` — `<border-radius>`. Initial: `0`. Inherited: no. */
  object borderTopRightRadius extends DS("border-top-right-radius")

  /** `border-bottom-right-radius` — `<border-radius>`. Initial: `0`. Inherited: no. */
  object borderBottomRightRadius extends DS("border-bottom-right-radius")

  /** `border-bottom-left-radius` — `<border-radius>`. Initial: `0`. Inherited: no. */
  object borderBottomLeftRadius extends DS("border-bottom-left-radius")

  /** `border-start-start-radius` — `<border-radius>`. Initial: `0`. Inherited: no. */
  object borderStartStartRadius extends DS("border-start-start-radius")

  /** `border-start-end-radius` — `<border-radius>`. Initial: `0`. Inherited: no. */
  object borderStartEndRadius extends DS("border-start-end-radius")

  /** `border-end-start-radius` — `<border-radius>`. Initial: `0`. Inherited: no. */
  object borderEndStartRadius extends DS("border-end-start-radius")

  /** `border-end-end-radius` — `<border-radius>`. Initial: `0`. Inherited: no. */
  object borderEndEndRadius extends DS("border-end-end-radius")

  /** `border-top-radius` — `<length-percentage [0,∞]>{1,2} [ / <length-percentage [0,∞]>{1,2} ]?`. Initial: `0`.
    * Inherited: no.
    */
  object borderTopRadius extends DS("border-top-radius") with Length with Percent with LengthBox

  /** `border-right-radius` — `<length-percentage [0,∞]>{1,2} [ / <length-percentage [0,∞]>{1,2} ]?`. Initial: `0`.
    * Inherited: no.
    */
  object borderRightRadius extends DS("border-right-radius") with Length with Percent with LengthBox

  /** `border-bottom-radius` — `<length-percentage [0,∞]>{1,2} [ / <length-percentage [0,∞]>{1,2} ]?`. Initial: `0`.
    * Inherited: no.
    */
  object borderBottomRadius extends DS("border-bottom-radius") with Length with Percent with LengthBox

  /** `border-left-radius` — `<length-percentage [0,∞]>{1,2} [ / <length-percentage [0,∞]>{1,2} ]?`. Initial: `0`.
    * Inherited: no.
    */
  object borderLeftRadius extends DS("border-left-radius") with Length with Percent with LengthBox

  /** `border-block-start-radius` — `<length-percentage [0,∞]>{1,2} [ / <length-percentage [0,∞]>{1,2} ]?`. Initial:
    * `0`. Inherited: no.
    */
  object borderBlockStartRadius extends DS("border-block-start-radius") with Length with Percent with LengthBox

  /** `border-block-end-radius` — `<length-percentage [0,∞]>{1,2} [ / <length-percentage [0,∞]>{1,2} ]?`. Initial: `0`.
    * Inherited: no.
    */
  object borderBlockEndRadius extends DS("border-block-end-radius") with Length with Percent with LengthBox

  /** `border-inline-start-radius` — `<length-percentage [0,∞]>{1,2} [ / <length-percentage [0,∞]>{1,2} ]?`. Initial:
    * `0`. Inherited: no.
    */
  object borderInlineStartRadius extends DS("border-inline-start-radius") with Length with Percent with LengthBox

  /** `border-inline-end-radius` — `<length-percentage [0,∞]>{1,2} [ / <length-percentage [0,∞]>{1,2} ]?`. Initial: `0`.
    * Inherited: no.
    */
  object borderInlineEndRadius extends DS("border-inline-end-radius") with Length with Percent with LengthBox

  /** `border-radius` — `<length-percentage [0,∞]>{1,4} [ / <length-percentage [0,∞]>{1,4} ]?`. Initial:
    * `see individual properties`. Inherited: see individual properties.
    */
  object borderRadius extends DS("border-radius") with Length with Percent with LengthBox

  /** `corner-top-left-shape` — `<corner-shape-value>`. Initial: `round`. Inherited: no. */
  object cornerTopLeftShape extends DS("corner-top-left-shape") with CornerShapeValue

  /** `corner-top-right-shape` — `<corner-shape-value>`. Initial: `round`. Inherited: no. */
  object cornerTopRightShape extends DS("corner-top-right-shape") with CornerShapeValue

  /** `corner-bottom-right-shape` — `<corner-shape-value>`. Initial: `round`. Inherited: no. */
  object cornerBottomRightShape extends DS("corner-bottom-right-shape") with CornerShapeValue

  /** `corner-bottom-left-shape` — `<corner-shape-value>`. Initial: `round`. Inherited: no. */
  object cornerBottomLeftShape extends DS("corner-bottom-left-shape") with CornerShapeValue

  /** `corner-start-start-shape` — `<corner-shape-value>`. Initial: `round`. Inherited: no. */
  object cornerStartStartShape extends DS("corner-start-start-shape") with CornerShapeValue

  /** `corner-start-end-shape` — `<corner-shape-value>`. Initial: `round`. Inherited: no. */
  object cornerStartEndShape extends DS("corner-start-end-shape") with CornerShapeValue

  /** `corner-end-start-shape` — `<corner-shape-value>`. Initial: `round`. Inherited: no. */
  object cornerEndStartShape extends DS("corner-end-start-shape") with CornerShapeValue

  /** `corner-end-end-shape` — `<corner-shape-value>`. Initial: `round`. Inherited: no. */
  object cornerEndEndShape extends DS("corner-end-end-shape") with CornerShapeValue

  /** `corner-top-shape` — `<'corner-top-left-shape'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object cornerTopShape extends DS("corner-top-shape") with CornerShapeValue

  /** `corner-right-shape` — `<'corner-top-left-shape'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object cornerRightShape extends DS("corner-right-shape") with CornerShapeValue

  /** `corner-bottom-shape` — `<'corner-top-left-shape'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object cornerBottomShape extends DS("corner-bottom-shape") with CornerShapeValue

  /** `corner-left-shape` — `<'corner-top-left-shape'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object cornerLeftShape extends DS("corner-left-shape") with CornerShapeValue

  /** `corner-block-start-shape` — `<'corner-top-left-shape'>{1,2}`. Initial: `see individual properties`. Inherited:
    * see individual properties.
    */
  object cornerBlockStartShape extends DS("corner-block-start-shape") with CornerShapeValue

  /** `corner-block-end-shape` — `<'corner-top-left-shape'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object cornerBlockEndShape extends DS("corner-block-end-shape") with CornerShapeValue

  /** `corner-inline-start-shape` — `<'corner-top-left-shape'>{1,2}`. Initial: `see individual properties`. Inherited:
    * see individual properties.
    */
  object cornerInlineStartShape extends DS("corner-inline-start-shape") with CornerShapeValue

  /** `corner-inline-end-shape` — `<'corner-top-left-shape'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object cornerInlineEndShape extends DS("corner-inline-end-shape") with CornerShapeValue

  /** `corner-shape` — `<'corner-top-left-shape'>{1,4}`. Initial: `round`. Inherited: no. */
  object cornerShape extends DS("corner-shape") with CornerShapeValue

  /** `corner-top-left` — `<'border-top-left-radius'> || <'corner-top-left-shape'>`. Initial: `0`. Inherited: no. */
  object cornerTopLeft extends DS("corner-top-left") with CornerShapeValue

  /** `corner-top-right` — `<'border-top-left-radius'> || <'corner-top-left-shape'>`. Initial: `0`. Inherited: no. */
  object cornerTopRight extends DS("corner-top-right") with CornerShapeValue

  /** `corner-bottom-left` — `<'border-top-left-radius'> || <'corner-top-left-shape'>`. Initial: `0`. Inherited: no. */
  object cornerBottomLeft extends DS("corner-bottom-left") with CornerShapeValue

  /** `corner-bottom-right` — `<'border-top-left-radius'> || <'corner-top-left-shape'>`. Initial: `0`. Inherited: no. */
  object cornerBottomRight extends DS("corner-bottom-right") with CornerShapeValue

  /** `corner-start-start` — `<'border-top-left-radius'> || <'corner-top-left-shape'>`. Initial: `0`. Inherited: no. */
  object cornerStartStart extends DS("corner-start-start") with CornerShapeValue

  /** `corner-start-end` — `<'border-top-left-radius'> || <'corner-top-left-shape'>`. Initial: `0`. Inherited: no. */
  object cornerStartEnd extends DS("corner-start-end") with CornerShapeValue

  /** `corner-end-start` — `<'border-top-left-radius'> || <'corner-top-left-shape'>`. Initial: `0`. Inherited: no. */
  object cornerEndStart extends DS("corner-end-start") with CornerShapeValue

  /** `corner-end-end` — `<'border-top-left-radius'> || <'corner-top-left-shape'>`. Initial: `0`. Inherited: no. */
  object cornerEndEnd extends DS("corner-end-end") with CornerShapeValue

  /** `corner-top` — `<'border-top-radius'> || <'corner-top-shape'>`. Initial: `0`. Inherited: no. */
  object cornerTop extends DS("corner-top") with Length with Percent

  /** `corner-right` — `<'border-top-radius'> || <'corner-top-shape'>`. Initial: `0`. Inherited: no. */
  object cornerRight extends DS("corner-right") with Length with Percent

  /** `corner-bottom` — `<'border-top-radius'> || <'corner-top-shape'>`. Initial: `0`. Inherited: no. */
  object cornerBottom extends DS("corner-bottom") with Length with Percent

  /** `corner-left` — `<'border-top-radius'> || <'corner-top-shape'>`. Initial: `0`. Inherited: no. */
  object cornerLeft extends DS("corner-left") with Length with Percent

  /** `corner-block-start` — `<'border-top-radius'> || <'corner-top-shape'>`. Initial: `0`. Inherited: no. */
  object cornerBlockStart extends DS("corner-block-start") with Length with Percent

  /** `corner-block-end` — `<'border-top-radius'> || <'corner-top-shape'>`. Initial: `0`. Inherited: no. */
  object cornerBlockEnd extends DS("corner-block-end") with Length with Percent

  /** `corner-inline-start` — `<'border-top-radius'> || <'corner-top-shape'>`. Initial: `0`. Inherited: no. */
  object cornerInlineStart extends DS("corner-inline-start") with Length with Percent

  /** `corner-inline-end` — `<'border-top-radius'> || <'corner-top-shape'>`. Initial: `0`. Inherited: no. */
  object cornerInlineEnd extends DS("corner-inline-end") with Length with Percent

  /** `corner` — `<'border-radius'> || <'corner-shape'>`. Initial: `0`. Inherited: no. */
  object corner extends DS("corner") with Length with Percent:
    def round: Declaration    = apply("round")
    def squircle: Declaration = apply("squircle")
    def square: Declaration   = apply("square")
    def bevel: Declaration    = apply("bevel")
    def scoop: Declaration    = apply("scoop")
    def notch: Declaration    = apply("notch")

  /** `border-image-source` — `none | <image>`. Initial: `none`. Inherited: no. */
  object borderImageSource extends DS("border-image-source") with None with Imageish

  /** `border-image-slice` — `[<number [0,∞]> | <percentage [0,∞]>]{1,4} && fill?`. Initial: `100%`. Inherited: no. */
  object borderImageSlice extends DS("border-image-slice") with Percent with Numeric:
    def fill: Declaration = apply("fill")

  /** `border-image-width` — `[ <length-percentage [0,∞]> | <number [0,∞]> | auto ]{1,4}`. Initial: `1`. Inherited: no.
    */
  object borderImageWidth
      extends DS("border-image-width")
      with Length
      with Percent
      with Auto
      with Numeric
      with LengthBox

  /** `border-image-outset` — `[ <length [0,∞]> | <number [0,∞]> ]{1,4}`. Initial: `0`. Inherited: no. */
  object borderImageOutset extends DS("border-image-outset") with Length with Numeric with LengthBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end borderImageOutset

  /** `border-image-repeat` — `[ stretch | repeat | round | space ]{1,2}`. Initial: `stretch`. Inherited: no. */
  object borderImageRepeat extends DS("border-image-repeat"):
    def stretch: Declaration = apply("stretch")
    def repeat: Declaration  = apply("repeat")
    def round: Declaration   = apply("round")
    def space: Declaration   = apply("space")

  /** `border-image` —
    * `<'border-image-source'> || <'border-image-slice'> [ / <'border-image-width'> | / <'border-image-width'>? / <'border-image-outset'> ]? || <'border-image-repeat'>`.
    * Initial: `See individual properties`. Inherited: no.
    */
  object borderImage extends DS("border-image") with Length with Percent with Auto with None with Numeric with Imageish:
    def fill: Declaration    = apply("fill")
    def rex: Declaration     = apply("rex")
    def cap: Declaration     = apply("cap")
    def rcap: Declaration    = apply("rcap")
    def rch: Declaration     = apply("rch")
    def ic: Declaration      = apply("ic")
    def ric: Declaration     = apply("ric")
    def lh: Declaration      = apply("lh")
    def rlh: Declaration     = apply("rlh")
    def svw: Declaration     = apply("svw")
    def lvw: Declaration     = apply("lvw")
    def dvw: Declaration     = apply("dvw")
    def svh: Declaration     = apply("svh")
    def lvh: Declaration     = apply("lvh")
    def dvh: Declaration     = apply("dvh")
    def vi: Declaration      = apply("vi")
    def svi: Declaration     = apply("svi")
    def lvi: Declaration     = apply("lvi")
    def dvi: Declaration     = apply("dvi")
    def vb: Declaration      = apply("vb")
    def svb: Declaration     = apply("svb")
    def lvb: Declaration     = apply("lvb")
    def dvb: Declaration     = apply("dvb")
    def svmin: Declaration   = apply("svmin")
    def lvmin: Declaration   = apply("lvmin")
    def dvmin: Declaration   = apply("dvmin")
    def svmax: Declaration   = apply("svmax")
    def lvmax: Declaration   = apply("lvmax")
    def dvmax: Declaration   = apply("dvmax")
    def Q: Declaration       = apply("Q")
    def stretch: Declaration = apply("stretch")
    def repeat: Declaration  = apply("repeat")
    def round: Declaration   = apply("round")
    def space: Declaration   = apply("space")
  end borderImage

  /** `border-limit` —
    * `all | [ sides | corners ] <length-percentage [0,∞]>? | [ top | right | bottom | left ] <length-percentage [0,∞]>`.
    * Initial: `all`. Inherited: no.
    */
  object borderLimit extends DS("border-limit") with Length with Percent:
    def all: Declaration     = apply("all")
    def sides: Declaration   = apply("sides")
    def corners: Declaration = apply("corners")
    def top: Declaration     = apply("top")
    def right: Declaration   = apply("right")
    def bottom: Declaration  = apply("bottom")
    def left: Declaration    = apply("left")

  /** `border-top-clip` — `none | [ <length-percentage [0,∞]> | <flex> ]+`. Initial: `none`. Inherited: no. */
  object borderTopClip extends DS("border-top-clip") with Length with Percent with None with Numeric:
    def fr: Declaration = apply("fr")

  /** `border-right-clip` — `none | [ <length-percentage [0,∞]> | <flex> ]+`. Initial: `none`. Inherited: no. */
  object borderRightClip extends DS("border-right-clip") with Length with Percent with None with Numeric:
    def fr: Declaration = apply("fr")

  /** `border-bottom-clip` — `none | [ <length-percentage [0,∞]> | <flex> ]+`. Initial: `none`. Inherited: no. */
  object borderBottomClip extends DS("border-bottom-clip") with Length with Percent with None with Numeric:
    def fr: Declaration = apply("fr")

  /** `border-left-clip` — `none | [ <length-percentage [0,∞]> | <flex> ]+`. Initial: `none`. Inherited: no. */
  object borderLeftClip extends DS("border-left-clip") with Length with Percent with None with Numeric:
    def fr: Declaration = apply("fr")

  /** `border-block-start-clip` — `none | [ <length-percentage [0,∞]> | <flex> ]+`. Initial: `none`. Inherited: no. */
  object borderBlockStartClip extends DS("border-block-start-clip") with Length with Percent with None with Numeric:
    def fr: Declaration = apply("fr")

  /** `border-block-end-clip` — `none | [ <length-percentage [0,∞]> | <flex> ]+`. Initial: `none`. Inherited: no. */
  object borderBlockEndClip extends DS("border-block-end-clip") with Length with Percent with None with Numeric:
    def fr: Declaration = apply("fr")

  /** `border-inline-start-clip` — `none | [ <length-percentage [0,∞]> | <flex> ]+`. Initial: `none`. Inherited: no. */
  object borderInlineStartClip extends DS("border-inline-start-clip") with Length with Percent with None with Numeric:
    def fr: Declaration = apply("fr")

  /** `border-inline-end-clip` — `none | [ <length-percentage [0,∞]> | <flex> ]+`. Initial: `none`. Inherited: no. */
  object borderInlineEndClip extends DS("border-inline-end-clip") with Length with Percent with None with Numeric:
    def fr: Declaration = apply("fr")

  /** `border-block-clip` — `<'border-top-clip'>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object borderBlockClip extends DS("border-block-clip") with Length with Percent with None with Numeric:
    def fr: Declaration = apply("fr")

  /** `border-inline-clip` — `<'border-top-clip'>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object borderInlineClip extends DS("border-inline-clip") with Length with Percent with None with Numeric:
    def fr: Declaration = apply("fr")

  /** `border-clip` — `<'border-top-clip'>`. Initial: `see individual properties`. Inherited: see individual properties.
    */
  object borderClip extends DS("border-clip") with Length with Percent with None with Numeric:
    def fr: Declaration = apply("fr")

  /** `box-shadow-color` — `<color>#`. Initial: `currentcolor`. Inherited: no. */
  object boxShadowColor extends DS("box-shadow-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end boxShadowColor

  /** `box-shadow-offset` — `[ none | <length>{1,2} ]#`. Initial: `none`. Inherited: no. */
  object boxShadowOffset extends DS("box-shadow-offset") with Length with None:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end boxShadowOffset

  /** `box-shadow-blur` — `<length [0,∞]>#`. Initial: `0`. Inherited: no. */
  object boxShadowBlur extends DS("box-shadow-blur") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end boxShadowBlur

  /** `box-shadow-spread` — `<length>#`. Initial: `0`. Inherited: no. */
  object boxShadowSpread extends DS("box-shadow-spread") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end boxShadowSpread

  /** `box-shadow-position` — `[ outset | inset ]#`. Initial: `outset`. Inherited: no. */
  object boxShadowPosition extends DS("box-shadow-position"):
    def outset: Declaration = apply("outset")
    def inset: Declaration  = apply("inset")

  /** `box-shadow` — `<spread-shadow>#`. Initial: `none`. Inherited: no. */
  object boxShadow extends DS("box-shadow") with Length with None with Shadowish

  /** `border-shape` — `none | [ <basic-shape> <geometry-box>?]{1,2}`. Initial: `none`. Inherited: no. */
  object borderShape extends DS("border-shape") with None with BasicShapeish with GeometryBox:
    override def borderBox: Declaration  = apply("border-box")
    override def contentBox: Declaration = apply("content-box")
    override def paddingBox: Declaration = apply("padding-box")

  /** `margin-trim` — `none | [ block || inline ] | [ block-start || inline-start || block-end || inline-end ]`.
    * Initial: `none`. Inherited: no.
    */
  object marginTrim extends DS("margin-trim") with None:
    def block: Declaration       = apply("block")
    def inline: Declaration      = apply("inline")
    def blockStart: Declaration  = apply("block-start")
    def inlineStart: Declaration = apply("inline-start")
    def blockEnd: Declaration    = apply("block-end")
    def inlineEnd: Declaration   = apply("inline-end")

  /** `padding-top` — `<length-percentage [0,∞]>`. Initial: `0`. Inherited: no. */
  object paddingTop extends DS("padding-top") with Length with Percent

  /** `padding-right` — `<length-percentage [0,∞]>`. Initial: `0`. Inherited: no. */
  object paddingRight extends DS("padding-right") with Length with Percent

  /** `padding-bottom` — `<length-percentage [0,∞]>`. Initial: `0`. Inherited: no. */
  object paddingBottom extends DS("padding-bottom") with Length with Percent

  /** `padding-left` — `<length-percentage [0,∞]>`. Initial: `0`. Inherited: no. */
  object paddingLeft extends DS("padding-left") with Length with Percent

  /** `break-before` —
    * `auto | avoid | always | all | avoid-page | page | left | right | recto | verso | avoid-column | column | avoid-region | region`.
    * Initial: `auto`. Inherited: no.
    */
  object breakBefore extends DS("break-before") with Auto:
    def avoid: Declaration       = apply("avoid")
    def always: Declaration      = apply("always")
    def all: Declaration         = apply("all")
    def avoidPage: Declaration   = apply("avoid-page")
    def page: Declaration        = apply("page")
    def left: Declaration        = apply("left")
    def right: Declaration       = apply("right")
    def recto: Declaration       = apply("recto")
    def verso: Declaration       = apply("verso")
    def avoidColumn: Declaration = apply("avoid-column")
    def column: Declaration      = apply("column")
    def avoidRegion: Declaration = apply("avoid-region")
    def region: Declaration      = apply("region")
  end breakBefore

  /** `break-after` —
    * `auto | avoid | always | all | avoid-page | page | left | right | recto | verso | avoid-column | column | avoid-region | region`.
    * Initial: `auto`. Inherited: no.
    */
  object breakAfter extends DS("break-after") with Auto:
    def avoid: Declaration       = apply("avoid")
    def always: Declaration      = apply("always")
    def all: Declaration         = apply("all")
    def avoidPage: Declaration   = apply("avoid-page")
    def page: Declaration        = apply("page")
    def left: Declaration        = apply("left")
    def right: Declaration       = apply("right")
    def recto: Declaration       = apply("recto")
    def verso: Declaration       = apply("verso")
    def avoidColumn: Declaration = apply("avoid-column")
    def column: Declaration      = apply("column")
    def avoidRegion: Declaration = apply("avoid-region")
    def region: Declaration      = apply("region")
  end breakAfter

  /** `break-inside` — `auto | avoid | avoid-page | avoid-column | avoid-region`. Initial: `auto`. Inherited: no. */
  object breakInside extends DS("break-inside") with Auto:
    def avoid: Declaration       = apply("avoid")
    def avoidPage: Declaration   = apply("avoid-page")
    def avoidColumn: Declaration = apply("avoid-column")
    def avoidRegion: Declaration = apply("avoid-region")

  /** `orphans` — `<integer [1,∞]>`. Initial: `2`. Inherited: yes. */
  object orphans extends DS("orphans") with Numeric

  /** `widows` — `<integer [1,∞]>`. Initial: `2`. Inherited: yes. */
  object widows extends DS("widows") with Numeric

  /** `margin-break` — `auto | keep | discard`. Initial: `auto`. Inherited: no. */
  object marginBreak extends DS("margin-break") with Auto:
    def keep: Declaration    = apply("keep")
    def discard: Declaration = apply("discard")

  /** `box-decoration-break` — `slice | clone`. Initial: `slice`. Inherited: no. */
  object boxDecorationBreak extends DS("box-decoration-break"):
    def slice: Declaration    = apply("slice")
    def cloneCss: Declaration = apply("clone")

  /** `all` — `initial | inherit | unset | revert | revert-layer | revert-rule`. Initial: `see individual properties`.
    * Inherited: see individual properties.
    */
  object all extends DS("all"):
    def revertRule: Declaration = apply("revert-rule")

  /** `color-scheme` — `normal | [ light | dark | <custom-ident> ]+ && only?`. Initial: `normal`. Inherited: yes. */
  object colorScheme extends DS("color-scheme") with Normal:
    def light: Declaration = apply("light")
    def dark: Declaration  = apply("dark")
    def only: Declaration  = apply("only")

  /** `forced-color-adjust` — `auto | none | preserve-parent-color`. Initial: `auto`. Inherited: yes. */
  object forcedColorAdjust extends DS("forced-color-adjust") with Auto with None:
    def preserveParentColor: Declaration = apply("preserve-parent-color")

  /** `print-color-adjust` — `economy | exact`. Initial: `economy`. Inherited: yes. */
  object printColorAdjust extends DS("print-color-adjust"):
    def economy: Declaration = apply("economy")
    def exact: Declaration   = apply("exact")

  /** `color-adjust` — `<'print-color-adjust'>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object colorAdjust extends DS("color-adjust"):
    def economy: Declaration = apply("economy")
    def exact: Declaration   = apply("exact")

  /** `dynamic-range-limit` — `standard | no-limit | constrained | <dynamic-range-limit-mix()>`. Initial: `no-limit`.
    * Inherited: yes.
    */
  object dynamicRangeLimit extends DS("dynamic-range-limit"):
    def standard: Declaration    = apply("standard")
    def noLimit: Declaration     = apply("no-limit")
    def constrained: Declaration = apply("constrained")

  /** `color` — `<color>`. Initial: `CanvasText`. Inherited: yes. */
  object color extends DS("color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end color

  /** `opacity` — `<opacity-value>`. Initial: `1`. Inherited: no. */
  object opacity extends DS("opacity") with Numeric

  /** `container-type` — `normal | [ [ size | inline-size ] || scroll-state ]`. Initial: `normal`. Inherited: no. */
  object containerType extends DS("container-type") with Normal:
    def size: Declaration        = apply("size")
    def inlineSize: Declaration  = apply("inline-size")
    def scrollState: Declaration = apply("scroll-state")

  /** `container-name` — `none | <custom-ident>+`. Initial: `none`. Inherited: no. */
  object containerName extends DS("container-name") with None

  /** `container` — `<'container-name'> [ / <'container-type'> ]?`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object container extends DS("container") with None with Normal:
    def size: Declaration        = apply("size")
    def inlineSize: Declaration  = apply("inline-size")
    def scrollState: Declaration = apply("scroll-state")

  /** `contain` — `none | strict | content | [ [size | inline-size] || layout || style || paint ]`. Initial: `none`.
    * Inherited: no.
    */
  object contain extends DS("contain") with None:
    def strict: Declaration     = apply("strict")
    def content: Declaration    = apply("content")
    def size: Declaration       = apply("size")
    def inlineSize: Declaration = apply("inline-size")
    def layout: Declaration     = apply("layout")
    def style: Declaration      = apply("style")
    def paint: Declaration      = apply("paint")

  /** `content-visibility` — `visible | auto | hidden`. Initial: `visible`. Inherited: no. */
  object contentVisibility extends DS("content-visibility") with Auto:
    def visible: Declaration = apply("visible")
    def hidden: Declaration  = apply("hidden")

  /** `content` —
    * `normal | none | [ <content-replacement> | <content-list> ] [/ [ <string> | <counter> | <attr()> ]+ ]?`. Initial:
    * `normal`. Inherited: no.
    */
  object content extends DS("content") with None with Normal

  /** `quotes` — `auto | none | match-parent | [ <string> <string> ]+`. Initial: `auto`. Inherited: yes. */
  object quotes extends DS("quotes") with Auto with None:
    def matchParent: Declaration = apply("match-parent")

  /** `string-set` — `none | [ <custom-ident> <string>+ ]#`. Initial: `none`. Inherited: no. */
  object stringSet extends DS("string-set") with None

  /** `bookmark-level` — `none | <integer [1,∞]>`. Initial: `none`. Inherited: no. */
  object bookmarkLevel extends DS("bookmark-level") with None with Numeric

  /** `bookmark-label` — `<content-list>`. Initial: `content(text)`. Inherited: no. */
  object bookmarkLabel extends DS("bookmark-label")

  /** `bookmark-state` — `open | closed`. Initial: `open`. Inherited: no. */
  object bookmarkState extends DS("bookmark-state"):
    def open: Declaration   = apply("open")
    def closed: Declaration = apply("closed")

  /** `display` —
    * `[ <display-outside> || <display-inside> ] | <display-listitem> | <display-internal> | <display-box> | <display-legacy>`.
    * Initial: `inline`. Inherited: no.
    */
  object display
      extends DS("display")
      with None
      with DisplayBox
      with DisplayInside
      with DisplayInternal
      with DisplayLegacy
      with DisplayListitem
      with DisplayOutside:
    override def flow: Declaration     = apply("flow")
    override def flowRoot: Declaration = apply("flow-root")
  end display

  /** `order` — `<integer>`. Initial: `0`. Inherited: no. */
  object order extends DS("order") with Numeric

  /** `reading-flow` — `normal | source-order | flex-visual | flex-flow | grid-rows | grid-columns | grid-order`.
    * Initial: `normal`. Inherited: no.
    */
  object readingFlow extends DS("reading-flow") with Normal:
    def sourceOrder: Declaration = apply("source-order")
    def flexVisual: Declaration  = apply("flex-visual")
    def flexFlow: Declaration    = apply("flex-flow")
    def gridRows: Declaration    = apply("grid-rows")
    def gridColumns: Declaration = apply("grid-columns")
    def gridOrder: Declaration   = apply("grid-order")

  /** `reading-order` — `<integer>`. Initial: `0`. Inherited: no. */
  object readingOrder extends DS("reading-order") with Numeric

  /** `visibility` — `visible | hidden | force-hidden | collapse`. Initial: `visible`. Inherited: yes. */
  object visibility extends DS("visibility"):
    def visible: Declaration     = apply("visible")
    def hidden: Declaration      = apply("hidden")
    def forceHidden: Declaration = apply("force-hidden")
    def collapse: Declaration    = apply("collapse")

  /** `wrap-flow` — `auto | both | start | end | minimum | maximum | clear`. Initial: `auto`. Inherited: no. */
  object wrapFlow extends DS("wrap-flow") with Auto:
    def both: Declaration    = apply("both")
    def start: Declaration   = apply("start")
    def end: Declaration     = apply("end")
    def minimum: Declaration = apply("minimum")
    def maximum: Declaration = apply("maximum")
    def clear: Declaration   = apply("clear")

  /** `wrap-through` — `wrap | none`. Initial: `wrap`. Inherited: no. */
  object wrapThrough extends DS("wrap-through") with None:
    def wrap: Declaration = apply("wrap")

  /** `flex-direction` — `row | row-reverse | column | column-reverse`. Initial: `row`. Inherited: no. */
  object flexDirection extends DS("flex-direction"):
    def row: Declaration           = apply("row")
    def rowReverse: Declaration    = apply("row-reverse")
    def column: Declaration        = apply("column")
    def columnReverse: Declaration = apply("column-reverse")

  /** `flex-wrap` — `nowrap | [ wrap | wrap-reverse ] || balance`. Initial: `nowrap`. Inherited: no. */
  object flexWrap extends DS("flex-wrap"):
    def nowrap: Declaration      = apply("nowrap")
    def wrap: Declaration        = apply("wrap")
    def wrapReverse: Declaration = apply("wrap-reverse")
    def balance: Declaration     = apply("balance")

  /** `flex-flow` — `<'flex-direction'> || <'flex-wrap'>`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object flexFlow extends DS("flex-flow"):
    def row: Declaration           = apply("row")
    def rowReverse: Declaration    = apply("row-reverse")
    def column: Declaration        = apply("column")
    def columnReverse: Declaration = apply("column-reverse")
    def nowrap: Declaration        = apply("nowrap")
    def wrap: Declaration          = apply("wrap")
    def wrapReverse: Declaration   = apply("wrap-reverse")
    def balance: Declaration       = apply("balance")

  /** `flex-line-count` — `<integer [1,∞]>`. Initial: `1`. Inherited: no. */
  object flexLineCount extends DS("flex-line-count") with Numeric

  /** `flex-grow` — `<number [0,∞]>`. Initial: `0`. Inherited: no. */
  object flexGrow extends DS("flex-grow") with Numeric

  /** `flex-shrink` — `<number [0,∞]>`. Initial: `1`. Inherited: no. */
  object flexShrink extends DS("flex-shrink") with Numeric

  /** `flex-basis` — `content | <'width'>`. Initial: `auto`. Inherited: no. */
  object flexBasis extends DS("flex-basis") with Length with Percent with Auto:
    def content: Declaration    = apply("content")
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `font-size-adjust` — `none | <number [0,∞]>`. Initial: `none`. Inherited: yes. */
  object fontSizeAdjust extends DS("font-size-adjust") with None with Numeric

  /** `font-family` — `[ <font-family-name> | <generic-font-family> ]#`. Initial: `depends on user agent`. Inherited:
    * yes.
    */
  object fontFamily extends DS("font-family") with FontFamilyish:
    def math: Declaration        = apply("math")
    def serif: Declaration       = apply("serif")
    def sansSerif: Declaration   = apply("sans-serif")
    def cursive: Declaration     = apply("cursive")
    def fantasy: Declaration     = apply("fantasy")
    def monospace: Declaration   = apply("monospace")
    def systemUi: Declaration    = apply("system-ui")
    def uiSerif: Declaration     = apply("ui-serif")
    def uiSansSerif: Declaration = apply("ui-sans-serif")
    def uiMonospace: Declaration = apply("ui-monospace")
    def uiRounded: Declaration   = apply("ui-rounded")
  end fontFamily

  /** `font-width` —
    * `normal | <percentage [0,∞]> | ultra-condensed | extra-condensed | condensed | semi-condensed | semi-expanded | expanded | extra-expanded | ultra-expanded`.
    * Initial: `normal`. Inherited: yes.
    */
  object fontWidth extends DS("font-width") with Percent with Normal:
    def ultraCondensed: Declaration = apply("ultra-condensed")
    def extraCondensed: Declaration = apply("extra-condensed")
    def condensed: Declaration      = apply("condensed")
    def semiCondensed: Declaration  = apply("semi-condensed")
    def semiExpanded: Declaration   = apply("semi-expanded")
    def expanded: Declaration       = apply("expanded")
    def extraExpanded: Declaration  = apply("extra-expanded")
    def ultraExpanded: Declaration  = apply("ultra-expanded")

  /** `font-style` — `normal | italic | left | right | oblique <angle [-90deg,90deg]>?`. Initial: `normal`. Inherited:
    * yes.
    */
  object fontStyle extends DS("font-style") with Normal with Angleish:
    def italic: Declaration  = apply("italic")
    def left: Declaration    = apply("left")
    def right: Declaration   = apply("right")
    def oblique: Declaration = apply("oblique")
    def deg: Declaration     = apply("deg")
    def grad: Declaration    = apply("grad")
    def rad: Declaration     = apply("rad")
    def turn: Declaration    = apply("turn")

  /** `font-size` — `<absolute-size> | <relative-size> | <length-percentage [0,∞]> | math`. Initial: `medium`.
    * Inherited: yes.
    */
  object fontSize extends DS("font-size") with Length with Percent with AbsoluteSize with RelativeSize:
    def math: Declaration = apply("math")

  /** `font` —
    * `[ [ <'font-style'> || <font-variant-css2> || <'font-weight'> || <font-width-css3> ]? <'font-size'> [ / <'line-height'> ]? <'font-family'># ] | <system-font-family-name>`.
    * Initial: `see individual properties`. Inherited: yes.
    */
  object font
      extends DS("font")
      with Length
      with Percent
      with Normal
      with Numeric
      with AbsoluteSize
      with Angleish
      with FontFamilyish
      with FontVariantCss2
      with FontWeightAbsolute
      with FontWidthCss3
      with RelativeSize
      with SystemFontFamilyName:
    def italic: Declaration      = apply("italic")
    def left: Declaration        = apply("left")
    def right: Declaration       = apply("right")
    def oblique: Declaration     = apply("oblique")
    def deg: Declaration         = apply("deg")
    def grad: Declaration        = apply("grad")
    def rad: Declaration         = apply("rad")
    def turn: Declaration        = apply("turn")
    def bolder: Declaration      = apply("bolder")
    def lighter: Declaration     = apply("lighter")
    def math: Declaration        = apply("math")
    def serif: Declaration       = apply("serif")
    def sansSerif: Declaration   = apply("sans-serif")
    def cursive: Declaration     = apply("cursive")
    def fantasy: Declaration     = apply("fantasy")
    def monospace: Declaration   = apply("monospace")
    def systemUi: Declaration    = apply("system-ui")
    def uiSerif: Declaration     = apply("ui-serif")
    def uiSansSerif: Declaration = apply("ui-sans-serif")
    def uiMonospace: Declaration = apply("ui-monospace")
    def uiRounded: Declaration   = apply("ui-rounded")
  end font

  /** `font-synthesis-weight` — `auto | none`. Initial: `auto`. Inherited: yes. */
  object fontSynthesisWeight extends DS("font-synthesis-weight") with Auto with None

  /** `font-synthesis-style` — `auto | none | oblique-only`. Initial: `auto`. Inherited: yes. */
  object fontSynthesisStyle extends DS("font-synthesis-style") with Auto with None:
    def obliqueOnly: Declaration = apply("oblique-only")

  /** `font-synthesis-small-caps` — `auto | none`. Initial: `auto`. Inherited: yes. */
  object fontSynthesisSmallCaps extends DS("font-synthesis-small-caps") with Auto with None

  /** `font-synthesis-position` — `auto | none`. Initial: `auto`. Inherited: yes. */
  object fontSynthesisPosition extends DS("font-synthesis-position") with Auto with None

  /** `font-synthesis` — `none | [ weight || style || small-caps || position]`. Initial:
    * `weight style small-caps position`. Inherited: yes.
    */
  object fontSynthesis extends DS("font-synthesis") with None:
    def weight: Declaration    = apply("weight")
    def style: Declaration     = apply("style")
    def smallCaps: Declaration = apply("small-caps")
    def position: Declaration  = apply("position")

  /** `font-kerning` — `auto | normal | none`. Initial: `auto`. Inherited: yes. */
  object fontKerning extends DS("font-kerning") with Auto with None with Normal

  /** `font-variant-ligatures` —
    * `normal | none | [ <common-lig-values> || <discretionary-lig-values> || <historical-lig-values> || <contextual-alt-values> ]`.
    * Initial: `normal`. Inherited: yes.
    */
  object fontVariantLigatures
      extends DS("font-variant-ligatures")
      with None
      with Normal
      with CommonLigValues
      with ContextualAltValues
      with DiscretionaryLigValues
      with HistoricalLigValues

  /** `font-variant-position` — `normal | sub | super`. Initial: `normal`. Inherited: yes. */
  object fontVariantPosition extends DS("font-variant-position") with Normal:
    def sub: Declaration     = apply("sub")
    def `super`: Declaration = apply("super")

  /** `font-variant-caps` —
    * `normal | small-caps | all-small-caps | petite-caps | all-petite-caps | unicase | titling-caps`. Initial:
    * `normal`. Inherited: yes.
    */
  object fontVariantCaps extends DS("font-variant-caps") with Normal:
    def smallCaps: Declaration     = apply("small-caps")
    def allSmallCaps: Declaration  = apply("all-small-caps")
    def petiteCaps: Declaration    = apply("petite-caps")
    def allPetiteCaps: Declaration = apply("all-petite-caps")
    def unicase: Declaration       = apply("unicase")
    def titlingCaps: Declaration   = apply("titling-caps")

  /** `font-variant-numeric` —
    * `normal | [ <numeric-figure-values> || <numeric-spacing-values> || <numeric-fraction-values> || ordinal || slashed-zero ]`.
    * Initial: `normal`. Inherited: yes.
    */
  object fontVariantNumeric
      extends DS("font-variant-numeric")
      with Normal
      with NumericFigureValues
      with NumericFractionValues
      with NumericSpacingValues:
    def ordinal: Declaration     = apply("ordinal")
    def slashedZero: Declaration = apply("slashed-zero")

  /** `font-variant-alternates` — `normal | [ stylistic(<font-feature-value-name>) || historical-forms ||
    * styleset(<font-feature-value-name>#) || character-variant(<font-feature-value-name>#) ||
    * swash(<font-feature-value-name>) || ornaments(<font
    */
  object fontVariantAlternates extends DS("font-variant-alternates") with Normal:
    def historicalForms: Declaration = apply("historical-forms")

  /** `font-variant-east-asian` — `normal | [ <east-asian-variant-values> || <east-asian-width-values> || ruby ]`.
    * Initial: `normal`. Inherited: yes.
    */
  object fontVariantEastAsian
      extends DS("font-variant-east-asian")
      with Normal
      with EastAsianVariantValues
      with EastAsianWidthValues:
    def ruby: Declaration = apply("ruby")

  /** `font-variant` — `normal | none | [ [ <common-lig-values> || <discretionary-lig-values> || <historical-lig-values> ||
    * <contextual-alt-values> ] || [ small-caps | all-small-caps | petite-caps | all-petite-caps | unicase |
    * titling-caps ] || [
    */
  object fontVariant
      extends DS("font-variant")
      with None
      with Normal
      with CommonLigValues
      with ContextualAltValues
      with DiscretionaryLigValues
      with EastAsianVariantValues
      with EastAsianWidthValues
      with HistoricalLigValues
      with NumericFigureValues
      with NumericFractionValues
      with NumericSpacingValues:
    def smallCaps: Declaration       = apply("small-caps")
    def allSmallCaps: Declaration    = apply("all-small-caps")
    def petiteCaps: Declaration      = apply("petite-caps")
    def allPetiteCaps: Declaration   = apply("all-petite-caps")
    def unicase: Declaration         = apply("unicase")
    def titlingCaps: Declaration     = apply("titling-caps")
    def historicalForms: Declaration = apply("historical-forms")
    def ordinal: Declaration         = apply("ordinal")
    def slashedZero: Declaration     = apply("slashed-zero")
    def ruby: Declaration            = apply("ruby")
    def sub: Declaration             = apply("sub")
    def `super`: Declaration         = apply("super")
    def text: Declaration            = apply("text")
    def emoji: Declaration           = apply("emoji")
    def unicode: Declaration         = apply("unicode")
  end fontVariant

  /** `font-feature-settings` — `normal | <feature-tag-value>#`. Initial: `normal`. Inherited: yes. */
  object fontFeatureSettings extends DS("font-feature-settings") with Normal

  /** `font-language-override` — `normal | <string>`. Initial: `normal`. Inherited: yes. */
  object fontLanguageOverride extends DS("font-language-override") with Normal

  /** `font-optical-sizing` — `auto | none`. Initial: `auto`. Inherited: yes. */
  object fontOpticalSizing extends DS("font-optical-sizing") with Auto with None

  /** `font-variation-settings` — `normal | [ <opentype-tag> <number> ]#`. Initial: `normal`. Inherited: yes. */
  object fontVariationSettings extends DS("font-variation-settings") with Normal with Numeric

  /** `font-palette` — `normal | light | dark | <palette-identifier> | <palette-mix()>`. Initial: `normal`. Inherited:
    * yes.
    */
  object fontPalette extends DS("font-palette") with Normal:
    def light: Declaration = apply("light")
    def dark: Declaration  = apply("dark")

  /** `font-variant-emoji` — `normal | text | emoji | unicode`. Initial: `normal`. Inherited: yes. */
  object fontVariantEmoji extends DS("font-variant-emoji") with Normal:
    def text: Declaration    = apply("text")
    def emoji: Declaration   = apply("emoji")
    def unicode: Declaration = apply("unicode")

  /** `font-stretch` —
    * `normal | <percentage [0,∞]> | ultra-condensed | extra-condensed | condensed | semi-condensed | semi-expanded | expanded | extra-expanded | ultra-expanded`.
    */
  object fontStretch extends DS("font-stretch") with Percent with Normal:
    def ultraCondensed: Declaration = apply("ultra-condensed")
    def extraCondensed: Declaration = apply("extra-condensed")
    def condensed: Declaration      = apply("condensed")
    def semiCondensed: Declaration  = apply("semi-condensed")
    def semiExpanded: Declaration   = apply("semi-expanded")
    def expanded: Declaration       = apply("expanded")
    def extraExpanded: Declaration  = apply("extra-expanded")
    def ultraExpanded: Declaration  = apply("ultra-expanded")

  /** `appearance` — `none | auto | base | base-select | <compat-auto> | <compat-special>`. Initial: `none`. Inherited:
    * no.
    */
  object appearance extends DS("appearance") with Auto with None:
    def base: Declaration           = apply("base")
    def baseSelect: Declaration     = apply("base-select")
    def searchfield: Declaration    = apply("searchfield")
    def textarea: Declaration       = apply("textarea")
    def checkbox: Declaration       = apply("checkbox")
    def radio: Declaration          = apply("radio")
    def menulist: Declaration       = apply("menulist")
    def listbox: Declaration        = apply("listbox")
    def meter: Declaration          = apply("meter")
    def progressBar: Declaration    = apply("progress-bar")
    def button: Declaration         = apply("button")
    def textfield: Declaration      = apply("textfield")
    def menulistButton: Declaration = apply("menulist-button")
  end appearance

  /** `field-sizing` — `fixed | content`. Initial: `fixed`. Inherited: no. */
  object fieldSizing extends DS("field-sizing"):
    def fixed: Declaration   = apply("fixed")
    def content: Declaration = apply("content")

  /** `slider-orientation` — `auto | left-to-right | right-to-left | top-to-bottom | bottom-to-top`. Initial: `auto`.
    * Inherited: no.
    */
  object sliderOrientation extends DS("slider-orientation") with Auto:
    def leftToRight: Declaration = apply("left-to-right")
    def rightToLeft: Declaration = apply("right-to-left")
    def topToBottom: Declaration = apply("top-to-bottom")
    def bottomToTop: Declaration = apply("bottom-to-top")

  /** `input-security` — `auto | none`. Initial: `auto`. Inherited: no. */
  object inputSecurity extends DS("input-security") with Auto with None

  /** `row-gap` — `normal | <length-percentage [0,∞]> | <line-width>`. Initial: `normal`. Inherited: no. */
  object rowGap extends DS("row-gap") with Length with Percent with Normal with LineWidth

  /** `column-gap` — `normal | <length-percentage [0,∞]> | <line-width>`. Initial: `normal`. Inherited: no. */
  object columnGap extends DS("column-gap") with Length with Percent with Normal with LineWidth

  /** `gap` — `<'row-gap'> <'column-gap'>?`. Initial: `see individual properties`. Inherited: no. */
  object gap extends DS("gap") with Length with Percent with Normal with LineWidth

  /** `column-rule-break` — `none | normal | intersection`. Initial: `normal`. Inherited: no. */
  object columnRuleBreak extends DS("column-rule-break") with None with Normal:
    def intersection: Declaration = apply("intersection")

  /** `row-rule-break` — `none | normal | intersection`. Initial: `normal`. Inherited: no. */
  object rowRuleBreak extends DS("row-rule-break") with None with Normal:
    def intersection: Declaration = apply("intersection")

  /** `rule-break` — `<'column-rule-break'>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object ruleBreak extends DS("rule-break") with None with Normal:
    def intersection: Declaration = apply("intersection")

  /** `column-rule-inset-cap-start` — `<inset-value>`. Initial: `0`. Inherited: no. */
  object columnRuleInsetCapStart extends DS("column-rule-inset-cap-start") with Length with Percent with InsetValue

  /** `column-rule-inset-cap-end` — `<inset-value>`. Initial: `0`. Inherited: no. */
  object columnRuleInsetCapEnd extends DS("column-rule-inset-cap-end") with Length with Percent with InsetValue

  /** `column-rule-inset-junction-start` — `<inset-value>`. Initial: `0`. Inherited: no. */
  object columnRuleInsetJunctionStart
      extends DS("column-rule-inset-junction-start")
      with Length
      with Percent
      with InsetValue

  /** `column-rule-inset-junction-end` — `<inset-value>`. Initial: `0`. Inherited: no. */
  object columnRuleInsetJunctionEnd
      extends DS("column-rule-inset-junction-end")
      with Length
      with Percent
      with InsetValue

  /** `row-rule-inset-cap-start` — `<inset-value>`. Initial: `0`. Inherited: no. */
  object rowRuleInsetCapStart extends DS("row-rule-inset-cap-start") with Length with Percent with InsetValue

  /** `row-rule-inset-cap-end` — `<inset-value>`. Initial: `0`. Inherited: no. */
  object rowRuleInsetCapEnd extends DS("row-rule-inset-cap-end") with Length with Percent with InsetValue

  /** `row-rule-inset-junction-start` — `<inset-value>`. Initial: `0`. Inherited: no. */
  object rowRuleInsetJunctionStart extends DS("row-rule-inset-junction-start") with Length with Percent with InsetValue

  /** `row-rule-inset-junction-end` — `<inset-value>`. Initial: `0`. Inherited: no. */
  object rowRuleInsetJunctionEnd extends DS("row-rule-inset-junction-end") with Length with Percent with InsetValue

  /** `column-rule-inset-start` — `<inset-value>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object columnRuleInsetStart extends DS("column-rule-inset-start") with Length with Percent with InsetValue

  /** `column-rule-inset-end` — `<inset-value>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object columnRuleInsetEnd extends DS("column-rule-inset-end") with Length with Percent with InsetValue

  /** `row-rule-inset-start` — `<inset-value>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object rowRuleInsetStart extends DS("row-rule-inset-start") with Length with Percent with InsetValue

  /** `row-rule-inset-end` — `<inset-value>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object rowRuleInsetEnd extends DS("row-rule-inset-end") with Length with Percent with InsetValue

  /** `rule-inset-start` — `<'column-rule-inset-start'>`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object ruleInsetStart extends DS("rule-inset-start") with Length with Percent with InsetValue

  /** `rule-inset-end` — `<'column-rule-inset-end'>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object ruleInsetEnd extends DS("rule-inset-end") with Length with Percent with InsetValue

  /** `column-rule-inset-cap` — `<inset-value> <inset-value>?`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object columnRuleInsetCap extends DS("column-rule-inset-cap") with Length with Percent with InsetValue

  /** `column-rule-inset-junction` — `<inset-value> <inset-value>?`. Initial: `see individual properties`. Inherited:
    * see individual properties.
    */
  object columnRuleInsetJunction extends DS("column-rule-inset-junction") with Length with Percent with InsetValue

  /** `row-rule-inset-cap` — `<inset-value> <inset-value>?`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object rowRuleInsetCap extends DS("row-rule-inset-cap") with Length with Percent with InsetValue

  /** `row-rule-inset-junction` — `<inset-value> <inset-value>?`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object rowRuleInsetJunction extends DS("row-rule-inset-junction") with Length with Percent with InsetValue

  /** `rule-inset-cap` — `<'column-rule-inset-cap'>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object ruleInsetCap extends DS("rule-inset-cap") with Length with Percent with InsetValue

  /** `rule-inset-junction` — `<'column-rule-inset-junction'>`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object ruleInsetJunction extends DS("rule-inset-junction") with Length with Percent with InsetValue

  /** `column-rule-inset` — `<'column-rule-inset-cap'> [ / <'column-rule-inset-junction'> ]?`. Initial:
    * `see individual properties`. Inherited: see individual properties.
    */
  object columnRuleInset extends DS("column-rule-inset") with Length with Percent with InsetValue

  /** `row-rule-inset` — `<'column-rule-inset-cap'> [ / <'column-rule-inset-junction'> ]?`. Initial:
    * `see individual properties`. Inherited: see individual properties.
    */
  object rowRuleInset extends DS("row-rule-inset") with Length with Percent with InsetValue

  /** `rule-inset` — `<'column-rule-inset'>`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object ruleInset extends DS("rule-inset")

  /** `column-rule-visibility-items` — `all | around | between | normal`. Initial: `normal`. Inherited: no. */
  object columnRuleVisibilityItems extends DS("column-rule-visibility-items") with Normal:
    def all: Declaration     = apply("all")
    def around: Declaration  = apply("around")
    def between: Declaration = apply("between")

  /** `row-rule-visibility-items` — `all | around | between | normal`. Initial: `normal`. Inherited: no. */
  object rowRuleVisibilityItems extends DS("row-rule-visibility-items") with Normal:
    def all: Declaration     = apply("all")
    def around: Declaration  = apply("around")
    def between: Declaration = apply("between")

  /** `rule-visibility-items` — `<'column-rule-visibility-items'>`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object ruleVisibilityItems extends DS("rule-visibility-items") with Normal:
    def all: Declaration     = apply("all")
    def around: Declaration  = apply("around")
    def between: Declaration = apply("between")

  /** `rule-overlap` — `row-over-column | column-over-row`. Initial: `row-over-column`. Inherited: no. */
  object ruleOverlap extends DS("rule-overlap"):
    def rowOverColumn: Declaration = apply("row-over-column")
    def columnOverRow: Declaration = apply("column-over-row")

  /** `column-rule-color` — `<line-color-list> | <auto-line-color-list>`. Initial: `currentcolor`. Inherited: no. */
  object columnRuleColor extends DS("column-rule-color")

  /** `row-rule-color` — `<line-color-list> | <auto-line-color-list>`. Initial: `currentcolor`. Inherited: no. */
  object rowRuleColor extends DS("row-rule-color")

  /** `rule-color` — `<'column-rule-color'>`. Initial: `see individual properties`. Inherited: no. */
  object ruleColor extends DS("rule-color")

  /** `column-rule-style` — `<line-style-list> | <auto-line-style-list>`. Initial: `none`. Inherited: no. */
  object columnRuleStyle extends DS("column-rule-style")

  /** `row-rule-style` — `<line-style-list> | <auto-line-style-list>`. Initial: `none`. Inherited: no. */
  object rowRuleStyle extends DS("row-rule-style")

  /** `rule-style` — `<'column-rule-style'>`. Initial: `see individual properties`. Inherited: no. */
  object ruleStyle extends DS("rule-style")

  /** `column-rule-width` — `<line-width-list> | <auto-line-width-list>`. Initial: `medium`. Inherited: no. */
  object columnRuleWidth extends DS("column-rule-width")

  /** `row-rule-width` — `<line-width-list> | <auto-line-width-list>`. Initial: `medium`. Inherited: no. */
  object rowRuleWidth extends DS("row-rule-width")

  /** `rule-width` — `<'column-rule-width'>`. Initial: `see individual properties`. Inherited: no. */
  object ruleWidth extends DS("rule-width")

  /** `column-rule` — `<gap-rule-list> | <gap-auto-rule-list>`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object columnRule extends DS("column-rule") with Borderish

  /** `row-rule` — `<gap-rule-list> | <gap-auto-rule-list>`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object rowRule extends DS("row-rule") with Borderish

  /** `rule` — `<'column-rule'>`. Initial: `see individual properties`. Inherited: no. */
  object rule extends DS("rule") with Borderish

  /** `grid-row-gap` — `normal | <length-percentage [0,∞]> | <line-width>`. */
  object gridRowGap extends DS("grid-row-gap") with Length with Percent with Normal with LineWidth

  /** `grid-column-gap` — `normal | <length-percentage [0,∞]> | <line-width>`. */
  object gridColumnGap extends DS("grid-column-gap") with Length with Percent with Normal with LineWidth

  /** `grid-gap` — `<'row-gap'> <'column-gap'>?`. */
  object gridGap extends DS("grid-gap") with Length with Percent with Normal with LineWidth

  /** `copy-into` — `none | [ [ <custom-ident> <content-level>] [, <custom-ident> <content-level>]* ]?`. Initial:
    * `none`. Inherited: no.
    */
  // could not parse: Expected root:1:1 / alternation:1:1 / anyOrder:1:8 / allInOrder:1:8 / juxtaposed:1:8 / postfix:1:8 / atom:1:8 / group:1:8 / (postfix | "&&" | "||" | "|" | "]"):
  object copyInto extends DS("copy-into")

  /** `position` — `static | relative | absolute | sticky | fixed`. Initial: `static`. Inherited: no. */
  object position extends DS("position"):
    def static: Declaration   = apply("static")
    def relative: Declaration = apply("relative")
    def absolute: Declaration = apply("absolute")
    def sticky: Declaration   = apply("sticky")
    def fixed: Declaration    = apply("fixed")

  /** `float` —
    * `block-start | block-end | inline-start | inline-end | snap-block | <snap-block()> | snap-inline | <snap-inline()> | left | right | top | bottom | none`.
    * Initial: `none`. Inherited: no.
    */
  object float extends DS("float") with None:
    def blockStart: Declaration  = apply("block-start")
    def blockEnd: Declaration    = apply("block-end")
    def inlineStart: Declaration = apply("inline-start")
    def inlineEnd: Declaration   = apply("inline-end")
    def snapBlock: Declaration   = apply("snap-block")
    def snapInline: Declaration  = apply("snap-inline")
    def left: Declaration        = apply("left")
    def right: Declaration       = apply("right")
    def top: Declaration         = apply("top")
    def bottom: Declaration      = apply("bottom")
  end float

  /** `footnote-display` — `block | inline | compact`. Initial: `block`. Inherited: no. */
  object footnoteDisplay extends DS("footnote-display"):
    def block: Declaration   = apply("block")
    def inline: Declaration  = apply("inline")
    def compact: Declaration = apply("compact")

  /** `footnote-policy` — `auto | line | block`. Initial: `auto`. Inherited: no. */
  object footnotePolicy extends DS("footnote-policy") with Auto:
    def line: Declaration  = apply("line")
    def block: Declaration = apply("block")

  /** `flow-tolerance` — `normal | <length-percentage [0,∞]> | infinite`. Initial: `normal`. Inherited: no. */
  object flowTolerance extends DS("flow-tolerance") with Length with Percent with Normal:
    def infinite: Declaration = apply("infinite")

  /** `grid-template-columns` — `none | <track-list> | <auto-track-list> | subgrid <line-name-list>?`. Initial: `none`.
    * Inherited: no.
    */
  object gridTemplateColumns extends DS("grid-template-columns") with Auto with None with GridTrackish:
    def subgrid: Declaration    = apply("subgrid")
    def maxContent: Declaration = apply("max-content")
    def minContent: Declaration = apply("min-content")

  /** `grid-template-rows` — `none | <track-list> | <auto-track-list> | subgrid <line-name-list>?`. Initial: `none`.
    * Inherited: no.
    */
  object gridTemplateRows extends DS("grid-template-rows") with Auto with None with GridTrackish:
    def subgrid: Declaration    = apply("subgrid")
    def maxContent: Declaration = apply("max-content")
    def minContent: Declaration = apply("min-content")

  /** `grid-template-areas` — `none | <string>+`. Initial: `none`. Inherited: no. */
  object gridTemplateAreas extends DS("grid-template-areas") with None

  /** `grid-template` —
    * `none | [ <'grid-template-rows'> / <'grid-template-columns'> ] | [ <line-names>? <string> <track-size>? <line-names>? ]+ [ / <explicit-track-list> ]?`.
    * Initial: `none`. Inherited: see individual properties.
    */
  object gridTemplate extends DS("grid-template") with Auto with None with GridTrackish:
    def subgrid: Declaration    = apply("subgrid")
    def maxContent: Declaration = apply("max-content")
    def minContent: Declaration = apply("min-content")

  /** `grid-auto-columns` — `<track-size>+`. Initial: `auto`. Inherited: no. */
  object gridAutoColumns extends DS("grid-auto-columns") with GridTrackish

  /** `grid-auto-rows` — `<track-size>+`. Initial: `auto`. Inherited: no. */
  object gridAutoRows extends DS("grid-auto-rows") with GridTrackish

  /** `grid-auto-flow` — `[ row | column ] || dense`. Initial: `row`. Inherited: no. */
  object gridAutoFlow extends DS("grid-auto-flow"):
    def row: Declaration    = apply("row")
    def column: Declaration = apply("column")
    def dense: Declaration  = apply("dense")

  /** `grid` —
    * `<'grid-template'> | <'grid-template-rows'> / [ auto-flow && dense? ] <'grid-auto-columns'>? | [ auto-flow && dense? ] <'grid-auto-rows'>? / <'grid-template-columns'>`.
    * Initial: `none`. Inherited: see individual properties.
    */
  object grid extends DS("grid") with Auto with None with GridTrackish:
    def autoFlow: Declaration   = apply("auto-flow")
    def dense: Declaration      = apply("dense")
    def subgrid: Declaration    = apply("subgrid")
    def maxContent: Declaration = apply("max-content")
    def minContent: Declaration = apply("min-content")

  /** `grid-row-start` — `<grid-line>`. Initial: `auto`. Inherited: no. */
  object gridRowStart extends DS("grid-row-start")

  /** `grid-column-start` — `<grid-line>`. Initial: `auto`. Inherited: no. */
  object gridColumnStart extends DS("grid-column-start")

  /** `grid-row-end` — `<grid-line>`. Initial: `auto`. Inherited: no. */
  object gridRowEnd extends DS("grid-row-end")

  /** `grid-column-end` — `<grid-line>`. Initial: `auto`. Inherited: no. */
  object gridColumnEnd extends DS("grid-column-end")

  /** `grid-row` — `<grid-line> [ / <grid-line> ]?`. Initial: `auto`. Inherited: no. */
  object gridRow extends DS("grid-row")

  /** `grid-column` — `<grid-line> [ / <grid-line> ]?`. Initial: `auto`. Inherited: no. */
  object gridColumn extends DS("grid-column")

  /** `grid-area` — `<grid-line> [ / <grid-line> ]{0,3}`. Initial: `auto`. Inherited: no. */
  object gridArea extends DS("grid-area")

  /** `image-animation` — `normal | paused | stopped | running`. Initial: `normal`. Inherited: yes. */
  object imageAnimation extends DS("image-animation") with Normal:
    def paused: Declaration  = apply("paused")
    def stopped: Declaration = apply("stopped")
    def running: Declaration = apply("running")

  /** `object-fit` — `fill | contain | cover | none | scale-down`. Initial: `fill`. Inherited: no. */
  object objectFit extends DS("object-fit") with None:
    def fill: Declaration      = apply("fill")
    def contain: Declaration   = apply("contain")
    def cover: Declaration     = apply("cover")
    def scaleDown: Declaration = apply("scale-down")

  /** `image-resolution` — `[ from-image || <resolution> ] && snap?`. Initial: `1dppx`. Inherited: yes. */
  object imageResolution extends DS("image-resolution") with Resolution:
    def fromImage: Declaration = apply("from-image")
    def snap: Declaration      = apply("snap")

  /** `object-view-box` — `none | <basic-shape-rect>`. Initial: `none`. Inherited: no. */
  object objectViewBox extends DS("object-view-box") with None with Clipish

  /** `object-position` — `<position>`. Initial: `50% 50%`. Inherited: no. */
  object objectPosition extends DS("object-position") with Length with Percent with Positionish:
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")

  /** `image-orientation` — `from-image | none | [ <angle> || flip ]`. Initial: `from-image`. Inherited: yes. */
  object imageOrientation extends DS("image-orientation") with None with Angleish:
    def fromImage: Declaration = apply("from-image")
    def flip: Declaration      = apply("flip")
    def deg: Declaration       = apply("deg")
    def grad: Declaration      = apply("grad")
    def rad: Declaration       = apply("rad")
    def turn: Declaration      = apply("turn")

  /** `image-rendering` — `auto | smooth | high-quality | pixelated | crisp-edges`. Initial: `auto`. Inherited: yes. */
  object imageRendering extends DS("image-rendering") with Auto:
    def smooth: Declaration      = apply("smooth")
    def highQuality: Declaration = apply("high-quality")
    def pixelated: Declaration   = apply("pixelated")
    def crispEdges: Declaration  = apply("crisp-edges")

  /** `dominant-baseline` — `auto | <baseline-metric>`. Initial: `auto`. Inherited: yes. */
  object dominantBaseline extends DS("dominant-baseline") with Auto with BaselineMetric

  /** `vertical-align` — `[ first | last] || <'alignment-baseline'> || <'baseline-shift'>`. Initial: `baseline`.
    * Inherited: no.
    */
  object verticalAlign extends DS("vertical-align") with Length with Percent with Auto with BaselineMetric:
    def first: Declaration          = apply("first")
    def last: Declaration           = apply("last")
    def sub: Declaration            = apply("sub")
    def `super`: Declaration        = apply("super")
    def top: Declaration            = apply("top")
    def center: Declaration         = apply("center")
    def bottom: Declaration         = apply("bottom")
    def baseline: Declaration       = apply("baseline")
    def textBeforeEdge: Declaration = apply("text-before-edge")
    def textAfterEdge: Declaration  = apply("text-after-edge")
  end verticalAlign

  /** `baseline-source` — `auto | first | last`. Initial: `auto`. Inherited: no. */
  object baselineSource extends DS("baseline-source") with Auto:
    def first: Declaration = apply("first")
    def last: Declaration  = apply("last")

  /** `alignment-baseline` — `baseline | <baseline-metric>`. Initial: `baseline`. Inherited: no. */
  object alignmentBaseline extends DS("alignment-baseline") with Auto with BaselineMetric:
    def baseline: Declaration       = apply("baseline")
    def textBeforeEdge: Declaration = apply("text-before-edge")
    def textAfterEdge: Declaration  = apply("text-after-edge")

  /** `baseline-shift` — `<length-percentage> | sub | super | top | center | bottom`. Initial: `0`. Inherited: no. */
  object baselineShift extends DS("baseline-shift") with Length with Percent:
    def sub: Declaration      = apply("sub")
    def `super`: Declaration  = apply("super")
    def top: Declaration      = apply("top")
    def center: Declaration   = apply("center")
    def bottom: Declaration   = apply("bottom")
    def baseline: Declaration = apply("baseline")

  /** `line-height` — `normal | <number [0,∞]> | <length-percentage [0,∞]>`. Initial: `normal`. Inherited: yes. */
  object lineHeight extends DS("line-height") with Length with Percent with Normal with Numeric

  /** `line-fit-edge` — `leading | <text-edge>`. Initial: `leading`. Inherited: yes. */
  object lineFitEdge extends DS("line-fit-edge") with TextEdge

  /** `text-box` — `normal | <'text-box-trim'> || <'text-box-edge'>`. Initial: `normal`. Inherited: no. */
  object textBox extends DS("text-box") with Auto with None with Normal with TextEdge:
    def trimStart: Declaration = apply("trim-start")
    def trimEnd: Declaration   = apply("trim-end")
    def trimBoth: Declaration  = apply("trim-both")

  /** `text-box-trim` — `none | trim-start | trim-end | trim-both`. Initial: `none`. Inherited: no. */
  object textBoxTrim extends DS("text-box-trim") with None:
    def trimStart: Declaration = apply("trim-start")
    def trimEnd: Declaration   = apply("trim-end")
    def trimBoth: Declaration  = apply("trim-both")

  /** `text-box-edge` — `auto | <text-edge>`. Initial: `auto`. Inherited: yes. */
  object textBoxEdge extends DS("text-box-edge") with Auto with TextEdge

  /** `inline-sizing` — `normal | stretch`. Initial: `normal`. Inherited: yes. */
  object inlineSizing extends DS("inline-sizing") with Normal:
    def stretch: Declaration = apply("stretch")

  /** `initial-letter` — `normal | <number [1,∞]> <integer [1,∞]> | <number [1,∞]> && [ drop | raise ]?`. Initial:
    * `normal`. Inherited: no.
    */
  object initialLetter extends DS("initial-letter") with Normal with Numeric:
    def drop: Declaration  = apply("drop")
    def raise: Declaration = apply("raise")

  /** `initial-letter-align` — `[ border-box? [ alphabetic | ideographic | hanging | leading ]? ]!`. Initial:
    * `alphabetic`. Inherited: yes.
    */
  object initialLetterAlign extends DS("initial-letter-align"):
    def borderBox: Declaration   = apply("border-box")
    def alphabetic: Declaration  = apply("alphabetic")
    def ideographic: Declaration = apply("ideographic")
    def hanging: Declaration     = apply("hanging")
    def leading: Declaration     = apply("leading")

  /** `initial-letter-wrap` — `none | first | all | grid | <length-percentage>`. Initial: `none`. Inherited: yes. */
  object initialLetterWrap extends DS("initial-letter-wrap") with Length with Percent with None:
    def first: Declaration = apply("first")
    def all: Declaration   = apply("all")
    def grid: Declaration  = apply("grid")

  /** `line-grid` — `match-parent | create`. Initial: `match-parent`. Inherited: no. */
  object lineGrid extends DS("line-grid"):
    def matchParent: Declaration = apply("match-parent")
    def create: Declaration      = apply("create")

  /** `line-snap` — `none | baseline | contain`. Initial: `none`. Inherited: yes. */
  object lineSnap extends DS("line-snap") with None:
    def baseline: Declaration = apply("baseline")
    def contain: Declaration  = apply("contain")

  /** `box-snap` — `none | block-start | block-end | center | baseline | last-baseline`. Initial: `none`. Inherited:
    * yes.
    */
  object boxSnap extends DS("box-snap") with None:
    def blockStart: Declaration   = apply("block-start")
    def blockEnd: Declaration     = apply("block-end")
    def center: Declaration       = apply("center")
    def baseline: Declaration     = apply("baseline")
    def lastBaseline: Declaration = apply("last-baseline")

  /** `link-parameters` — `none | <param()>#`. Initial: `none`. Inherited: no. */
  object linkParameters extends DS("link-parameters") with None

  /** `list-style-image` — `<image> | none`. Initial: `none`. Inherited: yes. */
  object listStyleImage extends DS("list-style-image") with None with Imageish

  /** `list-style-type` — `<counter-style> | <string> | none`. Initial: `disc`. Inherited: yes. */
  object listStyleType extends DS("list-style-type") with None with CounterStyleKw

  /** `list-style-position` — `inside | outside`. Initial: `outside`. Inherited: yes. */
  object listStylePosition extends DS("list-style-position"):
    def inside: Declaration  = apply("inside")
    def outside: Declaration = apply("outside")

  /** `list-style` — `<'list-style-position'> || <'list-style-image'> || <'list-style-type'>`. Initial:
    * `see individual properties`. Inherited: see individual properties.
    */
  object listStyle extends DS("list-style") with None with CounterStyleKw with Imageish:
    def inside: Declaration  = apply("inside")
    def outside: Declaration = apply("outside")

  /** `marker-side` — `match-self | match-parent`. Initial: `match-self`. Inherited: yes. */
  object markerSide extends DS("marker-side"):
    def matchSelf: Declaration   = apply("match-self")
    def matchParent: Declaration = apply("match-parent")

  /** `counter-reset` — `[ <counter-name> <integer>? | <reversed-counter-name> <integer>? ]+ | none`. Initial: `none`.
    * Inherited: no.
    */
  object counterReset extends DS("counter-reset") with None with Numeric:
    def listItem: Declaration = apply("list-item")

  /** `counter-increment` — `[ <counter-name> <integer>? ]+ | none`. Initial: `none`. Inherited: no. */
  object counterIncrement extends DS("counter-increment") with None with Numeric:
    def listItem: Declaration = apply("list-item")

  /** `counter-set` — `[ <counter-name> <integer>? ]+ | none`. Initial: `none`. Inherited: no. */
  object counterSet extends DS("counter-set") with None with Numeric:
    def listItem: Declaration = apply("list-item")

  /** `block-size` — `<'width'>`. Initial: `auto`. Inherited: no. */
  object blockSize extends DS("block-size") with Length with Percent with Auto:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `inline-size` — `<'width'>`. Initial: `auto`. Inherited: no. */
  object inlineSize extends DS("inline-size") with Length with Percent with Auto:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `min-block-size` — `<'min-width'>`. Initial: `0`. Inherited: no. */
  object minBlockSize extends DS("min-block-size") with Length with Percent with Auto:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `min-inline-size` — `<'min-width'>`. Initial: `0`. Inherited: no. */
  object minInlineSize extends DS("min-inline-size") with Length with Percent with Auto:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `max-block-size` — `<'max-width'>`. Initial: `none`. Inherited: no. */
  object maxBlockSize extends DS("max-block-size") with Length with Percent with None:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `max-inline-size` — `<'max-width'>`. Initial: `none`. Inherited: no. */
  object maxInlineSize extends DS("max-inline-size") with Length with Percent with None:
    def minContent: Declaration = apply("min-content")
    def maxContent: Declaration = apply("max-content")

  /** `margin-block-start` — `<'margin-top'>`. Initial: `0`. Inherited: no. */
  object marginBlockStart extends DS("margin-block-start") with Length with Percent with Auto

  /** `margin-block-end` — `<'margin-top'>`. Initial: `0`. Inherited: no. */
  object marginBlockEnd extends DS("margin-block-end") with Length with Percent with Auto

  /** `margin-inline-start` — `<'margin-top'>`. Initial: `0`. Inherited: no. */
  object marginInlineStart extends DS("margin-inline-start") with Length with Percent with Auto

  /** `margin-inline-end` — `<'margin-top'>`. Initial: `0`. Inherited: no. */
  object marginInlineEnd extends DS("margin-inline-end") with Length with Percent with Auto

  /** `margin-block` — `<'margin-top'>{1,2}`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object marginBlock extends DS("margin-block") with Length with Percent with Auto with LengthBox

  /** `margin-inline` — `<'margin-top'>{1,2}`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object marginInline extends DS("margin-inline") with Length with Percent with Auto with LengthBox

  /** `padding-block-start` — `<'padding-top'>`. Initial: `0`. Inherited: no. */
  object paddingBlockStart extends DS("padding-block-start") with Length with Percent

  /** `padding-block-end` — `<'padding-top'>`. Initial: `0`. Inherited: no. */
  object paddingBlockEnd extends DS("padding-block-end") with Length with Percent

  /** `padding-inline-start` — `<'padding-top'>`. Initial: `0`. Inherited: no. */
  object paddingInlineStart extends DS("padding-inline-start") with Length with Percent

  /** `padding-inline-end` — `<'padding-top'>`. Initial: `0`. Inherited: no. */
  object paddingInlineEnd extends DS("padding-inline-end") with Length with Percent

  /** `padding-block` — `<'padding-top'>{1,2}`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object paddingBlock extends DS("padding-block") with Length with Percent with LengthBox

  /** `padding-inline` — `<'padding-top'>{1,2}`. Initial: `see individual properties`. Inherited: see individual
    * properties.
    */
  object paddingInline extends DS("padding-inline") with Length with Percent with LengthBox

  /** `clip-path` — `<clip-source> | [ <basic-shape> || <geometry-box> ] | none`. Initial: `none`. Inherited: no. */
  object clipPath extends DS("clip-path") with None with BasicShapeish with GeometryBox:
    override def borderBox: Declaration  = apply("border-box")
    override def contentBox: Declaration = apply("content-box")
    override def paddingBox: Declaration = apply("padding-box")

  /** `clip-rule` — `nonzero | evenodd`. Initial: `nonzero`. Inherited: yes. */
  object clipRule extends DS("clip-rule"):
    def nonzero: Declaration = apply("nonzero")
    def evenodd: Declaration = apply("evenodd")

  /** `mask-image` — `<mask-reference>#`. Initial: `none`. Inherited: no. */
  object maskImage extends DS("mask-image") with None with Imageish

  /** `mask-mode` — `<masking-mode>#`. Initial: `match-source`. Inherited: no. */
  object maskMode extends DS("mask-mode") with MaskingMode

  /** `mask-repeat` — `<repeat-style>#`. Initial: `repeat`. Inherited: no. */
  object maskRepeat extends DS("mask-repeat") with RepeatStyle

  /** `mask-position` — `<position>#`. Initial: `0% 0%`. Inherited: no. */
  object maskPosition extends DS("mask-position") with Length with Percent with Positionish:
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")

  /** `mask-clip` — `[ <coord-box> | no-clip ]#`. Initial: `border-box`. Inherited: no. */
  object maskClip extends DS("mask-clip") with CoordBox:
    def noClip: Declaration = apply("no-clip")

  /** `mask-origin` — `<coord-box>#`. Initial: `border-box`. Inherited: no. */
  object maskOrigin extends DS("mask-origin") with CoordBox

  /** `mask-size` — `<bg-size>#`. Initial: `auto`. Inherited: no. */
  object maskSize extends DS("mask-size") with Length with Percent with Auto with BgSize

  /** `mask-composite` — `<compositing-operator>#`. Initial: `add`. Inherited: no. */
  object maskComposite extends DS("mask-composite") with CompositingOperator

  /** `mask` — `<mask-layer>#`. Initial: `see individual properties`. Inherited: no. */
  object mask extends DS("mask") with MaskLayer with Positionish:
    override def borderBox: Declaration  = apply("border-box")
    override def contentBox: Declaration = apply("content-box")
    override def paddingBox: Declaration = apply("padding-box")

  /** `mask-border-source` — `<'border-image-source'>`. Initial: `none`. Inherited: no. */
  object maskBorderSource extends DS("mask-border-source") with None with Imageish

  /** `mask-border-mode` — `luminance | alpha`. Initial: `alpha`. Inherited: no. */
  object maskBorderMode extends DS("mask-border-mode"):
    def luminance: Declaration = apply("luminance")
    def alpha: Declaration     = apply("alpha")

  /** `mask-border-slice` — `<'border-image-slice'>`. Initial: `0`. Inherited: no. */
  object maskBorderSlice extends DS("mask-border-slice") with Percent with Numeric:
    def fill: Declaration = apply("fill")

  /** `mask-border-width` — `<'border-image-width'>`. Initial: `auto`. Inherited: no. */
  object maskBorderWidth extends DS("mask-border-width") with Length with Percent with Auto with Numeric

  /** `mask-border-outset` — `<'border-image-outset'>`. Initial: `0`. Inherited: no. */
  object maskBorderOutset extends DS("mask-border-outset") with Length with Numeric:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end maskBorderOutset

  /** `mask-border-repeat` — `<'border-image-repeat'>`. Initial: `stretch`. Inherited: no. */
  object maskBorderRepeat extends DS("mask-border-repeat"):
    def stretch: Declaration = apply("stretch")
    def repeat: Declaration  = apply("repeat")
    def round: Declaration   = apply("round")
    def space: Declaration   = apply("space")

  /** `mask-border` —
    * `<'mask-border-source'> || <'mask-border-slice'> [ / <'mask-border-width'>? [ / <'mask-border-outset'> ]? ]? || <'mask-border-repeat'> || <'mask-border-mode'>`.
    * Initial: `See individual properties`. Inherited: no.
    */
  object maskBorder extends DS("mask-border"):
    def fill: Declaration      = apply("fill")
    def luminance: Declaration = apply("luminance")
    def alpha: Declaration     = apply("alpha")

  /** `mask-type` — `luminance | alpha`. Initial: `luminance`. Inherited: no. */
  object maskType extends DS("mask-type"):
    def luminance: Declaration = apply("luminance")
    def alpha: Declaration     = apply("alpha")

  /** `clip` — `<rect()> | auto`. Initial: `auto`. Inherited: no. */
  object clip extends DS("clip") with Auto with Clipish

  /** `column-width` — `auto | <length [0,∞]>`. Initial: `auto`. Inherited: no. */
  object columnWidth extends DS("column-width") with Length with Auto:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end columnWidth

  /** `column-height` — `auto | <length [0,∞]>`. Initial: `auto`. Inherited: no. */
  object columnHeight extends DS("column-height") with Length with Auto:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end columnHeight

  /** `column-count` — `auto | <integer [1,∞]>`. Initial: `auto`. Inherited: no. */
  object columnCount extends DS("column-count") with Auto with Numeric

  /** `column-wrap` — `auto | nowrap | wrap`. Initial: `auto`. Inherited: no. */
  object columnWrap extends DS("column-wrap") with Auto:
    def nowrap: Declaration = apply("nowrap")
    def wrap: Declaration   = apply("wrap")

  /** `columns` — `[ <'column-width'> || <'column-count'> ] [ / <'column-height'> ]?`. Initial:
    * `see individual properties`. Inherited: see individual properties.
    */
  object columns extends DS("columns") with Length with Auto with Numeric:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end columns

  /** `column-span` — `none | <integer [1,∞]> | all | auto`. Initial: `none`. Inherited: no. */
  object columnSpan extends DS("column-span") with Auto with None with Numeric:
    def all: Declaration = apply("all")

  /** `column-fill` — `auto | balance | balance-all`. Initial: `balance`. Inherited: no. */
  object columnFill extends DS("column-fill") with Auto:
    def balance: Declaration    = apply("balance")
    def balanceAll: Declaration = apply("balance-all")

  /** `spatial-navigation-contain` — `auto | contain`. Initial: `auto`. Inherited: no. */
  object spatialNavigationContain extends DS("spatial-navigation-contain") with Auto:
    def contain: Declaration = apply("contain")

  /** `spatial-navigation-action` — `auto | focus | scroll`. Initial: `auto`. Inherited: no. */
  object spatialNavigationAction extends DS("spatial-navigation-action") with Auto:
    def focus: Declaration  = apply("focus")
    def scroll: Declaration = apply("scroll")

  /** `spatial-navigation-function` — `normal | grid`. Initial: `normal`. Inherited: no. */
  object spatialNavigationFunction extends DS("spatial-navigation-function") with Normal:
    def grid: Declaration = apply("grid")

  /** `overflow-clip-margin-top` — `<visual-box> || <length>`. Initial: `0px`. Inherited: no. */
  object overflowClipMarginTop extends DS("overflow-clip-margin-top") with Length with VisualBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end overflowClipMarginTop

  /** `overflow-clip-margin-right` — `<visual-box> || <length>`. Initial: `0px`. Inherited: no. */
  object overflowClipMarginRight extends DS("overflow-clip-margin-right") with Length with VisualBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end overflowClipMarginRight

  /** `overflow-clip-margin-bottom` — `<visual-box> || <length>`. Initial: `0px`. Inherited: no. */
  object overflowClipMarginBottom extends DS("overflow-clip-margin-bottom") with Length with VisualBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end overflowClipMarginBottom

  /** `overflow-clip-margin-left` — `<visual-box> || <length>`. Initial: `0px`. Inherited: no. */
  object overflowClipMarginLeft extends DS("overflow-clip-margin-left") with Length with VisualBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end overflowClipMarginLeft

  /** `overflow-clip-margin-block-start` — `<visual-box> || <length>`. Initial: `0px`. Inherited: no. */
  object overflowClipMarginBlockStart extends DS("overflow-clip-margin-block-start") with Length with VisualBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end overflowClipMarginBlockStart

  /** `overflow-clip-margin-inline-start` — `<visual-box> || <length>`. Initial: `0px`. Inherited: no. */
  object overflowClipMarginInlineStart extends DS("overflow-clip-margin-inline-start") with Length with VisualBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end overflowClipMarginInlineStart

  /** `overflow-clip-margin-block-end` — `<visual-box> || <length>`. Initial: `0px`. Inherited: no. */
  object overflowClipMarginBlockEnd extends DS("overflow-clip-margin-block-end") with Length with VisualBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end overflowClipMarginBlockEnd

  /** `overflow-clip-margin-inline-end` — `<visual-box> || <length>`. Initial: `0px`. Inherited: no. */
  object overflowClipMarginInlineEnd extends DS("overflow-clip-margin-inline-end") with Length with VisualBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end overflowClipMarginInlineEnd

  /** `overflow-clip-margin` — `<visual-box> || <length>`. Initial: `0px`. Inherited: no. */
  object overflowClipMargin extends DS("overflow-clip-margin") with Length with VisualBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end overflowClipMargin

  /** `overflow-clip-margin-inline` — `<visual-box> || <length>`. Initial: `0px`. Inherited: no. */
  object overflowClipMarginInline extends DS("overflow-clip-margin-inline") with Length with VisualBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end overflowClipMarginInline

  /** `overflow-clip-margin-block` — `<visual-box> || <length>`. Initial: `0px`. Inherited: no. */
  object overflowClipMarginBlock extends DS("overflow-clip-margin-block") with Length with VisualBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end overflowClipMarginBlock

  /** `text-overflow` — `clip | ellipsis`. Initial: `clip`. Inherited: no. */
  object textOverflow extends DS("text-overflow"):
    def clip: Declaration     = apply("clip")
    def ellipsis: Declaration = apply("ellipsis")

  /** `block-ellipsis` — `no-ellipsis | auto | <string>`. Initial: `no-ellipsis`. Inherited: yes. */
  object blockEllipsis extends DS("block-ellipsis") with Auto:
    def noEllipsis: Declaration = apply("no-ellipsis")

  /** `line-clamp` — `none | [<integer [1,∞]> || <'block-ellipsis'>] -webkit-legacy?`. Initial: `none`. Inherited: see
    * individual properties.
    */
  // could not parse: Expected root:1:1 / (postfix | "&&" | "||" | "|" | end-of-input):1:48, found "-webkit-le"
  object lineClamp extends DS("line-clamp") with None

  /** `-webkit-line-clamp` — `none | <integer [1,∞]>`. Initial: `none`. Inherited: see individual properties. */
  object WebkitLineClamp extends DS("-webkit-line-clamp") with None with Numeric

  /** `continue` — `auto | discard | collapse | -webkit-legacy`. Initial: `auto`. Inherited: no. */
  // could not parse: Expected root:1:1 / ("&&" | "||" | "|" ~ ws ~ anyOrder | end-of-input):1:27, found "| -webkit-"
  object continue extends DS("continue") with Auto:
    def collapse: Declaration = apply("collapse")
    def discard: Declaration  = apply("discard")

  /** `max-lines` — `none | <integer [1,∞]>`. Initial: `none`. Inherited: no. */
  object maxLines extends DS("max-lines") with None with Numeric

  /** `scroll-target-group` — `none | auto`. Initial: `none`. Inherited: no. */
  object scrollTargetGroup extends DS("scroll-target-group") with Auto with None

  /** `scroll-marker-group` — `none | [ [ before | after ] || [ links | tabs ] ]`. Initial: `none`. Inherited: no. */
  object scrollMarkerGroup extends DS("scroll-marker-group") with None:
    def before: Declaration = apply("before")
    def after: Declaration  = apply("after")
    def links: Declaration  = apply("links")
    def tabs: Declaration   = apply("tabs")

  /** `overflow-x` — `visible | hidden | clip | scroll | auto`. Initial: `visible`. Inherited: no. */
  object overflowX extends DS("overflow-x") with Auto:
    def visible: Declaration = apply("visible")
    def hidden: Declaration  = apply("hidden")
    def clip: Declaration    = apply("clip")
    def scroll: Declaration  = apply("scroll")
    def overlay: Declaration = apply("overlay")

  /** `overflow-y` — `visible | hidden | clip | scroll | auto`. Initial: `visible`. Inherited: no. */
  object overflowY extends DS("overflow-y") with Auto:
    def visible: Declaration = apply("visible")
    def hidden: Declaration  = apply("hidden")
    def clip: Declaration    = apply("clip")
    def scroll: Declaration  = apply("scroll")
    def overlay: Declaration = apply("overlay")

  /** `overflow-block` — `visible | hidden | clip | scroll | auto`. Initial: `visible`. Inherited: no. */
  object overflowBlock extends DS("overflow-block") with Auto:
    def visible: Declaration = apply("visible")
    def hidden: Declaration  = apply("hidden")
    def clip: Declaration    = apply("clip")
    def scroll: Declaration  = apply("scroll")
    def overlay: Declaration = apply("overlay")

  /** `overflow-inline` — `visible | hidden | clip | scroll | auto`. Initial: `visible`. Inherited: no. */
  object overflowInline extends DS("overflow-inline") with Auto:
    def visible: Declaration = apply("visible")
    def hidden: Declaration  = apply("hidden")
    def clip: Declaration    = apply("clip")
    def scroll: Declaration  = apply("scroll")
    def overlay: Declaration = apply("overlay")

  /** `overflow` — `<'overflow-block'>{1,2}`. Initial: `visible`. Inherited: no. */
  object overflow extends DS("overflow") with Auto:
    def visible: Declaration = apply("visible")
    def hidden: Declaration  = apply("hidden")
    def clip: Declaration    = apply("clip")
    def scroll: Declaration  = apply("scroll")
    def overlay: Declaration = apply("overlay")

  /** `scroll-behavior` — `auto | smooth`. Initial: `auto`. Inherited: no. */
  object scrollBehavior extends DS("scroll-behavior") with Auto:
    def smooth: Declaration = apply("smooth")

  /** `scrollbar-gutter` — `auto | stable && both-edges?`. Initial: `auto`. Inherited: no. */
  object scrollbarGutter extends DS("scrollbar-gutter") with Auto:
    def stable: Declaration    = apply("stable")
    def bothEdges: Declaration = apply("both-edges")

  /** `overscroll-behavior` — `[ contain | none | auto | chain ]{1,2}`. Initial: `auto auto`. Inherited: no. */
  object overscrollBehavior extends DS("overscroll-behavior") with Auto with None:
    def contain: Declaration = apply("contain")
    def chain: Declaration   = apply("chain")

  /** `overscroll-behavior-x` — `contain | none | auto | chain`. Initial: `auto`. Inherited: no. */
  object overscrollBehaviorX extends DS("overscroll-behavior-x") with Auto with None:
    def contain: Declaration = apply("contain")
    def chain: Declaration   = apply("chain")

  /** `overscroll-behavior-y` — `contain | none | auto | chain`. Initial: `auto`. Inherited: no. */
  object overscrollBehaviorY extends DS("overscroll-behavior-y") with Auto with None:
    def contain: Declaration = apply("contain")
    def chain: Declaration   = apply("chain")

  /** `overscroll-behavior-inline` — `contain | none | auto | chain`. Initial: `auto`. Inherited: no. */
  object overscrollBehaviorInline extends DS("overscroll-behavior-inline") with Auto with None:
    def contain: Declaration = apply("contain")
    def chain: Declaration   = apply("chain")

  /** `overscroll-behavior-block` — `contain | none | auto | chain`. Initial: `auto`. Inherited: no. */
  object overscrollBehaviorBlock extends DS("overscroll-behavior-block") with Auto with None:
    def contain: Declaration = apply("contain")
    def chain: Declaration   = apply("chain")

  /** `float-reference` — `inline | column | region | page`. Initial: `inline`. Inherited: no. */
  object floatReference extends DS("float-reference"):
    def inline: Declaration = apply("inline")
    def column: Declaration = apply("column")
    def region: Declaration = apply("region")
    def page: Declaration   = apply("page")

  /** `clear` —
    * `inline-start | inline-end | block-start | block-end | left | right | top | bottom | both-inline | both-block | both | none`.
    * Initial: `none`. Inherited: no.
    */
  object clear extends DS("clear") with None:
    def inlineStart: Declaration = apply("inline-start")
    def inlineEnd: Declaration   = apply("inline-end")
    def blockStart: Declaration  = apply("block-start")
    def blockEnd: Declaration    = apply("block-end")
    def left: Declaration        = apply("left")
    def right: Declaration       = apply("right")
    def top: Declaration         = apply("top")
    def bottom: Declaration      = apply("bottom")
    def bothInline: Declaration  = apply("both-inline")
    def bothBlock: Declaration   = apply("both-block")
    def both: Declaration        = apply("both")
    def all: Declaration         = apply("all")
  end clear

  /** `float-defer` — `<integer> | last | none`. Initial: `none`. Inherited: no. */
  object floatDefer extends DS("float-defer") with None with Numeric:
    def last: Declaration = apply("last")

  /** `float-offset` — `<length-percentage>`. Initial: `0`. Inherited: no. */
  object floatOffset extends DS("float-offset") with Length with Percent

  /** `page` — `auto | <custom-ident>`. Initial: `auto`. Inherited: no (but see prose). */
  object page extends DS("page") with Auto

  /** `overlay` — `none | auto`. Initial: `none`. Inherited: no. */
  object overlay extends DS("overlay") with Auto with None

  /** `inset-block-start` — `auto | <length-percentage>`. Initial: `auto`. Inherited: no. */
  object insetBlockStart extends DS("inset-block-start") with Length with Percent with Auto

  /** `inset-inline-start` — `auto | <length-percentage>`. Initial: `auto`. Inherited: no. */
  object insetInlineStart extends DS("inset-inline-start") with Length with Percent with Auto

  /** `inset-block-end` — `auto | <length-percentage>`. Initial: `auto`. Inherited: no. */
  object insetBlockEnd extends DS("inset-block-end") with Length with Percent with Auto

  /** `inset-inline-end` — `auto | <length-percentage>`. Initial: `auto`. Inherited: no. */
  object insetInlineEnd extends DS("inset-inline-end") with Length with Percent with Auto

  /** `inset-block` — `<'top'>{1,2}`. Initial: `auto`. Inherited: no. */
  object insetBlock extends DS("inset-block") with Length with Percent with Auto with LengthBox

  /** `inset-inline` — `<'top'>{1,2}`. Initial: `auto`. Inherited: no. */
  object insetInline extends DS("inset-inline") with Length with Percent with Auto with LengthBox

  /** `inset` — `<'top'>{1,4}`. Initial: `auto`. Inherited: no. */
  object inset extends DS("inset") with Length with Percent with Auto with LengthBox

  /** `flow-into` — `none | <custom-ident> [element | content]?`. Initial: `none`. Inherited: no. */
  object flowInto extends DS("flow-into") with None:
    def element: Declaration = apply("element")
    def content: Declaration = apply("content")

  /** `flow-from` — `<custom-ident> | none`. Initial: `none`. Inherited: no. */
  object flowFrom extends DS("flow-from") with None

  /** `region-fragment` — `auto | break`. Initial: `auto`. Inherited: no. */
  object regionFragment extends DS("region-fragment") with Auto:
    def break: Declaration = apply("break")

  /** `block-step-size` — `none | <length [0,∞]>`. Initial: `none`. Inherited: no. */
  object blockStepSize extends DS("block-step-size") with Length with None:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end blockStepSize

  /** `block-step-insert` — `margin-box | padding-box | content-box`. Initial: `margin-box`. Inherited: no. */
  object blockStepInsert extends DS("block-step-insert"):
    def marginBox: Declaration  = apply("margin-box")
    def paddingBox: Declaration = apply("padding-box")
    def contentBox: Declaration = apply("content-box")

  /** `block-step-align` — `auto | center | start | end`. Initial: `auto`. Inherited: no. */
  object blockStepAlign extends DS("block-step-align") with Auto:
    def center: Declaration  = apply("center")
    def start: Declaration   = apply("start")
    def end: Declaration     = apply("end")
    def up: Declaration      = apply("up")
    def down: Declaration    = apply("down")
    def nearest: Declaration = apply("nearest")

  /** `block-step-round` — `up | down | nearest`. Initial: `up`. Inherited: no. */
  object blockStepRound extends DS("block-step-round"):
    def up: Declaration      = apply("up")
    def down: Declaration    = apply("down")
    def nearest: Declaration = apply("nearest")

  /** `block-step` — `<'block-step-size'> || <'block-step-insert'> || <'block-step-align'> || <'block-step-round'>`.
    * Initial: `see individual properties`. Inherited: no.
    */
  object blockStep extends DS("block-step") with Length with Auto with None:
    def rex: Declaration        = apply("rex")
    def cap: Declaration        = apply("cap")
    def rcap: Declaration       = apply("rcap")
    def rch: Declaration        = apply("rch")
    def ic: Declaration         = apply("ic")
    def ric: Declaration        = apply("ric")
    def lh: Declaration         = apply("lh")
    def rlh: Declaration        = apply("rlh")
    def svw: Declaration        = apply("svw")
    def lvw: Declaration        = apply("lvw")
    def dvw: Declaration        = apply("dvw")
    def svh: Declaration        = apply("svh")
    def lvh: Declaration        = apply("lvh")
    def dvh: Declaration        = apply("dvh")
    def vi: Declaration         = apply("vi")
    def svi: Declaration        = apply("svi")
    def lvi: Declaration        = apply("lvi")
    def dvi: Declaration        = apply("dvi")
    def vb: Declaration         = apply("vb")
    def svb: Declaration        = apply("svb")
    def lvb: Declaration        = apply("lvb")
    def dvb: Declaration        = apply("dvb")
    def svmin: Declaration      = apply("svmin")
    def lvmin: Declaration      = apply("lvmin")
    def dvmin: Declaration      = apply("dvmin")
    def svmax: Declaration      = apply("svmax")
    def lvmax: Declaration      = apply("lvmax")
    def dvmax: Declaration      = apply("dvmax")
    def Q: Declaration          = apply("Q")
    def marginBox: Declaration  = apply("margin-box")
    def paddingBox: Declaration = apply("padding-box")
    def contentBox: Declaration = apply("content-box")
    def center: Declaration     = apply("center")
    def start: Declaration      = apply("start")
    def end: Declaration        = apply("end")
    def up: Declaration         = apply("up")
    def down: Declaration       = apply("down")
    def nearest: Declaration    = apply("nearest")
  end blockStep

  /** `line-height-step` — `<length [0,∞]>`. Initial: `0`. Inherited: yes. */
  object lineHeightStep extends DS("line-height-step") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end lineHeightStep

  /** `border-boundary` — `none | parent | display`. Initial: `none`. Inherited: yes. */
  object borderBoundary extends DS("border-boundary") with None:
    def parent: Declaration  = apply("parent")
    def display: Declaration = apply("display")

  /** `ruby-position` — `[ alternate || [ over | under ] ] | inter-character`. Initial: `alternate`. Inherited: yes. */
  object rubyPosition extends DS("ruby-position"):
    def alternate: Declaration      = apply("alternate")
    def over: Declaration           = apply("over")
    def under: Declaration          = apply("under")
    def interCharacter: Declaration = apply("inter-character")

  /** `ruby-merge` — `separate | merge | auto`. Initial: `separate`. Inherited: yes. */
  object rubyMerge extends DS("ruby-merge") with Auto:
    def separate: Declaration = apply("separate")
    def merge: Declaration    = apply("merge")

  /** `ruby-align` — `start | center | space-between | space-around`. Initial: `space-around`. Inherited: yes. */
  object rubyAlign extends DS("ruby-align"):
    def start: Declaration        = apply("start")
    def center: Declaration       = apply("center")
    def spaceBetween: Declaration = apply("space-between")
    def spaceAround: Declaration  = apply("space-around")

  /** `ruby-overhang` — `auto | spaces`. Initial: `auto`. Inherited: yes. */
  object rubyOverhang extends DS("ruby-overhang") with Auto with None:
    def spaces: Declaration = apply("spaces")

  /** `overflow-anchor` — `auto | none`. Initial: `auto`. Inherited: no. */
  object overflowAnchor extends DS("overflow-anchor") with Auto with None

  /** `scroll-initial-target` — `none | nearest`. Initial: `none`. Inherited: no. */
  object scrollInitialTarget extends DS("scroll-initial-target") with None:
    def nearest: Declaration = apply("nearest")

  /** `scroll-snap-type` — `none | [ x | y | block | inline | both ] [ mandatory | proximity ]?`. Initial: `none`.
    * Inherited: no.
    */
  object scrollSnapType extends DS("scroll-snap-type") with None:
    def x: Declaration         = apply("x")
    def y: Declaration         = apply("y")
    def block: Declaration     = apply("block")
    def inline: Declaration    = apply("inline")
    def both: Declaration      = apply("both")
    def mandatory: Declaration = apply("mandatory")
    def proximity: Declaration = apply("proximity")

  /** `scroll-padding` — `[ auto | <length-percentage [0,∞]> ]{1,4}`. Initial: `auto`. Inherited: no. */
  object scrollPadding extends DS("scroll-padding") with Length with Percent with Auto with LengthBox

  /** `scroll-margin` — `<length>{1,4}`. Initial: `0`. Inherited: no. */
  object scrollMargin extends DS("scroll-margin") with Length with LengthBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end scrollMargin

  /** `scroll-snap-align` — `[ none | start | end | center ]{1,2}`. Initial: `none`. Inherited: no. */
  object scrollSnapAlign extends DS("scroll-snap-align") with None:
    def start: Declaration  = apply("start")
    def end: Declaration    = apply("end")
    def center: Declaration = apply("center")

  /** `scroll-snap-stop` — `normal | always`. Initial: `normal`. Inherited: no. */
  object scrollSnapStop extends DS("scroll-snap-stop") with Normal:
    def always: Declaration = apply("always")

  /** `scroll-padding-top` — `auto | <length-percentage [0,∞]>`. Initial: `auto`. Inherited: no. */
  object scrollPaddingTop extends DS("scroll-padding-top") with Length with Percent with Auto

  /** `scroll-padding-right` — `auto | <length-percentage [0,∞]>`. Initial: `auto`. Inherited: no. */
  object scrollPaddingRight extends DS("scroll-padding-right") with Length with Percent with Auto

  /** `scroll-padding-bottom` — `auto | <length-percentage [0,∞]>`. Initial: `auto`. Inherited: no. */
  object scrollPaddingBottom extends DS("scroll-padding-bottom") with Length with Percent with Auto

  /** `scroll-padding-left` — `auto | <length-percentage [0,∞]>`. Initial: `auto`. Inherited: no. */
  object scrollPaddingLeft extends DS("scroll-padding-left") with Length with Percent with Auto

  /** `scroll-padding-inline-start` — `auto | <length-percentage [0,∞]>`. Initial: `auto`. Inherited: no. */
  object scrollPaddingInlineStart extends DS("scroll-padding-inline-start") with Length with Percent with Auto

  /** `scroll-padding-block-start` — `auto | <length-percentage [0,∞]>`. Initial: `auto`. Inherited: no. */
  object scrollPaddingBlockStart extends DS("scroll-padding-block-start") with Length with Percent with Auto

  /** `scroll-padding-inline-end` — `auto | <length-percentage [0,∞]>`. Initial: `auto`. Inherited: no. */
  object scrollPaddingInlineEnd extends DS("scroll-padding-inline-end") with Length with Percent with Auto

  /** `scroll-padding-block-end` — `auto | <length-percentage [0,∞]>`. Initial: `auto`. Inherited: no. */
  object scrollPaddingBlockEnd extends DS("scroll-padding-block-end") with Length with Percent with Auto

  /** `scroll-padding-block` — `[ auto | <length-percentage [0,∞]> ]{1,2}`. Initial: `auto`. Inherited: no. */
  object scrollPaddingBlock extends DS("scroll-padding-block") with Length with Percent with Auto with LengthBox

  /** `scroll-padding-inline` — `[ auto | <length-percentage [0,∞]> ]{1,2}`. Initial: `auto`. Inherited: no. */
  object scrollPaddingInline extends DS("scroll-padding-inline") with Length with Percent with Auto with LengthBox

  /** `scroll-margin-top` — `<length>`. Initial: `0`. Inherited: no. */
  object scrollMarginTop extends DS("scroll-margin-top") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end scrollMarginTop

  /** `scroll-margin-right` — `<length>`. Initial: `0`. Inherited: no. */
  object scrollMarginRight extends DS("scroll-margin-right") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end scrollMarginRight

  /** `scroll-margin-bottom` — `<length>`. Initial: `0`. Inherited: no. */
  object scrollMarginBottom extends DS("scroll-margin-bottom") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end scrollMarginBottom

  /** `scroll-margin-left` — `<length>`. Initial: `0`. Inherited: no. */
  object scrollMarginLeft extends DS("scroll-margin-left") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end scrollMarginLeft

  /** `scroll-margin-block-start` — `<length>`. Initial: `0`. Inherited: no. */
  object scrollMarginBlockStart extends DS("scroll-margin-block-start") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end scrollMarginBlockStart

  /** `scroll-margin-inline-start` — `<length>`. Initial: `0`. Inherited: no. */
  object scrollMarginInlineStart extends DS("scroll-margin-inline-start") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end scrollMarginInlineStart

  /** `scroll-margin-block-end` — `<length>`. Initial: `0`. Inherited: no. */
  object scrollMarginBlockEnd extends DS("scroll-margin-block-end") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end scrollMarginBlockEnd

  /** `scroll-margin-inline-end` — `<length>`. Initial: `0`. Inherited: no. */
  object scrollMarginInlineEnd extends DS("scroll-margin-inline-end") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end scrollMarginInlineEnd

  /** `scroll-margin-block` — `<length>{1,2}`. Initial: `0`. Inherited: no. */
  object scrollMarginBlock extends DS("scroll-margin-block") with Length with LengthBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end scrollMarginBlock

  /** `scroll-margin-inline` — `<length>{1,2}`. Initial: `0`. Inherited: no. */
  object scrollMarginInline extends DS("scroll-margin-inline") with Length with LengthBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end scrollMarginInline

  /** `scrollbar-color` — `auto | <color>{2}`. Initial: `auto`. Inherited: yes. */
  object scrollbarColor extends DS("scrollbar-color") with Auto with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end scrollbarColor

  /** `scrollbar-width` — `auto | thin | none`. Initial: `auto`. Inherited: no. */
  object scrollbarWidth extends DS("scrollbar-width") with Auto with None:
    def thin: Declaration = apply("thin")

  /** `shape-inside` — `auto | outside-shape | [ <basic-shape> || shape-box ] | <image> | display`. Initial: `auto`.
    * Inherited: no.
    */
  object shapeInside extends DS("shape-inside") with Auto with BasicShapeish with Imageish:
    def outsideShape: Declaration = apply("outside-shape")
    def shapeBox: Declaration     = apply("shape-box")
    def display: Declaration      = apply("display")

  /** `shape-padding` — `<length-percentage [0,∞]>`. Initial: `0`. Inherited: no. */
  object shapePadding extends DS("shape-padding") with Length with Percent

  /** `shape-outside` — `none | [ <basic-shape> || <shape-box> ] | <image>`. Initial: `none`. Inherited: no. */
  object shapeOutside extends DS("shape-outside") with None with BasicShapeish with Imageish with ShapeBox:
    override def borderBox: Declaration  = apply("border-box")
    override def contentBox: Declaration = apply("content-box")
    override def paddingBox: Declaration = apply("padding-box")

  /** `shape-image-threshold` — `<opacity-value>`. Initial: `0`. Inherited: no. */
  object shapeImageThreshold extends DS("shape-image-threshold") with Numeric

  /** `shape-margin` — `<length-percentage [0,∞]>`. Initial: `0`. Inherited: no. */
  object shapeMargin extends DS("shape-margin") with Length with Percent

  /** `text-size-adjust` — `auto | none | <percentage [0,∞]>`. Initial: `auto`. Inherited: yes. */
  object textSizeAdjust extends DS("text-size-adjust") with Percent with Auto with None

  /** `aspect-ratio` — `auto || <ratio>`. Initial: `auto`. Inherited: no. */
  object aspectRatio extends DS("aspect-ratio") with Auto with Numeric

  /** `contain-intrinsic-width` — `auto? [ none | <length [0,∞]> ]`. Initial: `none`. Inherited: no. */
  object containIntrinsicWidth extends DS("contain-intrinsic-width") with Length with Auto with None:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end containIntrinsicWidth

  /** `contain-intrinsic-height` — `auto? [ none | <length [0,∞]> ]`. Initial: `none`. Inherited: no. */
  object containIntrinsicHeight extends DS("contain-intrinsic-height") with Length with Auto with None:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end containIntrinsicHeight

  /** `contain-intrinsic-block-size` — `auto? [ none | <length [0,∞]> ]`. Initial: `none`. Inherited: no. */
  object containIntrinsicBlockSize extends DS("contain-intrinsic-block-size") with Length with Auto with None:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end containIntrinsicBlockSize

  /** `contain-intrinsic-inline-size` — `auto? [ none | <length [0,∞]> ]`. Initial: `none`. Inherited: no. */
  object containIntrinsicInlineSize extends DS("contain-intrinsic-inline-size") with Length with Auto with None:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end containIntrinsicInlineSize

  /** `contain-intrinsic-size` — `[ auto? [ none | <length [0,∞]> ] ]{1,2}`. Initial: `see individual properties`.
    * Inherited: see individual properties.
    */
  object containIntrinsicSize extends DS("contain-intrinsic-size") with Length with Auto with None with LengthBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end containIntrinsicSize

  /** `frame-sizing` — `auto | content-width | content-height | content-block-size | content-inline-size`. Initial:
    * `auto`. Inherited: no.
    */
  object frameSizing extends DS("frame-sizing") with Auto:
    def contentWidth: Declaration      = apply("content-width")
    def contentHeight: Declaration     = apply("content-height")
    def contentBlockSize: Declaration  = apply("content-block-size")
    def contentInlineSize: Declaration = apply("content-inline-size")

  /** `min-intrinsic-sizing` — `legacy | zero-if-scroll || zero-if-extrinsic`. Initial: `legacy`. Inherited: no. */
  object minIntrinsicSizing extends DS("min-intrinsic-sizing"):
    def legacy: Declaration          = apply("legacy")
    def zeroIfScroll: Declaration    = apply("zero-if-scroll")
    def zeroIfExtrinsic: Declaration = apply("zero-if-extrinsic")

  /** `box-sizing` — `content-box | border-box`. Initial: `content-box`. Inherited: no. */
  object boxSizing extends DS("box-sizing"):
    def contentBox: Declaration = apply("content-box")
    def borderBox: Declaration  = apply("border-box")

  /** `voice-volume` — `silent | [ [ x-soft | soft | medium | loud | x-loud ] || <decibel> ]`. Initial: `medium`.
    * Inherited: yes.
    */
  object voiceVolume extends DS("voice-volume"):
    def silent: Declaration = apply("silent")
    def xSoft: Declaration  = apply("x-soft")
    def soft: Declaration   = apply("soft")
    def medium: Declaration = apply("medium")
    def loud: Declaration   = apply("loud")
    def xLoud: Declaration  = apply("x-loud")

  /** `voice-balance` — `<number> | left | center | right | leftwards | rightwards`. Initial: `center`. Inherited: yes.
    */
  object voiceBalance extends DS("voice-balance") with Numeric:
    def left: Declaration       = apply("left")
    def center: Declaration     = apply("center")
    def right: Declaration      = apply("right")
    def leftwards: Declaration  = apply("leftwards")
    def rightwards: Declaration = apply("rightwards")

  /** `speak` — `auto | never | always`. Initial: `auto`. Inherited: yes. */
  object speak extends DS("speak") with Auto:
    def never: Declaration  = apply("never")
    def always: Declaration = apply("always")

  /** `speak-as` — `normal | spell-out || digits || [ literal-punctuation | no-punctuation ]`. Initial: `normal`.
    * Inherited: yes.
    */
  object speakAs extends DS("speak-as") with Normal:
    def spellOut: Declaration           = apply("spell-out")
    def digits: Declaration             = apply("digits")
    def literalPunctuation: Declaration = apply("literal-punctuation")
    def noPunctuation: Declaration      = apply("no-punctuation")

  /** `pause-before` — `<time [0s,∞]> | none | x-weak | weak | medium | strong | x-strong`. Initial: `none`. Inherited:
    * no.
    */
  object pauseBefore extends DS("pause-before") with None with Timeish:
    def xWeak: Declaration   = apply("x-weak")
    def weak: Declaration    = apply("weak")
    def medium: Declaration  = apply("medium")
    def strong: Declaration  = apply("strong")
    def xStrong: Declaration = apply("x-strong")
    def s: Declaration       = apply("s")
    def ms: Declaration      = apply("ms")

  /** `pause-after` — `<time [0s,∞]> | none | x-weak | weak | medium | strong | x-strong`. Initial: `none`. Inherited:
    * no.
    */
  object pauseAfter extends DS("pause-after") with None with Timeish:
    def xWeak: Declaration   = apply("x-weak")
    def weak: Declaration    = apply("weak")
    def medium: Declaration  = apply("medium")
    def strong: Declaration  = apply("strong")
    def xStrong: Declaration = apply("x-strong")
    def s: Declaration       = apply("s")
    def ms: Declaration      = apply("ms")

  /** `pause` — `<'pause-before'> <'pause-after'>?`. Initial: `see individual properties`. Inherited: no. */
  object pause extends DS("pause") with None with Timeish:
    def xWeak: Declaration   = apply("x-weak")
    def weak: Declaration    = apply("weak")
    def medium: Declaration  = apply("medium")
    def strong: Declaration  = apply("strong")
    def xStrong: Declaration = apply("x-strong")
    def s: Declaration       = apply("s")
    def ms: Declaration      = apply("ms")

  /** `rest-before` — `<time [0s,∞]> | none | x-weak | weak | medium | strong | x-strong`. Initial: `none`. Inherited:
    * no.
    */
  object restBefore extends DS("rest-before") with None with Timeish:
    def xWeak: Declaration   = apply("x-weak")
    def weak: Declaration    = apply("weak")
    def medium: Declaration  = apply("medium")
    def strong: Declaration  = apply("strong")
    def xStrong: Declaration = apply("x-strong")
    def s: Declaration       = apply("s")
    def ms: Declaration      = apply("ms")

  /** `rest-after` — `<time [0s,∞]> | none | x-weak | weak | medium | strong | x-strong`. Initial: `none`. Inherited:
    * no.
    */
  object restAfter extends DS("rest-after") with None with Timeish:
    def xWeak: Declaration   = apply("x-weak")
    def weak: Declaration    = apply("weak")
    def medium: Declaration  = apply("medium")
    def strong: Declaration  = apply("strong")
    def xStrong: Declaration = apply("x-strong")
    def s: Declaration       = apply("s")
    def ms: Declaration      = apply("ms")

  /** `rest` — `<'rest-before'> <'rest-after'>?`. Initial: `see individual properties`. Inherited: no. */
  object rest extends DS("rest") with None with Timeish:
    def xWeak: Declaration   = apply("x-weak")
    def weak: Declaration    = apply("weak")
    def medium: Declaration  = apply("medium")
    def strong: Declaration  = apply("strong")
    def xStrong: Declaration = apply("x-strong")
    def s: Declaration       = apply("s")
    def ms: Declaration      = apply("ms")

  /** `cue-before` — `<url> <decibel>? | none`. Initial: `none`. Inherited: no. */
  object cueBefore extends DS("cue-before") with None

  /** `cue-after` — `<url> <decibel>? | none`. Initial: `none`. Inherited: no. */
  object cueAfter extends DS("cue-after") with None

  /** `cue` — `<'cue-before'> <'cue-after'>?`. Initial: `see individual properties`. Inherited: no. */
  object cue extends DS("cue") with None

  /** `voice-family` — `[ <voice-family-name> | <generic-voice> ]# | preserve`. Initial: `implementation-dependent`.
    * Inherited: yes.
    */
  object voiceFamily extends DS("voice-family") with Numeric:
    def preserve: Declaration = apply("preserve")
    def child: Declaration    = apply("child")
    def young: Declaration    = apply("young")
    def old: Declaration      = apply("old")
    def male: Declaration     = apply("male")
    def female: Declaration   = apply("female")
    def neutral: Declaration  = apply("neutral")

  /** `voice-rate` — `[ normal | x-slow | slow | medium | fast | x-fast ] || <percentage [0,∞]>`. Initial: `normal`.
    * Inherited: yes.
    */
  object voiceRate extends DS("voice-rate") with Percent with Normal:
    def xSlow: Declaration  = apply("x-slow")
    def slow: Declaration   = apply("slow")
    def medium: Declaration = apply("medium")
    def fast: Declaration   = apply("fast")
    def xFast: Declaration  = apply("x-fast")

  /** `voice-pitch` —
    * `<frequency [0Hz,∞]> && absolute | [ [ x-low | low | medium | high | x-high ] || [ <frequency [0Hz,∞]> | <semitones> | <percentage> ] ]`.
    * Initial: `medium`. Inherited: yes.
    */
  object voicePitch extends DS("voice-pitch") with Percent with Frequency:
    def absolute: Declaration = apply("absolute")
    def xLow: Declaration     = apply("x-low")
    def low: Declaration      = apply("low")
    def medium: Declaration   = apply("medium")
    def high: Declaration     = apply("high")
    def xHigh: Declaration    = apply("x-high")

  /** `voice-range` —
    * `<frequency [0Hz,∞]> && absolute | [ [ x-low | low | medium | high | x-high ] || [ <frequency [0Hz,∞]> | <semitones> | <percentage> ] ]`.
    * Initial: `medium`. Inherited: yes.
    */
  object voiceRange extends DS("voice-range") with Percent with Frequency:
    def absolute: Declaration = apply("absolute")
    def xLow: Declaration     = apply("x-low")
    def low: Declaration      = apply("low")
    def medium: Declaration   = apply("medium")
    def high: Declaration     = apply("high")
    def xHigh: Declaration    = apply("x-high")

  /** `voice-stress` — `normal | strong | moderate | none | reduced`. Initial: `normal`. Inherited: yes. */
  object voiceStress extends DS("voice-stress") with None with Normal:
    def strong: Declaration   = apply("strong")
    def moderate: Declaration = apply("moderate")
    def reduced: Declaration  = apply("reduced")

  /** `voice-duration` — `auto | <time [0s,∞]>`. Initial: `auto`. Inherited: no. */
  object voiceDuration extends DS("voice-duration") with Auto with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `table-layout` — `auto | fixed`. Initial: `auto`. Inherited: no. */
  object tableLayout extends DS("table-layout") with Auto:
    def fixed: Declaration = apply("fixed")

  /** `border-collapse` — `separate | collapse`. Initial: `separate`. Inherited: yes. */
  object borderCollapse extends DS("border-collapse"):
    def separate: Declaration = apply("separate")
    def collapse: Declaration = apply("collapse")

  /** `border-spacing` — `<length [0,∞]>{1,2}`. Initial: `0px 0px`. Inherited: yes. */
  object borderSpacing extends DS("border-spacing") with Length with LengthBox:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end borderSpacing

  /** `caption-side` — `top | bottom`. Initial: `top`. Inherited: yes. */
  object captionSide extends DS("caption-side"):
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")

  /** `empty-cells` — `show | hide`. Initial: `show`. Inherited: yes. */
  object emptyCells extends DS("empty-cells"):
    def show: Declaration = apply("show")
    def hide: Declaration = apply("hide")

  /** `text-transform` — `none | [capitalize | uppercase | lowercase ] || full-width || full-size-kana`. Initial:
    * `none`. Inherited: yes.
    */
  object textTransform extends DS("text-transform") with None:
    def capitalize: Declaration   = apply("capitalize")
    def uppercase: Declaration    = apply("uppercase")
    def lowercase: Declaration    = apply("lowercase")
    def fullWidth: Declaration    = apply("full-width")
    def fullSizeKana: Declaration = apply("full-size-kana")

  /** `word-space-transform` — `none | [ space | ideographic-space ] && auto-phrase?`. Initial: `none`. Inherited: yes.
    */
  object wordSpaceTransform extends DS("word-space-transform") with None:
    def space: Declaration            = apply("space")
    def ideographicSpace: Declaration = apply("ideographic-space")
    def autoPhrase: Declaration       = apply("auto-phrase")

  /** `white-space` — `normal | pre | nowrap | pre-wrap | break-spaces | pre-line`. Initial: `normal`. Inherited: yes.
    */
  object whiteSpace extends DS("white-space") with Normal:
    def pre: Declaration         = apply("pre")
    def nowrap: Declaration      = apply("nowrap")
    def preWrap: Declaration     = apply("pre-wrap")
    def breakSpaces: Declaration = apply("break-spaces")
    def preLine: Declaration     = apply("pre-line")

  /** `white-space-collapse` — `collapse | discard | preserve | preserve-breaks | preserve-spaces | break-spaces`.
    * Initial: `collapse`. Inherited: yes.
    */
  object whiteSpaceCollapse extends DS("white-space-collapse"):
    def collapse: Declaration       = apply("collapse")
    def discard: Declaration        = apply("discard")
    def preserve: Declaration       = apply("preserve")
    def preserveBreaks: Declaration = apply("preserve-breaks")
    def preserveSpaces: Declaration = apply("preserve-spaces")
    def breakSpaces: Declaration    = apply("break-spaces")

  /** `white-space-trim` — `none | discard-before || discard-after || discard-inner`. Initial: `none`. Inherited: no. */
  object whiteSpaceTrim extends DS("white-space-trim") with None:
    def discardBefore: Declaration = apply("discard-before")
    def discardAfter: Declaration  = apply("discard-after")
    def discardInner: Declaration  = apply("discard-inner")

  /** `tab-size` — `<number [0,∞]> | <length [0,∞]>`. Initial: `8`. Inherited: yes. */
  object tabSize extends DS("tab-size") with Length with Numeric:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end tabSize

  /** `text-wrap-mode` — `wrap | nowrap`. Initial: `wrap`. Inherited: yes. */
  object textWrapMode extends DS("text-wrap-mode"):
    def wrap: Declaration   = apply("wrap")
    def nowrap: Declaration = apply("nowrap")

  /** `wrap-inside` — `auto | avoid`. Initial: `auto`. Inherited: no. */
  object wrapInside extends DS("wrap-inside") with Auto:
    def avoid: Declaration = apply("avoid")

  /** `wrap-before` — `auto | avoid | avoid-line | avoid-flex | line | flex`. Initial: `auto`. Inherited: no. */
  object wrapBefore extends DS("wrap-before") with Auto:
    def avoid: Declaration     = apply("avoid")
    def avoidLine: Declaration = apply("avoid-line")
    def avoidFlex: Declaration = apply("avoid-flex")
    def line: Declaration      = apply("line")
    def flex: Declaration      = apply("flex")

  /** `wrap-after` — `auto | avoid | avoid-line | avoid-flex | line | flex`. Initial: `auto`. Inherited: no. */
  object wrapAfter extends DS("wrap-after") with Auto:
    def avoid: Declaration     = apply("avoid")
    def avoidLine: Declaration = apply("avoid-line")
    def avoidFlex: Declaration = apply("avoid-flex")
    def line: Declaration      = apply("line")
    def flex: Declaration      = apply("flex")

  /** `text-wrap-style` — `auto | balance | stable | pretty | avoid-orphans`. Initial: `auto`. Inherited: yes. */
  object textWrapStyle extends DS("text-wrap-style") with Auto:
    def balance: Declaration      = apply("balance")
    def stable: Declaration       = apply("stable")
    def pretty: Declaration       = apply("pretty")
    def avoidOrphans: Declaration = apply("avoid-orphans")

  /** `text-wrap` — `<'text-wrap-mode'> || <'text-wrap-style'>`. Initial: `wrap`. Inherited: see individual properties.
    */
  object textWrap extends DS("text-wrap") with Auto:
    def wrap: Declaration         = apply("wrap")
    def nowrap: Declaration       = apply("nowrap")
    def balance: Declaration      = apply("balance")
    def stable: Declaration       = apply("stable")
    def pretty: Declaration       = apply("pretty")
    def avoidOrphans: Declaration = apply("avoid-orphans")

  /** `word-break` — `normal | keep-all | break-all | break-word`. Initial: `normal`. Inherited: yes. */
  object wordBreak extends DS("word-break") with Normal:
    def keepAll: Declaration   = apply("keep-all")
    def breakAll: Declaration  = apply("break-all")
    def breakWord: Declaration = apply("break-word")

  /** `line-break` — `auto | loose | normal | strict | anywhere`. Initial: `auto`. Inherited: yes. */
  object lineBreak extends DS("line-break") with Auto with Normal:
    def loose: Declaration    = apply("loose")
    def strict: Declaration   = apply("strict")
    def anywhere: Declaration = apply("anywhere")

  /** `hyphens` — `none | manual | auto`. Initial: `manual`. Inherited: yes. */
  object hyphens extends DS("hyphens") with Auto with None:
    def manual: Declaration = apply("manual")

  /** `hyphenate-character` — `auto | <string>`. Initial: `auto`. Inherited: yes. */
  object hyphenateCharacter extends DS("hyphenate-character") with Auto

  /** `hyphenate-limit-zone` — `<length-percentage>`. Initial: `0`. Inherited: yes. */
  object hyphenateLimitZone extends DS("hyphenate-limit-zone") with Length with Percent

  /** `hyphenate-limit-chars` — `[ auto | <integer> ]{1,3}`. Initial: `auto`. Inherited: yes. */
  object hyphenateLimitChars extends DS("hyphenate-limit-chars") with Auto with Numeric

  /** `hyphenate-limit-lines` — `no-limit | <integer>`. Initial: `no-limit`. Inherited: yes. */
  object hyphenateLimitLines extends DS("hyphenate-limit-lines") with None with Numeric:
    def noLimit: Declaration = apply("no-limit")
    def always: Declaration  = apply("always")
    def column: Declaration  = apply("column")
    def page: Declaration    = apply("page")
    def spread: Declaration  = apply("spread")

  /** `hyphenate-limit-last` — `none | always | column | page | spread`. Initial: `none`. Inherited: yes. */
  object hyphenateLimitLast extends DS("hyphenate-limit-last") with None:
    def always: Declaration = apply("always")
    def column: Declaration = apply("column")
    def page: Declaration   = apply("page")
    def spread: Declaration = apply("spread")

  /** `overflow-wrap` — `normal | break-word | anywhere`. Initial: `normal`. Inherited: yes. */
  object overflowWrap extends DS("overflow-wrap") with Normal:
    def breakWord: Declaration = apply("break-word")
    def anywhere: Declaration  = apply("anywhere")

  /** `word-wrap` — `normal | break-word | anywhere`. Initial: `normal`. Inherited: yes. */
  object wordWrap extends DS("word-wrap") with Normal:
    def breakWord: Declaration = apply("break-word")
    def anywhere: Declaration  = apply("anywhere")

  /** `text-align` — `start | end | left | right | center | justify | match-parent | justify-all`. Initial: `start`.
    * Inherited: yes.
    */
  object textAlign extends DS("text-align"):
    def start: Declaration       = apply("start")
    def end: Declaration         = apply("end")
    def left: Declaration        = apply("left")
    def right: Declaration       = apply("right")
    def center: Declaration      = apply("center")
    def justify: Declaration     = apply("justify")
    def matchParent: Declaration = apply("match-parent")
    def justifyAll: Declaration  = apply("justify-all")

  /** `text-align-all` — `start | end | left | right | center | justify | match-parent`. Initial: `start`. Inherited:
    * yes.
    */
  object textAlignAll extends DS("text-align-all"):
    def start: Declaration       = apply("start")
    def end: Declaration         = apply("end")
    def left: Declaration        = apply("left")
    def right: Declaration       = apply("right")
    def center: Declaration      = apply("center")
    def justify: Declaration     = apply("justify")
    def matchParent: Declaration = apply("match-parent")

  /** `text-align-last` — `auto | start | end | left | right | center | justify | match-parent`. Initial: `auto`.
    * Inherited: yes.
    */
  object textAlignLast extends DS("text-align-last") with Auto:
    def start: Declaration       = apply("start")
    def end: Declaration         = apply("end")
    def left: Declaration        = apply("left")
    def right: Declaration       = apply("right")
    def center: Declaration      = apply("center")
    def justify: Declaration     = apply("justify")
    def matchParent: Declaration = apply("match-parent")

  /** `text-justify` — `auto | none | inter-word | inter-character`. Initial: `auto`. Inherited: yes. */
  object textJustify extends DS("text-justify") with Auto with None:
    def interWord: Declaration      = apply("inter-word")
    def interCharacter: Declaration = apply("inter-character")
    def distribute: Declaration     = apply("distribute")

  /** `text-group-align` — `none | start | end | left | right | center`. Initial: `none`. Inherited: no. */
  object textGroupAlign extends DS("text-group-align") with None:
    def start: Declaration  = apply("start")
    def end: Declaration    = apply("end")
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")
    def center: Declaration = apply("center")

  /** `text-fit` — `[ none | grow | shrink ] [consistent | per-line | per-line-all]? <percentage>?`. Initial: `none`.
    * Inherited: yes.
    */
  object textFit extends DS("text-fit") with Percent with None:
    def grow: Declaration       = apply("grow")
    def shrink: Declaration     = apply("shrink")
    def consistent: Declaration = apply("consistent")
    def perLine: Declaration    = apply("per-line")
    def perLineAll: Declaration = apply("per-line-all")

  /** `word-spacing` — `normal | <length>`. Initial: `normal`. Inherited: yes. */
  object wordSpacing extends DS("word-spacing") with Length with Normal:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end wordSpacing

  /** `letter-spacing` — `normal | <length>`. Initial: `normal`. Inherited: yes. */
  object letterSpacing extends DS("letter-spacing") with Length with Normal:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end letterSpacing

  /** `line-padding` — `<length>`. Initial: `0`. Inherited: yes. */
  object linePadding extends DS("line-padding") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end linePadding

  /** `text-autospace` — `normal | <autospace> | auto`. Initial: `normal`. Inherited: yes. */
  object textAutospace extends DS("text-autospace") with Auto with Normal with Autospace

  /** `text-spacing-trim` — `<spacing-trim> | auto`. Initial: `normal`. Inherited: yes. */
  object textSpacingTrim extends DS("text-spacing-trim") with Auto with Normal with SpacingTrim

  /** `text-spacing` — `none | auto | <spacing-trim> || <autospace>`. Initial: `see individual properties`. Inherited:
    * yes.
    */
  object textSpacing extends DS("text-spacing") with Auto with None with Normal with Autospace with SpacingTrim

  /** `text-indent` — `[ <length-percentage> ] && hanging? && each-line?`. Initial: `0`. Inherited: yes. */
  object textIndent extends DS("text-indent") with Length with Percent:
    def hanging: Declaration  = apply("hanging")
    def eachLine: Declaration = apply("each-line")

  /** `hanging-punctuation` — `none | [ first || [ force-end | allow-end ] || last ]`. Initial: `none`. Inherited: yes.
    */
  object hangingPunctuation extends DS("hanging-punctuation") with None:
    def first: Declaration    = apply("first")
    def forceEnd: Declaration = apply("force-end")
    def allowEnd: Declaration = apply("allow-end")
    def last: Declaration     = apply("last")

  /** `text-decoration-line` — `none | [ underline || overline || line-through || blink ]`. Initial: `none`. Inherited:
    * no (but see prose, above).
    */
  object textDecorationLine extends DS("text-decoration-line") with None:
    def underline: Declaration   = apply("underline")
    def overline: Declaration    = apply("overline")
    def lineThrough: Declaration = apply("line-through")
    def blink: Declaration       = apply("blink")

  /** `text-decoration-style` — `solid | double | dotted | dashed | wavy`. Initial: `solid`. Inherited: no. */
  object textDecorationStyle extends DS("text-decoration-style"):
    def solid: Declaration  = apply("solid")
    def double: Declaration = apply("double")
    def dotted: Declaration = apply("dotted")
    def dashed: Declaration = apply("dashed")
    def wavy: Declaration   = apply("wavy")

  /** `text-decoration-color` — `<color>`. Initial: `currentcolor`. Inherited: no. */
  object textDecorationColor extends DS("text-decoration-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end textDecorationColor

  /** `text-decoration-thickness` — `auto | from-font | <length-percentage> | <line-width>`. Initial: `auto`. Inherited:
    * no.
    */
  object textDecorationThickness
      extends DS("text-decoration-thickness")
      with Length
      with Percent
      with Auto
      with LineWidth:
    def fromFont: Declaration = apply("from-font")

  /** `text-decoration` — `<'text-decoration-line'> || <'text-decoration-style'> || <'text-decoration-color'>`. Initial:
    * `see individual properties`. Inherited: see individual properties.
    */
  object textDecoration extends DS("text-decoration") with None with ColorLike:
    def underline: Declaration           = apply("underline")
    def overline: Declaration            = apply("overline")
    def lineThrough: Declaration         = apply("line-through")
    def blink: Declaration               = apply("blink")
    def solid: Declaration               = apply("solid")
    def double: Declaration              = apply("double")
    def dotted: Declaration              = apply("dotted")
    def dashed: Declaration              = apply("dashed")
    def wavy: Declaration                = apply("wavy")
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end textDecoration

  /** `text-underline-position` — `auto | [ under || [ left | right ] ]`. Initial: `auto`. Inherited: yes. */
  object textUnderlinePosition extends DS("text-underline-position") with Auto:
    def under: Declaration = apply("under")
    def left: Declaration  = apply("left")
    def right: Declaration = apply("right")

  /** `text-underline-offset` — `auto | <length-percentage>`. Initial: `auto`. Inherited: yes. */
  object textUnderlineOffset extends DS("text-underline-offset") with Length with Percent with Auto

  /** `text-decoration-inset` — `<length>{1,2} | auto`. Initial: `0`. Inherited: no. */
  object textDecorationInset extends DS("text-decoration-inset") with Length with Auto:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end textDecorationInset

  /** `text-decoration-skip` — `none | auto`. Initial: `See individual properties`. Inherited: yes. */
  object textDecorationSkip extends DS("text-decoration-skip") with Auto with None

  /** `text-decoration-skip-self` —
    * `auto | skip-all | [ skip-underline || skip-overline || skip-line-through ] | no-skip`. Initial: `auto`.
    * Inherited: no.
    */
  object textDecorationSkipSelf extends DS("text-decoration-skip-self") with Auto:
    def skipAll: Declaration         = apply("skip-all")
    def skipUnderline: Declaration   = apply("skip-underline")
    def skipOverline: Declaration    = apply("skip-overline")
    def skipLineThrough: Declaration = apply("skip-line-through")
    def noSkip: Declaration          = apply("no-skip")

  /** `text-decoration-skip-box` — `none | all`. Initial: `none`. Inherited: yes. */
  object textDecorationSkipBox extends DS("text-decoration-skip-box") with None:
    def all: Declaration = apply("all")

  /** `text-decoration-skip-spaces` — `none | all | [ start || end ]`. Initial: `start end`. Inherited: yes. */
  object textDecorationSkipSpaces extends DS("text-decoration-skip-spaces") with None:
    def all: Declaration   = apply("all")
    def start: Declaration = apply("start")
    def end: Declaration   = apply("end")

  /** `text-decoration-skip-ink` — `auto | none | all`. Initial: `auto`. Inherited: yes. */
  object textDecorationSkipInk extends DS("text-decoration-skip-ink") with Auto with None:
    def all: Declaration = apply("all")

  /** `text-emphasis-style` —
    * `none | [ [ filled | open ] || [ dot | circle | double-circle | triangle | sesame ] ] | <string>`. Initial:
    * `none`. Inherited: yes.
    */
  object textEmphasisStyle extends DS("text-emphasis-style") with None:
    def filled: Declaration       = apply("filled")
    def open: Declaration         = apply("open")
    def dot: Declaration          = apply("dot")
    def circle: Declaration       = apply("circle")
    def doubleCircle: Declaration = apply("double-circle")
    def triangle: Declaration     = apply("triangle")
    def sesame: Declaration       = apply("sesame")

  /** `text-emphasis-color` — `<color>`. Initial: `currentcolor`. Inherited: yes. */
  object textEmphasisColor extends DS("text-emphasis-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end textEmphasisColor

  /** `text-emphasis` — `<'text-emphasis-style'> || <'text-emphasis-color'>`. Initial: `see individual properties`.
    * Inherited: see individual properties.
    */
  object textEmphasis extends DS("text-emphasis") with None with ColorLike:
    def filled: Declaration              = apply("filled")
    def open: Declaration                = apply("open")
    def dot: Declaration                 = apply("dot")
    def circle: Declaration              = apply("circle")
    def doubleCircle: Declaration        = apply("double-circle")
    def triangle: Declaration            = apply("triangle")
    def sesame: Declaration              = apply("sesame")
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end textEmphasis

  /** `text-emphasis-position` — `[ over | under ] && [ right | left ]?`. Initial: `over right`. Inherited: yes. */
  object textEmphasisPosition extends DS("text-emphasis-position"):
    def over: Declaration  = apply("over")
    def under: Declaration = apply("under")
    def right: Declaration = apply("right")
    def left: Declaration  = apply("left")

  /** `text-emphasis-skip` — `spaces || punctuation || symbols || narrow`. Initial: `spaces punctuation`. Inherited:
    * yes.
    */
  object textEmphasisSkip extends DS("text-emphasis-skip"):
    def spaces: Declaration      = apply("spaces")
    def punctuation: Declaration = apply("punctuation")
    def symbols: Declaration     = apply("symbols")
    def narrow: Declaration      = apply("narrow")

  /** `text-shadow` — `none | [ <color>? && [ <length>{2} <length [0,∞]>? ] ]#`. Initial: `none`. Inherited: yes. */
  object textShadow extends DS("text-shadow") with Length with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
    def rex: Declaration                 = apply("rex")
    def cap: Declaration                 = apply("cap")
    def rcap: Declaration                = apply("rcap")
    def rch: Declaration                 = apply("rch")
    def ic: Declaration                  = apply("ic")
    def ric: Declaration                 = apply("ric")
    def lh: Declaration                  = apply("lh")
    def rlh: Declaration                 = apply("rlh")
    def svw: Declaration                 = apply("svw")
    def lvw: Declaration                 = apply("lvw")
    def dvw: Declaration                 = apply("dvw")
    def svh: Declaration                 = apply("svh")
    def lvh: Declaration                 = apply("lvh")
    def dvh: Declaration                 = apply("dvh")
    def vi: Declaration                  = apply("vi")
    def svi: Declaration                 = apply("svi")
    def lvi: Declaration                 = apply("lvi")
    def dvi: Declaration                 = apply("dvi")
    def vb: Declaration                  = apply("vb")
    def svb: Declaration                 = apply("svb")
    def lvb: Declaration                 = apply("lvb")
    def dvb: Declaration                 = apply("dvb")
    def svmin: Declaration               = apply("svmin")
    def lvmin: Declaration               = apply("lvmin")
    def dvmin: Declaration               = apply("dvmin")
    def svmax: Declaration               = apply("svmax")
    def lvmax: Declaration               = apply("lvmax")
    def dvmax: Declaration               = apply("dvmax")
    def Q: Declaration                   = apply("Q")
  end textShadow

  /** `translate` — `none | <length-percentage> [ <length-percentage> <length>? ]?`. Initial: `none`. Inherited: no. */
  object translate extends DS("translate") with Length with Percent with None:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end translate

  /** `rotate` — `none | <angle> | [ x | y | z | <number>{3} ] && <angle>`. Initial: `none`. Inherited: no. */
  object rotate extends DS("rotate") with None with Numeric with Angleish:
    def x: Declaration    = apply("x")
    def y: Declaration    = apply("y")
    def z: Declaration    = apply("z")
    def deg: Declaration  = apply("deg")
    def grad: Declaration = apply("grad")
    def rad: Declaration  = apply("rad")
    def turn: Declaration = apply("turn")

  /** `scale` — `none | [ <number> | <percentage> ]{1,3}`. Initial: `none`. Inherited: no. */
  object scale extends DS("scale") with Percent with None with Numeric

  /** `transform-style` — `flat | preserve-3d`. Initial: `flat`. Inherited: no. */
  object transformStyle extends DS("transform-style"):
    def flat: Declaration       = apply("flat")
    def preserve3d: Declaration = apply("preserve-3d")

  /** `perspective` — `none | <length [0,∞]>`. Initial: `none`. Inherited: no. */
  object perspective extends DS("perspective") with Length with None:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end perspective

  /** `perspective-origin` — `<position>`. Initial: `50% 50%`. Inherited: no. */
  object perspectiveOrigin extends DS("perspective-origin") with Length with Percent with Positionish:
    def top: Declaration    = apply("top")
    def right: Declaration  = apply("right")
    def bottom: Declaration = apply("bottom")
    def left: Declaration   = apply("left")

  /** `backface-visibility` — `visible | hidden`. Initial: `visible`. Inherited: no. */
  object backfaceVisibility extends DS("backface-visibility"):
    def visible: Declaration = apply("visible")
    def hidden: Declaration  = apply("hidden")

  /** `transform` — `none | <transform-list>`. Initial: `none`. Inherited: no. */
  object transform extends DS("transform") with None with Transformish

  /** `transform-origin` — `[ left | center | right | top | bottom | <length-percentage> ] | [ left | center | right |
    * <length-percentage> ] [ top | center | bottom | <length-percentage> ] <length>? | [ [ center | left | right ] && [
    * center | top
    */
  object transformOrigin extends DS("transform-origin") with Length with Percent:
    def left: Declaration   = apply("left")
    def center: Declaration = apply("center")
    def right: Declaration  = apply("right")
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")
    def rex: Declaration    = apply("rex")
    def cap: Declaration    = apply("cap")
    def rcap: Declaration   = apply("rcap")
    def rch: Declaration    = apply("rch")
    def ic: Declaration     = apply("ic")
    def ric: Declaration    = apply("ric")
    def lh: Declaration     = apply("lh")
    def rlh: Declaration    = apply("rlh")
    def svw: Declaration    = apply("svw")
    def lvw: Declaration    = apply("lvw")
    def dvw: Declaration    = apply("dvw")
    def svh: Declaration    = apply("svh")
    def lvh: Declaration    = apply("lvh")
    def dvh: Declaration    = apply("dvh")
    def vi: Declaration     = apply("vi")
    def svi: Declaration    = apply("svi")
    def lvi: Declaration    = apply("lvi")
    def dvi: Declaration    = apply("dvi")
    def vb: Declaration     = apply("vb")
    def svb: Declaration    = apply("svb")
    def lvb: Declaration    = apply("lvb")
    def dvb: Declaration    = apply("dvb")
    def svmin: Declaration  = apply("svmin")
    def lvmin: Declaration  = apply("lvmin")
    def dvmin: Declaration  = apply("dvmin")
    def svmax: Declaration  = apply("svmax")
    def lvmax: Declaration  = apply("lvmax")
    def dvmax: Declaration  = apply("dvmax")
    def Q: Declaration      = apply("Q")
  end transformOrigin

  /** `transform-box` — `content-box | border-box | fill-box | stroke-box | view-box`. Initial: `view-box`. Inherited:
    * no.
    */
  object transformBox extends DS("transform-box"):
    def contentBox: Declaration = apply("content-box")
    def borderBox: Declaration  = apply("border-box")
    def fillBox: Declaration    = apply("fill-box")
    def strokeBox: Declaration  = apply("stroke-box")
    def viewBox: Declaration    = apply("view-box")

  /** `transition-behavior` — `<transition-behavior-value>#`. Initial: `normal`. Inherited: no. */
  object transitionBehavior extends DS("transition-behavior") with Normal with TransitionBehaviorValue

  /** `transition-property` — `none | <single-transition-property>#`. Initial: `all`. Inherited: no. */
  object transitionProperty extends DS("transition-property") with None with SingleTransitionProperty

  /** `transition-duration` — `<time [0s,∞]>#`. Initial: `0s`. Inherited: no. */
  object transitionDuration extends DS("transition-duration") with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `transition-timing-function` — `<easing-function>#`. Initial: `ease`. Inherited: no. */
  object transitionTimingFunction extends DS("transition-timing-function") with TimingFunctionish

  /** `transition-delay` — `<time>#`. Initial: `0s`. Inherited: no. */
  object transitionDelay extends DS("transition-delay") with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `transition` — `<single-transition>#`. Initial: `see individual properties`. Inherited: no. */
  object transition extends DS("transition") with None with Timeish with TimingFunctionish with Transitionish

  /** `outline` — `<'outline-width'> || <'outline-style'> || <'outline-color'>`. Initial: `see individual properties`.
    * Inherited: no.
    */
  object outline
      extends DS("outline")
      with Length
      with Auto
      with None
      with Borderish
      with LineWidth
      with OutlineLineStyle

  /** `outline-width` — `<line-width>`. Initial: `medium`. Inherited: no. */
  object outlineWidth extends DS("outline-width") with Length with LineWidth

  /** `outline-style` — `auto | <outline-line-style>`. Initial: `none`. Inherited: no. */
  object outlineStyle extends DS("outline-style") with Auto with None with OutlineLineStyle

  /** `outline-color` — `auto | <'border-top-color'>`. Initial: `auto`. Inherited: no. */
  object outlineColor extends DS("outline-color") with Auto with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end outlineColor

  /** `outline-offset` — `<length>`. Initial: `0`. Inherited: no. */
  object outlineOffset extends DS("outline-offset") with Length:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end outlineOffset

  /** `resize` — `none | both | horizontal | vertical | block | inline`. Initial: `none`. Inherited: no. */
  object resize extends DS("resize") with None:
    def both: Declaration       = apply("both")
    def horizontal: Declaration = apply("horizontal")
    def vertical: Declaration   = apply("vertical")
    def block: Declaration      = apply("block")
    def inline: Declaration     = apply("inline")

  /** `cursor` — `[<cursor-image>,]* <cursor-predefined>`. Initial: `auto`. Inherited: yes. */
  // could not parse: Expected root:1:1 / alternation:1:1 / anyOrder:1:1 / allInOrder:1:1 / juxtaposed:1:1 / postfix:1:1 / atom:1:1 / group:1:1 / (multiplier | [ \t\r\n] | postfix |
  object cursor extends DS("cursor") with Auto with None:
    def default: Declaration      = apply("default")
    def contextMenu: Declaration  = apply("context-menu")
    def help: Declaration         = apply("help")
    def pointer: Declaration      = apply("pointer")
    def progress: Declaration     = apply("progress")
    def waitCss: Declaration      = apply("wait")
    def cell: Declaration         = apply("cell")
    def crosshair: Declaration    = apply("crosshair")
    def text: Declaration         = apply("text")
    def verticalText: Declaration = apply("vertical-text")
    def alias: Declaration        = apply("alias")
    def copy: Declaration         = apply("copy")
    def move: Declaration         = apply("move")
    def noDrop: Declaration       = apply("no-drop")
    def notAllowed: Declaration   = apply("not-allowed")
    def grab: Declaration         = apply("grab")
    def grabbing: Declaration     = apply("grabbing")
    def eResize: Declaration      = apply("e-resize")
    def nResize: Declaration      = apply("n-resize")
    def neResize: Declaration     = apply("ne-resize")
    def nwResize: Declaration     = apply("nw-resize")
    def sResize: Declaration      = apply("s-resize")
    def seResize: Declaration     = apply("se-resize")
    def swResize: Declaration     = apply("sw-resize")
    def wResize: Declaration      = apply("w-resize")
    def ewResize: Declaration     = apply("ew-resize")
    def nsResize: Declaration     = apply("ns-resize")
    def neswResize: Declaration   = apply("nesw-resize")
    def nwseResize: Declaration   = apply("nwse-resize")
    def colResize: Declaration    = apply("col-resize")
    def rowResize: Declaration    = apply("row-resize")
    def allScroll: Declaration    = apply("all-scroll")
    def zoomIn: Declaration       = apply("zoom-in")
    def zoomOut: Declaration      = apply("zoom-out")
  end cursor

  /** `caret-color` — `auto | <color> [auto | <color>]?`. Initial: `auto`. Inherited: yes. */
  object caretColor extends DS("caret-color") with Auto with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end caretColor

  /** `caret-animation` — `auto | manual`. Initial: `auto`. Inherited: yes. */
  object caretAnimation extends DS("caret-animation") with Auto:
    def manual: Declaration = apply("manual")

  /** `caret-shape` — `auto | bar | block | underscore`. Initial: `auto`. Inherited: yes. */
  object caretShape extends DS("caret-shape") with Auto:
    def bar: Declaration        = apply("bar")
    def block: Declaration      = apply("block")
    def underscore: Declaration = apply("underscore")

  /** `caret` — `<'caret-color'> || <'caret-animation'> || <'caret-shape'>`. Initial: `auto`. Inherited: yes. */
  object caret extends DS("caret") with Auto with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
    def manual: Declaration              = apply("manual")
    def bar: Declaration                 = apply("bar")
    def block: Declaration               = apply("block")
    def underscore: Declaration          = apply("underscore")
  end caret

  /** `nav-up` — `auto | <id> [ current | root | <target-name> ]?`. Initial: `auto`. Inherited: no. */
  object navUp extends DS("nav-up") with Auto:
    def current: Declaration = apply("current")
    def root: Declaration    = apply("root")

  /** `nav-right` — `auto | <id> [ current | root | <target-name> ]?`. Initial: `auto`. Inherited: no. */
  object navRight extends DS("nav-right") with Auto:
    def current: Declaration = apply("current")
    def root: Declaration    = apply("root")

  /** `nav-down` — `auto | <id> [ current | root | <target-name> ]?`. Initial: `auto`. Inherited: no. */
  object navDown extends DS("nav-down") with Auto:
    def current: Declaration = apply("current")
    def root: Declaration    = apply("root")

  /** `nav-left` — `auto | <id> [ current | root | <target-name> ]?`. Initial: `auto`. Inherited: no. */
  object navLeft extends DS("nav-left") with Auto:
    def current: Declaration = apply("current")
    def root: Declaration    = apply("root")

  /** `user-select` — `auto | text | none | contain | all`. Initial: `auto`. Inherited: no. */
  object userSelect extends DS("user-select") with Auto with None:
    def text: Declaration    = apply("text")
    def contain: Declaration = apply("contain")
    def all: Declaration     = apply("all")

  /** `interactivity` — `auto | inert`. Initial: `auto`. Inherited: yes. */
  object interactivity extends DS("interactivity") with Auto:
    def inert: Declaration = apply("inert")

  /** `interest-delay-start` — `normal | <time>`. Initial: `normal`. Inherited: yes. */
  object interestDelayStart extends DS("interest-delay-start") with Normal with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `interest-delay-end` — `normal | <time>`. Initial: `normal`. Inherited: yes. */
  object interestDelayEnd extends DS("interest-delay-end") with Normal with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `interest-delay` — `<'interest-delay-start'>{1,2}`. Initial: `see individual properties`. Inherited: see
    * individual properties.
    */
  object interestDelay extends DS("interest-delay") with Normal with Timeish:
    def s: Declaration  = apply("s")
    def ms: Declaration = apply("ms")

  /** `accent-color` — `auto | <color>`. Initial: `auto`. Inherited: yes. */
  object accentColor extends DS("accent-color") with Auto with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end accentColor

  /** `window-drag` — `none | move`. Initial: `none`. Inherited: yes. */
  object windowDrag extends DS("window-drag") with None:
    def move: Declaration = apply("move")

  object WebkitAppearance extends DS("-webkit-appearance")

  /** `-webkit-user-select` — `auto | text | none | contain | all`. */
  object WebkitUserSelect extends DS("-webkit-user-select") with Auto with None:
    def text: Declaration    = apply("text")
    def contain: Declaration = apply("contain")
    def all: Declaration     = apply("all")

  /** `interpolate-size` — `numeric-only | allow-keywords`. Initial: `numeric-only`. Inherited: yes. */
  object interpolateSize extends DS("interpolate-size"):
    def numericOnly: Declaration   = apply("numeric-only")
    def allowKeywords: Declaration = apply("allow-keywords")

  /** `view-transition-name` — `none | <custom-ident>`. Initial: `none`. Inherited: no. */
  object viewTransitionName extends DS("view-transition-name") with None

  /** `view-transition-scope` — `none | all`. Initial: `none`. Inherited: no. */
  object viewTransitionScope extends DS("view-transition-scope") with None:
    def all: Declaration = apply("all")

  /** `view-transition-group` — `normal | contain | nearest | <custom-ident>`. Initial: `normal`. Inherited: no. */
  object viewTransitionGroup extends DS("view-transition-group") with Normal:
    def contain: Declaration = apply("contain")
    def nearest: Declaration = apply("nearest")

  /** `view-transition-class` — `none | <custom-ident>+`. Initial: `none`. Inherited: no. */
  object viewTransitionClass extends DS("view-transition-class") with None

  /** `zoom` — `<number [0,∞]> | <percentage [0,∞]>`. Initial: `1`. Inherited: no. */
  object zoom extends DS("zoom") with Percent with Numeric

  /** `will-change` — `auto | <animateable-feature>#`. Initial: `auto`. Inherited: no. */
  object willChange extends DS("will-change") with Auto with AnimateableFeature

  /** `direction` — `ltr | rtl`. Initial: `ltr`. Inherited: yes. */
  object direction extends DS("direction"):
    def ltr: Declaration = apply("ltr")
    def rtl: Declaration = apply("rtl")

  /** `unicode-bidi` — `normal | embed | isolate | bidi-override | isolate-override | plaintext`. Initial: `normal`.
    * Inherited: no.
    */
  object unicodeBidi extends DS("unicode-bidi") with Normal:
    def embed: Declaration           = apply("embed")
    def isolate: Declaration         = apply("isolate")
    def bidiOverride: Declaration    = apply("bidi-override")
    def isolateOverride: Declaration = apply("isolate-override")
    def plaintext: Declaration       = apply("plaintext")

  /** `writing-mode` — `horizontal-tb | vertical-rl | vertical-lr | sideways-rl | sideways-lr`. Initial:
    * `horizontal-tb`. Inherited: yes.
    */
  object writingMode extends DS("writing-mode"):
    def horizontalTb: Declaration = apply("horizontal-tb")
    def verticalRl: Declaration   = apply("vertical-rl")
    def verticalLr: Declaration   = apply("vertical-lr")
    def sidewaysRl: Declaration   = apply("sideways-rl")
    def sidewaysLr: Declaration   = apply("sideways-lr")

  /** `text-orientation` — `mixed | upright | sideways`. Initial: `mixed`. Inherited: yes. */
  object textOrientation extends DS("text-orientation"):
    def mixed: Declaration         = apply("mixed")
    def upright: Declaration       = apply("upright")
    def sideways: Declaration      = apply("sideways")
    def sidewaysRight: Declaration = apply("sideways-right")

  /** `glyph-orientation-vertical` — `auto | 0deg | 90deg | 0 | 90`. Initial: `n/a`. Inherited: n/a. */
  // could not parse: Expected root:1:1 / ("&&" | "||" | "|" ~ ws ~ anyOrder | end-of-input):1:6, found "| 0deg | 9"
  object glyphOrientationVertical extends DS("glyph-orientation-vertical")

  /** `text-combine-upright` — `none | all | [ digits <integer [2,4]>? ]`. Initial: `none`. Inherited: yes. */
  object textCombineUpright extends DS("text-combine-upright") with None with Numeric:
    def all: Declaration    = apply("all")
    def digits: Declaration = apply("digits")

  /** `fill-rule` — `nonzero | evenodd`. Initial: `nonzero`. Inherited: yes. */
  object fillRule extends DS("fill-rule"):
    def nonzero: Declaration = apply("nonzero")
    def evenodd: Declaration = apply("evenodd")

  /** `fill-break` — `bounding-box | slice | clone`. Initial: `bounding-box`. Inherited: yes?. */
  object fillBreak extends DS("fill-break"):
    def boundingBox: Declaration = apply("bounding-box")
    def slice: Declaration       = apply("slice")
    def cloneCss: Declaration    = apply("clone")

  /** `fill-color` — `<color>`. Initial: `currentcolor`. Inherited: yes. */
  object fillColor extends DS("fill-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end fillColor

  /** `fill-image` — `<paint>#`. Initial: `none`. Inherited: yes. */
  object fillImage extends DS("fill-image") with None with Imageish

  /** `fill-origin` — `match-parent | fill-box | stroke-box | content-box | padding-box | border-box`. Initial:
    * `match-parent`. Inherited: no.
    */
  object fillOrigin extends DS("fill-origin"):
    def matchParent: Declaration = apply("match-parent")
    def fillBox: Declaration     = apply("fill-box")
    def strokeBox: Declaration   = apply("stroke-box")
    def contentBox: Declaration  = apply("content-box")
    def paddingBox: Declaration  = apply("padding-box")
    def borderBox: Declaration   = apply("border-box")

  /** `fill-position` — `<position>#`. Initial: `0% 0%`. Inherited: yes. */
  object fillPosition extends DS("fill-position") with Length with Percent with Positionish:
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")

  /** `fill-size` — `<bg-size>#`. Initial: `auto`. Inherited: yes. */
  object fillSize extends DS("fill-size") with Length with Percent with Auto with BgSize

  /** `fill-repeat` — `<repeat-style>#`. Initial: `repeat`. Inherited: yes. */
  object fillRepeat extends DS("fill-repeat") with RepeatStyle

  /** `fill-opacity` — `<'opacity'>`. Initial: `1`. Inherited: yes. */
  object fillOpacity extends DS("fill-opacity") with Numeric

  /** `stroke-width` — `[ <length-percentage> | <line-width> | <number> ]#`. Initial: `1px`. Inherited: yes. */
  object strokeWidth extends DS("stroke-width") with Length with Percent with Numeric with LineWidth

  /** `stroke-align` — `center | inset | outset`. Initial: `center`. Inherited: yes. */
  object strokeAlign extends DS("stroke-align"):
    def center: Declaration = apply("center")
    def inset: Declaration  = apply("inset")
    def outset: Declaration = apply("outset")

  /** `stroke-linecap` — `butt | round | square`. Initial: `butt`. Inherited: yes. */
  object strokeLinecap extends DS("stroke-linecap"):
    def butt: Declaration   = apply("butt")
    def round: Declaration  = apply("round")
    def square: Declaration = apply("square")

  /** `stroke-linejoin` — `[ crop | arcs | miter ] || [ bevel | round | fallback ]`. Initial: `miter`. Inherited: yes.
    */
  object strokeLinejoin extends DS("stroke-linejoin"):
    def crop: Declaration     = apply("crop")
    def arcs: Declaration     = apply("arcs")
    def miter: Declaration    = apply("miter")
    def bevel: Declaration    = apply("bevel")
    def round: Declaration    = apply("round")
    def fallback: Declaration = apply("fallback")

  /** `stroke-miterlimit` — `<number>`. Initial: `4`. Inherited: yes. */
  object strokeMiterlimit extends DS("stroke-miterlimit") with Numeric

  /** `stroke-break` — `bounding-box | slice | clone`. Initial: `bounding-box`. Inherited: ?. */
  object strokeBreak extends DS("stroke-break"):
    def boundingBox: Declaration = apply("bounding-box")
    def slice: Declaration       = apply("slice")
    def cloneCss: Declaration    = apply("clone")

  /** `stroke-dasharray` — `none | [<length-percentage> | <number>]+#`. Initial: `none`. Inherited: yes. */
  // could not parse: Expected root:1:1 / ([ \t\r\n] | postfix | "&&" | "||" | "|" | end-of-input):1:41, found "#"
  object strokeDasharray extends DS("stroke-dasharray") with None

  /** `stroke-dashoffset` — `<length-percentage> | <number>`. Initial: `0`. Inherited: yes. */
  object strokeDashoffset extends DS("stroke-dashoffset") with Length with Percent with Numeric

  /** `stroke-dash-corner` — `none | <length>`. Initial: `none`. Inherited: yes. */
  object strokeDashCorner extends DS("stroke-dash-corner") with Length with None:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end strokeDashCorner

  /** `stroke-dash-justify` — `none | [ stretch | compress ] || [ dashes || gaps ]`. Initial: `none`. Inherited: yes. */
  object strokeDashJustify extends DS("stroke-dash-justify") with None:
    def stretch: Declaration  = apply("stretch")
    def compress: Declaration = apply("compress")
    def dashes: Declaration   = apply("dashes")
    def gaps: Declaration     = apply("gaps")

  /** `stroke-color` — `<color>#`. Initial: `transparent`. Inherited: yes. */
  object strokeColor extends DS("stroke-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end strokeColor

  /** `stroke-image` — `<paint>#`. Initial: `none`. Inherited: yes. */
  object strokeImage extends DS("stroke-image") with None with Imageish

  /** `stroke-origin` — `match-parent | fill-box | stroke-box | content-box | padding-box | border-box`. Initial:
    * `match-parent`. Inherited: no.
    */
  object strokeOrigin extends DS("stroke-origin"):
    def matchParent: Declaration = apply("match-parent")
    def fillBox: Declaration     = apply("fill-box")
    def strokeBox: Declaration   = apply("stroke-box")
    def contentBox: Declaration  = apply("content-box")
    def paddingBox: Declaration  = apply("padding-box")
    def borderBox: Declaration   = apply("border-box")

  /** `stroke-position` — `<position>#`. Initial: `0% 0%`. Inherited: yes. */
  object strokePosition extends DS("stroke-position") with Length with Percent with Positionish:
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")

  /** `stroke-size` — `<bg-size>#`. Initial: `auto`. Inherited: yes. */
  object strokeSize extends DS("stroke-size") with Length with Percent with Auto with BgSize

  /** `stroke-repeat` — `<repeat-style>#`. Initial: `repeat`. Inherited: yes. */
  object strokeRepeat extends DS("stroke-repeat") with RepeatStyle

  /** `stroke-opacity` — `<'opacity'>`. Initial: `1`. Inherited: yes. */
  object strokeOpacity extends DS("stroke-opacity") with Numeric

  /** `backdrop-filter` — `none | <filter-value-list>`. Initial: `none`. Inherited: no. */
  object backdropFilter extends DS("backdrop-filter") with None with Filterish

  /** `filter` — `none | <filter-value-list>`. Initial: `none`. Inherited: no. */
  object filter extends DS("filter") with None with Filterish

  /** `flood-color` — `<color>`. Initial: `black`. Inherited: no. */
  object floodColor extends DS("flood-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end floodColor

  /** `flood-opacity` — `<'opacity'>`. Initial: `1`. Inherited: no. */
  object floodOpacity extends DS("flood-opacity") with Numeric

  /** `color-interpolation-filters` — `auto | sRGB | linearRGB`. Initial: `linearRGB`. Inherited: yes. */
  object colorInterpolationFilters extends DS("color-interpolation-filters") with Auto:
    def sRGB: Declaration      = apply("sRGB")
    def linearRGB: Declaration = apply("linearRGB")

  /** `lighting-color` — `<color>`. Initial: `white`. Inherited: no. */
  object lightingColor extends DS("lighting-color") with None with ColorLike:
    def transparent: Declaration         = apply("transparent")
    def currentcolor: Declaration        = apply("currentcolor")
    def ActiveBorder: Declaration        = apply("ActiveBorder")
    def ActiveCaption: Declaration       = apply("ActiveCaption")
    def AppWorkspace: Declaration        = apply("AppWorkspace")
    def Background: Declaration          = apply("Background")
    def ButtonHighlight: Declaration     = apply("ButtonHighlight")
    def ButtonShadow: Declaration        = apply("ButtonShadow")
    def CaptionText: Declaration         = apply("CaptionText")
    def InactiveBorder: Declaration      = apply("InactiveBorder")
    def InactiveCaption: Declaration     = apply("InactiveCaption")
    def InactiveCaptionText: Declaration = apply("InactiveCaptionText")
    def InfoBackground: Declaration      = apply("InfoBackground")
    def InfoText: Declaration            = apply("InfoText")
    def Menu: Declaration                = apply("Menu")
    def MenuText: Declaration            = apply("MenuText")
    def Scrollbar: Declaration           = apply("Scrollbar")
    def ThreeDDarkShadow: Declaration    = apply("ThreeDDarkShadow")
    def ThreeDFace: Declaration          = apply("ThreeDFace")
    def ThreeDHighlight: Declaration     = apply("ThreeDHighlight")
    def ThreeDLightShadow: Declaration   = apply("ThreeDLightShadow")
    def ThreeDShadow: Declaration        = apply("ThreeDShadow")
    def Window: Declaration              = apply("Window")
    def WindowFrame: Declaration         = apply("WindowFrame")
    def WindowText: Declaration          = apply("WindowText")
    def currentColor: Declaration        = apply("currentColor")
  end lightingColor

  /** `math-style` — `normal | compact`. Initial: `normal`. Inherited: yes. */
  object mathStyle extends DS("math-style") with Normal:
    def compact: Declaration = apply("compact")

  /** `math-shift` — `normal | compact`. Initial: `normal`. Inherited: yes. */
  object mathShift extends DS("math-shift") with Normal:
    def compact: Declaration = apply("compact")

  /** `math-depth` — `auto-add | add(<integer>) | <integer>`. Initial: `0`. Inherited: yes. */
  object mathDepth extends DS("math-depth") with Numeric:
    def autoAdd: Declaration = apply("auto-add")

  /** `offset-path` — `none | <offset-path> || <coord-box>`. Initial: `none`. Inherited: no. */
  object offsetPath extends DS("offset-path") with None with BasicShapeish with CoordBox

  /** `offset-distance` — `<length-percentage>`. Initial: `0`. Inherited: no. */
  object offsetDistance extends DS("offset-distance") with Length with Percent

  /** `offset-position` — `normal | auto | <position>`. Initial: `normal`. Inherited: no. */
  object offsetPosition extends DS("offset-position") with Length with Percent with Auto with Normal with Positionish:
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")

  /** `offset-anchor` — `auto | <position>`. Initial: `auto`. Inherited: no. */
  object offsetAnchor extends DS("offset-anchor") with Length with Percent with Auto with Positionish:
    def left: Declaration   = apply("left")
    def right: Declaration  = apply("right")
    def top: Declaration    = apply("top")
    def bottom: Declaration = apply("bottom")

  /** `offset-rotate` — `[ auto | reverse ] || <angle>`. Initial: `auto`. Inherited: no. */
  object offsetRotate extends DS("offset-rotate") with Auto with Angleish:
    def reverse: Declaration = apply("reverse")
    def deg: Declaration     = apply("deg")
    def grad: Declaration    = apply("grad")
    def rad: Declaration     = apply("rad")
    def turn: Declaration    = apply("turn")

  /** `offset` —
    * `[ <'offset-position'>? [ <'offset-path'> [ <'offset-distance'> || <'offset-rotate'> ]? ]? ]! [ / <'offset-anchor'> ]?`.
    * Initial: `see individual properties`. Inherited: see individual properties.
    */
  object offset
      extends DS("offset")
      with Length
      with Percent
      with Auto
      with None
      with Normal
      with Angleish
      with BasicShapeish
      with CoordBox
      with Positionish:
    def left: Declaration    = apply("left")
    def right: Declaration   = apply("right")
    def top: Declaration     = apply("top")
    def bottom: Declaration  = apply("bottom")
    def reverse: Declaration = apply("reverse")
    def deg: Declaration     = apply("deg")
    def grad: Declaration    = apply("grad")
    def rad: Declaration     = apply("rad")
    def turn: Declaration    = apply("turn")
  end offset

  /** `pointer-timeline-name` — `[ none | <dashed-ident> ]#`. Initial: `none`. Inherited: no. */
  object pointerTimelineName extends DS("pointer-timeline-name") with None

  /** `pointer-timeline-axis` — `[ block | inline | x | y ]#`. Initial: `block`. Inherited: no. */
  object pointerTimelineAxis extends DS("pointer-timeline-axis"):
    def block: Declaration   = apply("block")
    def inline: Declaration  = apply("inline")
    def x: Declaration       = apply("x")
    def y: Declaration       = apply("y")
    def self: Declaration    = apply("self")
    def nearest: Declaration = apply("nearest")
    def root: Declaration    = apply("root")

  /** `pointer-timeline` — `[ <'pointer-timeline-name'> <'pointer-timeline-axis'>? ]#`. Initial:
    * `see individual properties`. Inherited: see individual properties.
    */
  object pointerTimeline extends DS("pointer-timeline") with None:
    def block: Declaration   = apply("block")
    def inline: Declaration  = apply("inline")
    def x: Declaration       = apply("x")
    def y: Declaration       = apply("y")
    def self: Declaration    = apply("self")
    def nearest: Declaration = apply("nearest")
    def root: Declaration    = apply("root")

  /** `animation-range-center` —
    * `[ normal | [ <length-percentage> | <timeline-range-center-subject> <length-percentage>? ] ]#`. Initial: `normal`.
    * Inherited: no.
    */
  object animationRangeCenter
      extends DS("animation-range-center")
      with Length
      with Percent
      with Normal
      with TimelineRangeCenterSubject

  /** `scroll-timeline-name` — `[ none | <dashed-ident> ]#`. Initial: `none`. Inherited: no. */
  object scrollTimelineName extends DS("scroll-timeline-name") with None

  /** `scroll-timeline-axis` — `[ block | inline | x | y ]#`. Initial: `block`. Inherited: no. */
  object scrollTimelineAxis extends DS("scroll-timeline-axis"):
    def block: Declaration   = apply("block")
    def inline: Declaration  = apply("inline")
    def x: Declaration       = apply("x")
    def y: Declaration       = apply("y")
    def nearest: Declaration = apply("nearest")
    def root: Declaration    = apply("root")
    def self: Declaration    = apply("self")

  /** `scroll-timeline` — `[ <'scroll-timeline-name'> <'scroll-timeline-axis'>? ]#`. Initial:
    * `see individual properties`. Inherited: no.
    */
  object scrollTimeline extends DS("scroll-timeline") with None:
    def block: Declaration   = apply("block")
    def inline: Declaration  = apply("inline")
    def x: Declaration       = apply("x")
    def y: Declaration       = apply("y")
    def nearest: Declaration = apply("nearest")
    def root: Declaration    = apply("root")
    def self: Declaration    = apply("self")

  /** `view-timeline-name` — `[ none | <dashed-ident> ]#`. Initial: `none`. Inherited: no. */
  object viewTimelineName extends DS("view-timeline-name") with None

  /** `view-timeline-axis` — `[ block | inline | x | y ]#`. Initial: `block`. Inherited: no. */
  object viewTimelineAxis extends DS("view-timeline-axis"):
    def block: Declaration   = apply("block")
    def inline: Declaration  = apply("inline")
    def x: Declaration       = apply("x")
    def y: Declaration       = apply("y")
    def nearest: Declaration = apply("nearest")
    def root: Declaration    = apply("root")
    def self: Declaration    = apply("self")

  /** `view-timeline-inset` — `[ [ auto | <length-percentage> ]{1,2} ]#`. Initial: `auto`. Inherited: no. */
  object viewTimelineInset extends DS("view-timeline-inset") with Length with Percent with Auto

  /** `view-timeline` — `[ <'view-timeline-name'> [ <'view-timeline-axis'> || <'view-timeline-inset'> ]? ]#`. Initial:
    * `see individual properties`. Inherited: see individual properties.
    */
  object viewTimeline extends DS("view-timeline") with Length with Percent with Auto with None:
    def block: Declaration   = apply("block")
    def inline: Declaration  = apply("inline")
    def x: Declaration       = apply("x")
    def y: Declaration       = apply("y")
    def nearest: Declaration = apply("nearest")
    def root: Declaration    = apply("root")
    def self: Declaration    = apply("self")

  /** `animation-range` — `[ <'animation-range-start'> <'animation-range-end'>? ]#`. Initial:
    * `see individual properties`. Inherited: see individual properties.
    */
  object animationRange extends DS("animation-range") with Length with Percent with Normal

  /** `animation-range-start` — `[ normal | <length-percentage> | <timeline-range-name> <length-percentage>? ]#`.
    * Initial: `normal`. Inherited: no.
    */
  object animationRangeStart extends DS("animation-range-start") with Length with Percent with Normal

  /** `animation-range-end` — `[ normal | <length-percentage> | <timeline-range-name> <length-percentage>? ]#`. Initial:
    * `normal`. Inherited: no.
    */
  object animationRangeEnd extends DS("animation-range-end") with Length with Percent with Normal

  /** `timeline-scope` — `none | all | <dashed-ident>#`. Initial: `none`. Inherited: no. */
  object timelineScope extends DS("timeline-scope") with None:
    def all: Declaration = apply("all")

  /** `stroke-alignment` — `center | inner | outer`. Initial: `center`. Inherited: yes. */
  object strokeAlignment extends DS("stroke-alignment"):
    def center: Declaration = apply("center")
    def inner: Declaration  = apply("inner")
    def outer: Declaration  = apply("outer")

  /** `stroke-dashcorner` — `none | <length>`. Initial: `none`. Inherited: yes. */
  object strokeDashcorner_stroke_dashcorner extends DS("stroke-dashcorner") with Length with None:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end strokeDashcorner_stroke_dashcorner

  /** `stroke-dashadjust` — `none | [stretch | compress] [dashes | gaps]?`. Initial: `none`. Inherited: yes. */
  object strokeDashadjust extends DS("stroke-dashadjust") with None:
    def stretch: Declaration  = apply("stretch")
    def compress: Declaration = apply("compress")
    def dashes: Declaration   = apply("dashes")
    def gaps: Declaration     = apply("gaps")
end StylesGenerated
