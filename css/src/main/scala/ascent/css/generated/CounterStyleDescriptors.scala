// AUTO-GENERATED by ascent-domgen. Do not edit by hand — see domgen/README.md to regenerate.

package ascent.css

import StylesFoundation.*

/** Generated `@counter-style` descriptor catalog. The descriptors come from webref's
  * `atrules[@counter-style].descriptors[]`: `system`, `symbols`, `additive-symbols`, `range`, `pad`, `prefix`,
  * `suffix`, `negative`, `fallback`, `speak-as`. None of these collide with general `Styles` properties, so the
  * shared-set filter is a no-op for this at-rule.
  */
object CounterStyleDescriptors extends StylesValueTraits:

  /** `system` —
    * `cyclic | numeric | alphabetic | symbolic | additive | [fixed <integer>?] | [ extends <counter-style-name> ]`.
    * Initial: `symbolic`.
    */
  object system extends DS("system") with Numeric with CounterStyleName:
    def cyclic: Declaration     = apply("cyclic")
    def numeric: Declaration    = apply("numeric")
    def alphabetic: Declaration = apply("alphabetic")
    def symbolic: Declaration   = apply("symbolic")
    def additive: Declaration   = apply("additive")
    def fixed: Declaration      = apply("fixed")
    def `extends`: Declaration  = apply("extends")

  /** `negative` — `<symbol> <symbol>?`. Initial: `"-"`. */
  object negative extends DS("negative") with Imageish

  /** `prefix` — `<symbol>`. Initial: `""`. */
  object prefix extends DS("prefix") with Imageish

  /** `suffix` — `<symbol>`. Initial: `". "`. */
  object suffix extends DS("suffix") with Imageish

  /** `range` — `[ [ <integer> | infinite ]{2} ]# | auto`. Initial: `auto`. */
  object range extends DS("range") with Auto with Numeric:
    def infinite: Declaration = apply("infinite")

  /** `pad` — `<integer [0,∞]> && <symbol>`. Initial: `0 ""`. */
  object pad extends DS("pad") with Numeric with Imageish

  /** `fallback` — `<counter-style-name>`. Initial: `decimal`. */
  object fallback extends DS("fallback") with CounterStyleName

  /** `symbols` — `<symbol>+`. Initial: `n/a`. */
  object symbols extends DS("symbols") with Imageish

  /** `additive-symbols` — `[ <integer [0,∞]> && <symbol> ]#`. Initial: `n/a`. */
  object additiveSymbols extends DS("additive-symbols") with Numeric with Imageish
end CounterStyleDescriptors
