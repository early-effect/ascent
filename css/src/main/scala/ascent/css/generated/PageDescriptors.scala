// AUTO-GENERATED by ascent-domgen. Do not edit by hand — see domgen/README.md to regenerate.

package ascent.css

import StylesFoundation.*

/** Generated `@page` descriptor catalog (paged-media controls — printing, paged overflow). The descriptors come from
  * webref's `atrules[@page].descriptors[]`: `bleed`, `marks`, `page-margin-safety`, `page-orientation`, `size`. General
  * CSS properties used inside `@page` (`margin`, `padding`, …) live in [[Styles]] and are skipped here.
  */
object PageDescriptors extends StylesValueTraits:

  /** `size` — `<length [0,∞]>{1,2} | auto | [ <page-size> || [ portrait | landscape ] ]`. Initial: `auto`. */
  object size extends DS("size") with Length with Auto:
    def portrait: Declaration  = apply("portrait")
    def landscape: Declaration = apply("landscape")
    def rex: Declaration       = apply("rex")
    def cap: Declaration       = apply("cap")
    def rcap: Declaration      = apply("rcap")
    def rch: Declaration       = apply("rch")
    def ic: Declaration        = apply("ic")
    def ric: Declaration       = apply("ric")
    def lh: Declaration        = apply("lh")
    def rlh: Declaration       = apply("rlh")
    def svw: Declaration       = apply("svw")
    def lvw: Declaration       = apply("lvw")
    def dvw: Declaration       = apply("dvw")
    def svh: Declaration       = apply("svh")
    def lvh: Declaration       = apply("lvh")
    def dvh: Declaration       = apply("dvh")
    def vi: Declaration        = apply("vi")
    def svi: Declaration       = apply("svi")
    def lvi: Declaration       = apply("lvi")
    def dvi: Declaration       = apply("dvi")
    def vb: Declaration        = apply("vb")
    def svb: Declaration       = apply("svb")
    def lvb: Declaration       = apply("lvb")
    def dvb: Declaration       = apply("dvb")
    def svmin: Declaration     = apply("svmin")
    def lvmin: Declaration     = apply("lvmin")
    def dvmin: Declaration     = apply("dvmin")
    def svmax: Declaration     = apply("svmax")
    def lvmax: Declaration     = apply("lvmax")
    def dvmax: Declaration     = apply("dvmax")
    def Q: Declaration         = apply("Q")
  end size

  /** `page-orientation` — `upright | rotate-left | rotate-right`. Initial: `upright`. */
  object pageOrientation extends DS("page-orientation"):
    def upright: Declaration     = apply("upright")
    def rotateLeft: Declaration  = apply("rotate-left")
    def rotateRight: Declaration = apply("rotate-right")

  /** `marks` — `none | [ crop || cross ]`. Initial: `none`. */
  object marks extends DS("marks") with None:
    def crop: Declaration  = apply("crop")
    def cross: Declaration = apply("cross")

  /** `bleed` — `auto | <length>`. Initial: `auto`. */
  object bleed extends DS("bleed") with Length with Auto:
    def rex: Declaration   = apply("rex")
    def cap: Declaration   = apply("cap")
    def rcap: Declaration  = apply("rcap")
    def rch: Declaration   = apply("rch")
    def ic: Declaration    = apply("ic")
    def ric: Declaration   = apply("ric")
    def lh: Declaration    = apply("lh")
    def rlh: Declaration   = apply("rlh")
    def svw: Declaration   = apply("svw")
    def lvw: Declaration   = apply("lvw")
    def dvw: Declaration   = apply("dvw")
    def svh: Declaration   = apply("svh")
    def lvh: Declaration   = apply("lvh")
    def dvh: Declaration   = apply("dvh")
    def vi: Declaration    = apply("vi")
    def svi: Declaration   = apply("svi")
    def lvi: Declaration   = apply("lvi")
    def dvi: Declaration   = apply("dvi")
    def vb: Declaration    = apply("vb")
    def svb: Declaration   = apply("svb")
    def lvb: Declaration   = apply("lvb")
    def dvb: Declaration   = apply("dvb")
    def svmin: Declaration = apply("svmin")
    def lvmin: Declaration = apply("lvmin")
    def dvmin: Declaration = apply("dvmin")
    def svmax: Declaration = apply("svmax")
    def lvmax: Declaration = apply("lvmax")
    def dvmax: Declaration = apply("dvmax")
    def Q: Declaration     = apply("Q")
  end bleed

  /** `page-margin-safety` — `none | clamp | add`. Initial: `auto`. */
  object pageMarginSafety extends DS("page-margin-safety") with None:
    def clamp: Declaration = apply("clamp")
    def add: Declaration   = apply("add")
end PageDescriptors
