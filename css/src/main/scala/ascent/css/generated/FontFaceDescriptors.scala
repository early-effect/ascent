// AUTO-GENERATED by ascent-domgen. Do not edit by hand — see domgen/README.md to regenerate.

package ascent.css

import StylesFoundation.*

/** Generated `@font-face` descriptor catalog. The descriptors that are unique to `@font-face` (`src`, `unicode-range`,
  * the `*-override` family, `size-adjust`) — the descriptors that ALSO exist in [[Styles]] are skipped to keep one
  * canonical typed builder per property name.
  */
object FontFaceDescriptors extends StylesValueTraits:

  /** `size-adjust` — `<percentage [0,∞]>`. Initial: `100%`. */
  object sizeAdjust extends DS("size-adjust") with Percent

  /** `ascent-override` — `normal | <percentage [0,∞]>`. Initial: `normal`. */
  object ascentOverride extends DS("ascent-override") with Percent with Normal

  /** `descent-override` — `normal | <percentage [0,∞]>`. Initial: `normal`. */
  object descentOverride extends DS("descent-override") with Percent with Normal

  /** `line-gap-override` — `normal | <percentage [0,∞]>`. Initial: `normal`. */
  object lineGapOverride extends DS("line-gap-override") with Percent with Normal

  /** `superscript-position-override` — `[ normal | from-font | <percentage> ]{1,2}`. Initial: `normal`. */
  object superscriptPositionOverride
      extends DS("superscript-position-override")
      with Percent
      with Normal
      with LengthBox:
    def fromFont: Declaration = apply("from-font")

  /** `subscript-position-override` — `[ normal | from-font | <percentage> ]{1,2}`. Initial: `normal`. */
  object subscriptPositionOverride extends DS("subscript-position-override") with Percent with Normal with LengthBox:
    def fromFont: Declaration = apply("from-font")

  /** `superscript-size-override` — `[ normal | from-font | <percentage [0,∞]> ]{1,2}`. Initial: `normal`. */
  object superscriptSizeOverride extends DS("superscript-size-override") with Percent with Normal with LengthBox:
    def fromFont: Declaration = apply("from-font")

  /** `subscript-size-override` — `[ normal | from-font | <percentage [0,∞]> ]{1,2}`. Initial: `normal`. */
  object subscriptSizeOverride extends DS("subscript-size-override") with Percent with Normal with LengthBox:
    def fromFont: Declaration = apply("from-font")

  /** `font-named-instance` — `auto | <string>`. Initial: `auto`. */
  object fontNamedInstance extends DS("font-named-instance") with Auto

  /** `font-display` — `auto | block | swap | fallback | optional`. Initial: `auto`. */
  object fontDisplay extends DS("font-display") with Auto:
    def block: Declaration    = apply("block")
    def swap: Declaration     = apply("swap")
    def fallback: Declaration = apply("fallback")
    def optional: Declaration = apply("optional")

  /** `src: url("<href>") format("<format>")` — the most distinctive @font-face descriptor. The webref grammar for `src`
    * is `<font-src-list>` (a comma-separated list of `<url> [format(<format-list>)]?` entries), too rich for the
    * analyzer's top-level-alts heuristic to surface. Hand-typed builder pinned here so users get the everyday case
    * without falling through to `apply(String)`.
    */
  object src extends DS("src"):
    /** Single-source builder. Quotes the href and the format per the spec. */
    def url(href: String, format: String): Declaration =
      apply("url(\"" + href + "\") format(\"" + format + "\")")

    /** Single-source url-only builder (no format() call) — useful for system fonts where the format is implied or not
      * relevant.
      */
    def url(href: String): Declaration = apply("url(\"" + href + "\")")

  /** `unicode-range: <range-token>#` — the range string is opaque (the spec grammar is too rich for top-level alts).
    * The bare `apply(String)` form takes a raw range literal: `FontFaceDescriptors.unicodeRange("U+0000-00FF")`.
    */
  object unicodeRange extends DS("unicode-range")
end FontFaceDescriptors
