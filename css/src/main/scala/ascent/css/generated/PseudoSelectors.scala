// AUTO-GENERATED by ascent-domgen. Do not edit by hand — see domgen/README.md to regenerate.

package ascent.css

/** Generated simple (argument-less) pseudo-CLASSES from the webref selectors catalog. Mixed into the hand-written
  * `object PseudoClass`, so `PseudoClass.hover` resolves here while the functional constructors (`nthChild`, `not`, …)
  * live in the companion. Each `val` carries its webref prose as scaladoc.
  */
trait PseudoClassesGenerated:
  /** Build a simple pseudo-class from its bare name (no leading colon). Provided by `object PseudoClass`. */
  protected def simple(name: String): PseudoClass

  /** The :active pseudo-class applies while an element is being “activated” by the user, as defined by the host
    * language; for example, while a hyperlink is being triggered.
    */
  val active: PseudoClass = simple("active")

  /** The :active-view-transition pseudo-class applies to the root element of the document, if it has an active view
    * transition.
    */
  val activeViewTransition: PseudoClass = simple("active-view-transition")

  /** The :animated-image pseudo-class represents content image elements where a animated image has been loaded. For the
    * animated-image pseudo-class to match, the image must not only be in a format that is capable of animation, but
    * must also be a
    */
  val animatedImage: PseudoClass = simple("animated-image")

  /** The :any-link pseudo-class represents an element that acts as the source anchor of a hyperlink. For example, in
    * [HTML5], any a or area elements with an href attribute are hyperlinks, and thus match :any-link. It matches an
    * element if the el
    */
  val anyLink: PseudoClass = simple("any-link")

  /** The :autofill pseudo-class represents input elements that have been automatically filled by the user agent, and
    * have not been subsequently altered by the user.
    */
  val autofill: PseudoClass = simple("autofill")

  /** The :blank pseudo-class applies to user-input elements whose input value is empty (consists of the empty string or
    * otherwise null input).
    */
  val blank: PseudoClass = simple("blank")

  /** The :buffering pseudo-class represents an element that is capable of being “played” or “paused”, when that element
    * cannot continue playing because it is actively attempting to obtain media data but has not yet obtained enough
    * data to resume
    */
  val buffering: PseudoClass = simple("buffering")

  /** When such elements are toggled “on” the :checked pseudo-class applies. For example, [HTML5] defines that checked
    * checkboxes, radio buttons, and selected <option> elements match :checked. Similarly, when such elements are
    * toggled “off”, the
    */
  val checked: PseudoClass = simple("checked")

  /** The :current pseudo-class represents the element, or an ancestor of the element, that is currently being
    * displayed.
    */
  val current: PseudoClass = simple("current")

  /** The :default pseudo-class applies to the one or more UI elements that are the default among a set of similar
    * elements. Typically applies to context menu items, buttons and select lists/menus.
    */
  val default: PseudoClass = simple("default")

  /** In some host languages, elements can have a distinction between being “defined”/“constructed” or not. The :defined
    * pseudo-class matches elements that are fully defined, as dictated by the host language.
    */
  val defined: PseudoClass = simple("defined")

  /** Conversely, the :disabled pseudo-class represents user interface elements that are in a disabled state; such
    * elements must have a corresponding enabled state.
    */
  val disabled: PseudoClass = simple("disabled")

  /** The :empty pseudo-class represents an element that has no children except, optionally, document white space
    * characters. In terms of the document tree, only element nodes and content nodes (such as [DOM] text nodes, and
    * entity references) wh
    */
  val empty: PseudoClass = simple("empty")

  /** The :enabled pseudo-class represents user interface elements that are in an enabled state; such elements must have
    * a corresponding disabled state.
    */
  val enabled: PseudoClass = simple("enabled")

  /** Authors may also specify style for the first page of a document with the :first pseudo-class: */
  val first: PseudoClass = simple("first")

  /** The :first-child pseudo-class represents an element that is first among its inclusive siblings. Same as
    * :nth-child(1).
    */
  val firstChild: PseudoClass = simple("first-child")

  /** Same as :nth-of-page(n), but where n = 1 (it is the first matched element on the page). */
  val firstOfPage: PseudoClass = simple("first-of-page")

  /** The :first-of-type pseudo-class represents the same element as :nth-of-type(1). */
  val firstOfType: PseudoClass = simple("first-of-type")

  /** The :focus pseudo-class applies while an element (or pseudo-element) has the focus (accepts keyboard or other
    * forms of input).
    */
  val focus: PseudoClass = simple("focus")

  /** While the :focus pseudo-class always matches the currently-focused element, UAs only sometimes visibly indicate
    * focus (such as by drawing a “focus ring”), instead using a variety of heuristics to visibly indicate the focus
    * only when it woul
    */
  val focusVisible: PseudoClass = simple("focus-visible")

  /** The :focus-within pseudo-class applies to any element (or pseudo-element) for which the :focus pseudo-class
    * applies, as well as to an element (or pseudo-element) whose descendant in the flat tree (including non-element
    * nodes, such as text n
    */
  val focusWithin: PseudoClass = simple("focus-within")

  /** The :fullscreen pseudo-class represents an element which is displayed in a mode that takes up most (usually all)
    * of the screen, such as that defined by the Fullscreen API. [FULLSCREEN]
    */
  val fullscreen: PseudoClass = simple("fullscreen")

  /** The :future pseudo-class represents any element that is defined to occur entirely after a :current element. For
    * example, the WebVTT spec defines the :future pseudo-class relative to the current playback position of a media
    * element. If a tim
    */
  val future: PseudoClass = simple("future")

  /** The :has-slotted pseudo-class matches slot elements which have a non-empty list of flattened slotted nodes. */
  val hasSlotted: PseudoClass = simple("has-slotted")

  /** The (non-functional) :heading pseudo-class matches an element which has a heading level, with respect to the
    * semantics defined by the document language (e.g. [HTML5]).
    */
  val heading: PseudoClass = simple("heading")

  /** The :high-value pseudo-class matches on a meter element when its value is over the value specified by the high
    * HTML attribute.
    */
  val highValue: PseudoClass = simple("high-value")

  /** The :host pseudo-class, when evaluated in the context of a shadow tree, matches the shadow tree’s shadow host. In
    * any other context, it matches nothing.
    */
  val host: PseudoClass = simple("host")

  /** The :hover pseudo-class applies while the user designates an element (or pseudo-element) with a pointing device,
    * but does not necessarily activate it. For example, a visual user agent could apply this pseudo-class when the
    * cursor (mouse poi
    */
  val hover: PseudoClass = simple("hover")

  /** The :in-range and :out-of-range pseudo-classes apply only to elements that have range limitations. An element is
    * :in-range or :out-of-range when the value that the element is bound to is in range or out of range with respect to
    * its range li
    */
  val inRange: PseudoClass = simple("in-range")

  /** If an element that could match :checked or :unchecked is neither "on" nor "off", the :indeterminate pseudo-class
    * applies. :indeterminate also matches elements which do not have a notion of being "checked", but whose "value" is
    * still in an i
    */
  val indeterminate: PseudoClass = simple("indeterminate")

  /** The :interest-source pseudo-class applies to an interest source element that the user is currently "showing
    * interest" in, and the :interest-target pseudo-class applies to the associated interest target of an element
    * matching :interest-sourc
    */
  val interestSource: PseudoClass = simple("interest-source")

  /** The :interest-source pseudo-class applies to an interest source element that the user is currently "showing
    * interest" in, and the :interest-target pseudo-class applies to the associated interest target of an element
    * matching :interest-sourc
    */
  val interestTarget: PseudoClass = simple("interest-target")

  /** An element is :valid or :invalid when its contents or value is, respectively, valid or invalid with respect to
    * data validity semantics defined by the document language (e.g. [XFORMS11] or [HTML5]). An element which lacks data
    * validity seman
    */
  val invalid: PseudoClass = simple("invalid")

  /** The :last-child pseudo-class represents an element that is last among its inclusive siblings. Same as
    * :nth-last-child(1).
    */
  val lastChild: PseudoClass = simple("last-child")

  /** The element is the last matched element on the page. */
  val lastOfPage: PseudoClass = simple("last-of-page")

  /** The :last-of-type pseudo-class represents the same element as :nth-last-of-type(1). */
  val lastOfType: PseudoClass = simple("last-of-type")

  /** All pages are automatically classified by user agents into either the :left or :right pseudo-class. Whether the
    * first page of a document is :left or :right depends on the major writing direction of the root element. For
    * example, the first p
    */
  val left: PseudoClass = simple("left")

  /** User agents commonly display unvisited hyperlinks differently from previously visited ones. Selectors provides the
    * pseudo-classes :link and :visited to distinguish them:
    */
  val link: PseudoClass = simple("link")

  /** The :local-link pseudo-class allows authors to style hyperlinks based on the users current location within a site
    * and to differentiate site-internal versus site-external links.
    */
  val localLink: PseudoClass = simple("local-link")

  /** The :low-value pseudo-class matches on a meter element when its value is under the value specified by the low HTML
    * attribute.
    */
  val lowValue: PseudoClass = simple("low-value")

  /** The :modal pseudo-class represents an element which is in a state that excludes all interaction with elements
    * outside it until it has been dismissed. Multiple elements can be :modal simultaneously, with only one of them
    * active (able to rece
    */
  val modal: PseudoClass = simple("modal")

  /** The :muted pseudo-class represents an element that is capable of making sound, but is currently “muted“ (forced
    * silent). (For the audio and video elements of HTML, see muted. [HTML])
    */
  val muted: PseudoClass = simple("muted")

  /** The :only-child pseudo-class represents an element that has no siblings. Same as :first-child:last-child or
    * :nth-child(1):nth-last-child(1), but with a lower specificity.
    */
  val onlyChild: PseudoClass = simple("only-child")

  /** The :only-of-type pseudo-class represents the same element as :first-of-type:last-of-type. */
  val onlyOfType: PseudoClass = simple("only-of-type")

  /** The :open pseudo-class represents an element that has both “open” and “closed” states, and which is currently in
    * the “open” state.
    */
  val open: PseudoClass = simple("open")

  /** The :optimal-value pseudo-class matches on a meter element when its value is in the range determined by the
    * optimum / low / high HTML attributes.
    */
  val optimalValue: PseudoClass = simple("optimal-value")

  /** A form element is :required or :optional if a value for it is, respectively, required or optional before the form
    * it belongs to can be validly submitted. Elements that are not form elements are neither required nor optional.
    */
  val optional: PseudoClass = simple("optional")

  /** The :in-range and :out-of-range pseudo-classes apply only to elements that have range limitations. An element is
    * :in-range or :out-of-range when the value that the element is bound to is in range or out of range with respect to
    * its range li
    */
  val outOfRange: PseudoClass = simple("out-of-range")

  /** The :past pseudo-class represents any element that is defined to occur entirely prior to a :current element. For
    * example, the WebVTT spec defines the :past pseudo-class relative to the current playback position of a media
    * element. If a time
    */
  val past: PseudoClass = simple("past")

  /** The :paused pseudo-class represents an element that is capable of being “played” or “paused”, when that element is
    * “paused” (i.e. not ”playing”). (This includes both an explicit “paused” state, and other non-playing states like
    * “loaded, has
    */
  val paused: PseudoClass = simple("paused")

  /** The :picture-in-picture pseudo-class represents an element which is displayed in a mode that takes up most
    * (usually all) of the viewport, and whose viewport is confined to part of the screen while being displayed over
    * other content, for exa
    */
  val pictureInPicture: PseudoClass = simple("picture-in-picture")

  /** Input elements can sometimes show placeholder text as a hint to the user on what to type in. See, for example, the
    * placeholder attribute in [HTML5]. The :placeholder-shown pseudo-class matches an input element that is showing
    * such placehold
    */
  val placeholderShown: PseudoClass = simple("placeholder-shown")

  /** The :playing pseudo-class represents an element that is capable of being “played” or “paused”, when that element
    * is “playing”. (This includes both when the element is explicitly playing, and when it’s temporarily stopped for
    * some reason not
    */
  val playing: PseudoClass = simple("playing")

  /** The :popover-open pseudo-class represents an element that has both “popover-showing” and “popover-hidden” states
    * and which is currently in the “popover-showing” state.
    */
  val popoverOpen: PseudoClass = simple("popover-open")

  /** An element matches :read-write if it is user-alterable, as defined by the document language. Otherwise, it is
    * :read-only.
    */
  val readOnly: PseudoClass = simple("read-only")

  /** An element matches :read-write if it is user-alterable, as defined by the document language. Otherwise, it is
    * :read-only.
    */
  val readWrite: PseudoClass = simple("read-write")

  /** A form element is :required or :optional if a value for it is, respectively, required or optional before the form
    * it belongs to can be validly submitted. Elements that are not form elements are neither required nor optional.
    */
  val required: PseudoClass = simple("required")

  /** All pages are automatically classified by user agents into either the :left or :right pseudo-class. Whether the
    * first page of a document is :left or :right depends on the major writing direction of the root element. For
    * example, the first p
    */
  val right: PseudoClass = simple("right")

  /** The :root pseudo-class represents an element that is the root of the document. */
  val root: PseudoClass = simple("root")

  /** In some contexts, selectors are matched with respect to one or more scoping roots, such as when calling the
    * querySelector() method in [DOM]. The :scope pseudo-class represents this scoping root, and may be either a true
    * element or a virtual
    */
  val scope: PseudoClass = simple("scope")

  /** The :seeking pseudo-class represents an element that is capable of ”seeking” when that element is ”seeking”. (For
    * the audio and video elements of HTML, see HTML § 4.8.11.9 Seeking.)
    */
  val seeking: PseudoClass = simple("seeking")

  /** The :snapped pseudo-class matches any snap targets, regardless of axis. The longform physical and logical
    * pseudo-class selectors allow for more finite snapped children styling as they can target an individual axis.
    */
  val snapped: PseudoClass = simple("snapped")

  /** Matches the child snapped on the block axis. */
  val snappedBlock: PseudoClass = simple("snapped-block")

  /** Matches the child snapped on the inline axis. */
  val snappedInline: PseudoClass = simple("snapped-inline")

  /** Matches the child snapped on the horizontal axis. */
  val snappedX: PseudoClass = simple("snapped-x")

  /** Matches the child snapped on the vertical axis. */
  val snappedY: PseudoClass = simple("snapped-y")

  /** The :stalled pseudo-class represents an element when that element cannot continue playing because it is actively
    * attempting to obtain media data but it has failed to receive any data for some amount of time. For the audio and
    * video elements
    */
  val stalled: PseudoClass = simple("stalled")

  /** The element is the first matched element on the page, and neither it nor its ancestors have any previous siblings
    * that appear on the page.
    */
  val startOfPage: PseudoClass = simple("start-of-page")

  /** The :target pseudo-class matches the document’s target elements. If the document’s URL has no fragment identifier,
    * then the document has no target elements.
    */
  val target: PseudoClass = simple("target")

  /** In addition to the :target-current pseudo-class, this specification introduces the :target-before and
    * :target-after pseudo-classes for use with scroll marker elements.
    */
  val targetAfter: PseudoClass = simple("target-after")

  /** In addition to the :target-current pseudo-class, this specification introduces the :target-before and
    * :target-after pseudo-classes for use with scroll marker elements.
    */
  val targetBefore: PseudoClass = simple("target-before")

  /** Exactly one scroll marker within each scroll marker group is determined to be active at a time. Such "active"
    * scroll markers match the :target-current pseudo-class.
    */
  val targetCurrent: PseudoClass = simple("target-current")

  /** When such elements are toggled “on” the :checked pseudo-class applies. For example, [HTML5] defines that checked
    * checkboxes, radio buttons, and selected <option> elements match :checked. Similarly, when such elements are
    * toggled “off”, the
    */
  val unchecked: PseudoClass = simple("unchecked")

  /** The :user-invalid and the :user-valid pseudo-classes represent an element with incorrect or correct input,
    * respectively, but only after the user has significantly interacted with it. Their purpose is to help the user
    * identify mistakes in th
    */
  val userInvalid: PseudoClass = simple("user-invalid")

  /** The :user-invalid and the :user-valid pseudo-classes represent an element with incorrect or correct input,
    * respectively, but only after the user has significantly interacted with it. Their purpose is to help the user
    * identify mistakes in th
    */
  val userValid: PseudoClass = simple("user-valid")

  /** An element is :valid or :invalid when its contents or value is, respectively, valid or invalid with respect to
    * data validity semantics defined by the document language (e.g. [XFORMS11] or [HTML5]). An element which lacks data
    * validity seman
    */
  val valid: PseudoClass = simple("valid")

  /** User agents commonly display unvisited hyperlinks differently from previously visited ones. Selectors provides the
    * pseudo-classes :link and :visited to distinguish them:
    */
  val visited: PseudoClass = simple("visited")

  /** The :volume-locked pseudo-class represents an element that is capable of making sound, and currently has its
    * volume "locked" by the UA or the user, so the page author cannot change it. (For the audio and video elements of
    * HTML, see the algo
    */
  val volumeLocked: PseudoClass = simple("volume-locked")

  /** The :xr-overlay pseudo-class MUST match the overlay element for the duration of an immersive session using a DOM
    * Overlay.
    */
  val xrOverlay: PseudoClass = simple("xr-overlay")
end PseudoClassesGenerated

/** Generated simple pseudo-ELEMENTS from the webref selectors catalog. Mixed into `object PseudoElement`. */
trait PseudoElementsGenerated:
  /** Build a simple pseudo-element from its bare name (no `::`). Provided by `object PseudoElement`. */
  protected def simple(name: String): PseudoElement

  /** Represents a styleable child pseudo-element immediately after the originating element’s actual content. */
  val after: PseudoElement = simple("after")

  /** Each element rendered in the top layer has a ::backdrop pseudo-element, for which it is the originating element.
    */
  val backdrop: PseudoElement = simple("backdrop")

  /** Represents a styleable child pseudo-element immediately before the originating element’s actual content. */
  val before: PseudoElement = simple("before")

  /** The ::checkmark pseudo-element represents an indicator of whether the item is checked, and is present on
    * checkboxes, radios, and option elements.
    */
  val checkmark: PseudoElement = simple("checkmark")

  /** The ::clear-icon pseudo-element represents the portion of the input that allows the user to clear the input when
    * clicked if provided by the user agent. With appearance: textfield, the user agent must not generate this part.
    */
  val clearIcon: PseudoElement = simple("clear-icon")

  /** The ::color-swatch pseudo-element represents the portion of the control that displays the chosen color value. */
  val colorSwatch: PseudoElement = simple("color-swatch")

  /** The ::column pseudo-element represents the individual columns in a multi-column container. It only exists on
    * multi-column containers.
    */
  val column: PseudoElement = simple("column")

  /** The ::cue pseudo-element (with no argument) matches any list of WebVTT Node Objects constructed for the matched
    * element, with the exception that the properties corresponding to the background shorthand must be applied to the
    * WebVTT cue back
    */
  val cue: PseudoElement = simple("cue")

  /** The ::cue-region pseudo-element (with no argument) matches any list of WebVTT region objects constructed for the
    * matched element.
    */
  val cueRegion: PseudoElement = simple("cue-region")

  /** The ::details-content pseudo-element targets the additional information in a details element that can be expanded
    * or collapsed. It is an element-backed pseudo-element.
    */
  val detailsContent: PseudoElement = simple("details-content")

  /** The ::field-component pseudo-element represents the portions of the control that contain the date/time component
    * values.
    */
  val fieldComponent: PseudoElement = simple("field-component")

  /** The ::field-separator pseudo-element represents the portions of the control that separate the date/time component
    * values if the user agent provides those portions.
    */
  val fieldSeparator: PseudoElement = simple("field-separator")

  /** The ::field-text pseudo-element represents the portion of the input that contains the editable text. */
  val fieldText: PseudoElement = simple("field-text")

  /** The ::file-selector-button pseudo-element represents the button used to open a file picker, if the UA renders such
    * a button.
    */
  val fileSelectorButton: PseudoElement = simple("file-selector-button")

  /** The ::first-letter pseudo-element represents the first Letter, Number, or Symbol (Unicode category L*, N*, or S*)
    * typographic character unit on the first formatted line of its originating element (the first letter) as well as
    * its associated
    */
  val firstLetter: PseudoElement = simple("first-letter")

  /** The ::first-line pseudo-element represents the contents of the first formatted line of its originating element. */
  val firstLine: PseudoElement = simple("first-line")

  /** The ::grammar-error pseudo-element represents a portion of text that has been flagged by the user agent as
    * grammatically incorrect.
    */
  val grammarError: PseudoElement = simple("grammar-error")

  /** The ::marker pseudo-element represents the automatically generated marker box of a list item. (See [CSS-DISPLAY-3]
    * and [CSS-LISTS-3].)
    */
  val marker: PseudoElement = simple("marker")

  /** The ::picker-icon pseudo-element represents the part of the control that represents the icon denoting the presence
    * of the picker. It is only generated when the originating element has basic appearance and if it opens a picker. It
    * is a fully
    */
  val pickerIcon: PseudoElement = simple("picker-icon")

  /** The ::placeholder pseudo-element represents the portion of the input that contains the placeholder text. */
  val placeholder: PseudoElement = simple("placeholder")

  /** The ::reveal-icon pseudo-element represents the portion of the input that allows the user to temporarily disable
    * obscuring of sensitive text input when clicked if provided by the user agent. User agents providing ::reveal-icon
    * may choose to
    */
  val revealIcon: PseudoElement = simple("reveal-icon")

  /** When the computed content value of a ::scroll-marker pseudo-element is not none and its nearest ancestor scroll
    * container scroll container has a computed scroll-marker-group property that is not none, the pseudo-element
    * generates a box atta
    */
  val scrollMarker: PseudoElement = simple("scroll-marker")

  /** The ::scroll-marker-group fully styleable pseudo-element is generated by a scroll container element having a
    * computed scroll-marker-group property that is not none.
    */
  val scrollMarkerGroup: PseudoElement = simple("scroll-marker-group")

  /** The ::search-text pseudo-element represents text identified by the user agent’s find-in-page feature. Since not
    * all UAs style matched text in ways expressible with the highlight pseudo-elements, this pseudo-element is optional
    * to implement.
    */
  val searchText: PseudoElement = simple("search-text")

  /** The ::selection pseudo-element represents the portion of a document that has been selected as the target or object
    * of some possible future user-agent operation(s). It applies, for example, to selected text within an editable text
    * field, whi
    */
  val selection: PseudoElement = simple("selection")

  /** The ::slider-fill pseudo-element represents the portion containing the progressed portion of the control. When the
    * progress of control is indeterminate (like with <progress>), the user agent must give this portion an inline-size
    * of zero.
    */
  val sliderFill: PseudoElement = simple("slider-fill")

  /** The ::slider-thumb pseudo-element represents the portion that allows the user to adjust the progress of the
    * control.
    */
  val sliderThumb: PseudoElement = simple("slider-thumb")

  /** The ::slider-track pseudo-element represents the portion containing both the progressed and unprogressed portions
    * of the control.
    */
  val sliderTrack: PseudoElement = simple("slider-track")

  /** The ::spelling-error pseudo-element represents a portion of text that has been flagged by the user agent as
    * misspelled.
    */
  val spellingError: PseudoElement = simple("spelling-error")

  /** The ::step-control pseudo-element represents the portion of a number input that contains the up and down buttons.
    */
  val stepControl: PseudoElement = simple("step-control")

  /** The ::step-down pseudo-element represents the button that decrements the value inside a number input when
    * activated.
    */
  val stepDown: PseudoElement = simple("step-down")

  /** The ::step-up pseudo-element represents the button that increments the value inside a number input when activated.
    */
  val stepUp: PseudoElement = simple("step-up")

  /** The ::target-text pseudo-element represents text directly targeted by the document URL’s fragment, if any. */
  val targetText: PseudoElement = simple("target-text")

  /** The ::view-transition pseudo-element is a tree-abiding pseudo-element that is also a pseudo-element root. Its
    * originating element is the document’s document element, and its containing block is the snapshot containing block.
    */
  val viewTransition: PseudoElement = simple("view-transition")
end PseudoElementsGenerated

/** Generated typed element-tag selectors from the HTML element catalog (`elements/html.json`, the same source as
  * `Elements`). Mixed into `object Elem`, so `Elem.button` / `Elem.div` resolve here while the `tag(String)` escape
  * hatch lives in the companion. One `val` per element; void/non-void is irrelevant to selectors.
  */
trait ElemsGenerated:
  /** Build a type/element selector from its tag name. Provided by `object Elem`. */
  protected def tag(name: String): Sel

  val a: Sel               = tag("a")
  val abbr: Sel            = tag("abbr")
  val acronym: Sel         = tag("acronym")
  val address: Sel         = tag("address")
  val applet: Sel          = tag("applet")
  val area: Sel            = tag("area")
  val article: Sel         = tag("article")
  val aside: Sel           = tag("aside")
  val audio: Sel           = tag("audio")
  val b: Sel               = tag("b")
  val base: Sel            = tag("base")
  val basefont: Sel        = tag("basefont")
  val bdi: Sel             = tag("bdi")
  val bdo: Sel             = tag("bdo")
  val bgsound: Sel         = tag("bgsound")
  val big: Sel             = tag("big")
  val blink: Sel           = tag("blink")
  val blockquote: Sel      = tag("blockquote")
  val body: Sel            = tag("body")
  val br: Sel              = tag("br")
  val button: Sel          = tag("button")
  val canvas: Sel          = tag("canvas")
  val caption: Sel         = tag("caption")
  val center: Sel          = tag("center")
  val cite: Sel            = tag("cite")
  val code: Sel            = tag("code")
  val col: Sel             = tag("col")
  val colgroup: Sel        = tag("colgroup")
  val data: Sel            = tag("data")
  val datalist: Sel        = tag("datalist")
  val dd: Sel              = tag("dd")
  val del: Sel             = tag("del")
  val details: Sel         = tag("details")
  val dfn: Sel             = tag("dfn")
  val dialog: Sel          = tag("dialog")
  val dir: Sel             = tag("dir")
  val div: Sel             = tag("div")
  val dl: Sel              = tag("dl")
  val dt: Sel              = tag("dt")
  val em: Sel              = tag("em")
  val embed: Sel           = tag("embed")
  val fieldset: Sel        = tag("fieldset")
  val figcaption: Sel      = tag("figcaption")
  val figure: Sel          = tag("figure")
  val font: Sel            = tag("font")
  val footer: Sel          = tag("footer")
  val form: Sel            = tag("form")
  val frame: Sel           = tag("frame")
  val frameset: Sel        = tag("frameset")
  val h1: Sel              = tag("h1")
  val h2: Sel              = tag("h2")
  val h3: Sel              = tag("h3")
  val h4: Sel              = tag("h4")
  val h5: Sel              = tag("h5")
  val h6: Sel              = tag("h6")
  val head: Sel            = tag("head")
  val header: Sel          = tag("header")
  val hgroup: Sel          = tag("hgroup")
  val hr: Sel              = tag("hr")
  val html: Sel            = tag("html")
  val i: Sel               = tag("i")
  val iframe: Sel          = tag("iframe")
  val img: Sel             = tag("img")
  val input: Sel           = tag("input")
  val ins: Sel             = tag("ins")
  val isindex: Sel         = tag("isindex")
  val kbd: Sel             = tag("kbd")
  val keygen: Sel          = tag("keygen")
  val label: Sel           = tag("label")
  val legend: Sel          = tag("legend")
  val li: Sel              = tag("li")
  val link: Sel            = tag("link")
  val listing: Sel         = tag("listing")
  val main: Sel            = tag("main")
  val map: Sel             = tag("map")
  val mark: Sel            = tag("mark")
  val marquee: Sel         = tag("marquee")
  val menu: Sel            = tag("menu")
  val menuitem: Sel        = tag("menuitem")
  val meta: Sel            = tag("meta")
  val meter: Sel           = tag("meter")
  val multicol: Sel        = tag("multicol")
  val nav: Sel             = tag("nav")
  val nextid: Sel          = tag("nextid")
  val nobr: Sel            = tag("nobr")
  val noembed: Sel         = tag("noembed")
  val noframes: Sel        = tag("noframes")
  val noscript: Sel        = tag("noscript")
  val `object`: Sel        = tag("object")
  val ol: Sel              = tag("ol")
  val optgroup: Sel        = tag("optgroup")
  val option: Sel          = tag("option")
  val output: Sel          = tag("output")
  val p: Sel               = tag("p")
  val param: Sel           = tag("param")
  val picture: Sel         = tag("picture")
  val plaintext: Sel       = tag("plaintext")
  val pre: Sel             = tag("pre")
  val progress: Sel        = tag("progress")
  val q: Sel               = tag("q")
  val rb: Sel              = tag("rb")
  val rp: Sel              = tag("rp")
  val rt: Sel              = tag("rt")
  val rtc: Sel             = tag("rtc")
  val ruby: Sel            = tag("ruby")
  val s: Sel               = tag("s")
  val samp: Sel            = tag("samp")
  val script: Sel          = tag("script")
  val search: Sel          = tag("search")
  val section: Sel         = tag("section")
  val select: Sel          = tag("select")
  val selectedcontent: Sel = tag("selectedcontent")
  val slot: Sel            = tag("slot")
  val small: Sel           = tag("small")
  val source: Sel          = tag("source")
  val spacer: Sel          = tag("spacer")
  val span: Sel            = tag("span")
  val strike: Sel          = tag("strike")
  val strong: Sel          = tag("strong")
  val style: Sel           = tag("style")
  val sub: Sel             = tag("sub")
  val summary: Sel         = tag("summary")
  val sup: Sel             = tag("sup")
  val table: Sel           = tag("table")
  val tbody: Sel           = tag("tbody")
  val td: Sel              = tag("td")
  val template: Sel        = tag("template")
  val textarea: Sel        = tag("textarea")
  val tfoot: Sel           = tag("tfoot")
  val th: Sel              = tag("th")
  val thead: Sel           = tag("thead")
  val time: Sel            = tag("time")
  val title: Sel           = tag("title")
  val tr: Sel              = tag("tr")
  val track: Sel           = tag("track")
  val tt: Sel              = tag("tt")
  val u: Sel               = tag("u")
  val ul: Sel              = tag("ul")
  val `var`: Sel           = tag("var")
  val video: Sel           = tag("video")
  val wbr: Sel             = tag("wbr")
  val xmp: Sel             = tag("xmp")
end ElemsGenerated
