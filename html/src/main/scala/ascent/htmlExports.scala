package ascent

/** Export facade for `ascent-html`. Contributes to the OPEN `package ascent` so a plain `import ascent.*` exposes the
  * server-side renderer entry point.
  */
export ascent.html.Html
