package ascent

/** Export facade for `ascent-datastar-js`. Contributes to the OPEN `package ascent` so a view's plain `import ascent.*`
  * exposes the client runtime entry points.
  */
export ascent.datastar.js.{DatastarClient, Action}
